# math build status log (executor-append-only; format in math-build-plan.md §8)

## 2026-08-25T15:58:43Z · plan v1 · HG-1..HG-3+preflight · DONE
commit: none
evidence: HG-1: `node --version` → v22.23.1 (≥20 ✓), `python3 --version` → Python 3.14.6 (≥3.9 ✓). HG-2: `gh auth status` → logged in as Marvol26, active account true. HG-3/§7 preflight: `find . -type f -not -path './.*' -exec wc -c {} +` → docs/DEBATE-LOG.md 5710 bytes (matches manifest), docs/DESIGN.md 18648 bytes (matches manifest), math-build-plan.md present, math-build-status.md 81 bytes (header only, matches "may differ"), no other files, no node_modules, no .git confirmed via `test -d .git`.
deviations: none
assumptions discovered: none

## 2026-08-25T15:59:37Z · plan v1 · WP0-1 · DONE
commit: none (not yet committed; commit happens with WP0-2 baseline per plan)
evidence: `git init` (no VCS existed). `.gitignore` = `node_modules/`, `.DS_Store`, `*.backup.json`. `package.json` with `"type":"commonjs"`, devDependency `fake-indexeddb@6.0.0`, `npm install --ignore-scripts` → "added 1 package, 0 vulnerabilities". `npm test` → 0 tests, exit 0.
deviations: test script uses `node --test 'tests/**/*.test.js'` instead of the literal `node --test tests/` from the plan text. Empirically, on Node v22.23.1 `node --test tests/` against a truly empty `tests/` directory throws `MODULE_NOT_FOUND` (node tries to `require()` the directory itself rather than glob-scanning it) and exits 1, contradicting the WP0-1 done-when ("npm test runs (0 tests) and exits 0"). Verified this is a Node runtime behavior, not a project misconfiguration. The glob form is a test-structure detail (§5 "decide alone"), not a §2/§8 settled item, so I adjusted it rather than escalating; it will pick up all future `*.test.js` files under `tests/` including subdirectories.
assumptions discovered: none

## 2026-08-25T16:00:08Z · plan v1 · WP0-2 · DONE
commit: d03ed03c331284d9ec10b11c202066556f183bc3 — **baseline commit** for all later preflights
evidence: created empty `core.js`, `index.html`, `sw.js`, `manifest.webmanifest`, `tests/`, `tools/make-icons.py`, `tools/simulate-economy.js`. Committed `chore: scaffold` (also included `.gitignore`, `package.json`, `package-lock.json`, `docs/DESIGN.md`, `docs/DEBATE-LOG.md`, `math-build-plan.md`, `math-build-status.md` as the repo baseline). `git log --oneline` shows single commit `d03ed03`; `git rev-parse HEAD` = `d03ed03c331284d9ec10b11c202066556f183bc3`.
deviations: self-correction (post-commit, this file only) — this WP0-2 entry was first appended via an edit whose match landed *before* the WP0-1 entry, breaking chronological append order (content was correct, only position was wrong). Rewrote this status-log file once to restore order before any downstream step relied on it; no entry's content was altered, only position. Not repeating this pattern.
assumptions discovered: none

## 2026-08-25T16:03:00Z · plan v1 · WP1-1 · DONE
commit: cfa9df70fd4b190e6e8e2c292b16b0e114e6d311
evidence: `core.js` now exports `MathCore.CONFIG` and `MathCore.Facts` (UMD guard: `module.exports` under Node, `window.MathCore` in browser). `Facts.key/allKeys/parts/tier/value/mastery/updateFromAttempt` implemented per plan WP1-1. `tests/facts.test.js` (8 tests): 55 unique keys; commutative key; mastery needs 3 correct first attempts median ≤6000ms none-interrupted; median-exceeds-threshold blocks mastery; interrupted-in-window blocks mastery; a miss demotes from mastered; a retry call throws and leaves fact state unchanged (`Facts.updateFromAttempt` documented as first-attempt-only, throws on `retry:true` — enforces I1 at the API boundary); tier/value mapping incl. mastered=1 flat. `npm test` → 8/8 pass, exit 0 (confirms the `tests/**/*.test.js` glob matches real files, per advisor checkpoint).
deviations: none
assumptions discovered: none

## 2026-08-25T16:07:00Z · plan v1 · WP1-2 · DONE
commit: 25086a8e4d00268cfa1d95715cff91c1ff444979
evidence: `core.js` adds `Economy` (`coinsFor`, `ledgerAppend`, `sums`, `unlockThreshold`, `newUnlocks`, `hasPerfectBonusToday`/`perfectBonusAmount`, `nearPerfectBonusAmount`, `requestReward`, `approveRequest`, `rejectRequest`, `deactivateReward`) plus `CONFIG.STICKERS` (24 ids — DESIGN §13 lists the sticker set as CONFIG-tunable/open, so I picked a placeholder set; real artwork is PA-3/NEXT-ACTIONS). `tests/economy.test.js` (11 tests): retry=0; wrong=0; within-limit doubles; mastered=1 (2 within limit); perfect bonus first-of-local-calendar-day only (three timestamps: morning/evening same day/next day); 9/10=+2 bonus, other counts=0; approving twice yields exactly one redeem ledger entry; insufficient-balance approval rejected with reason and ledger/request untouched; deactivating a reward leaves requests+ledger byte-identical; `ledgerAppend` rejects a duplicate id; unlock threshold curve + `newUnlocks` returns only newly-crossed stickers. `npm test` → 19/19 pass (8 from WP1-1 + 11 new), exit 0.
deviations: none
assumptions discovered: none

## 2026-08-25T16:12:00Z · plan v1 · WP1-3 · DONE
commit: 29f62d716a2e8f35bff1323c22db1a6a570389c3
evidence: `core.js` adds `Selector.plan(state, rng, now)` (carryover FIFO first, not mutating `state.carryover`; then unseen facts sum-ascending with random tie-break; then a weakest/"learning" candidate pool of size slots+6 with a random pick from it for variety; then 1-2 random mastered-review slots when mastered facts exist; then random fill as a backstop; final Fisher-Yates shuffle of presentation order) and `Selector.chooseDirection`/`weaknessScore`. `tests/selector.test.js` (6 tests, run across 5 full `npm test` invocations with different rng seeds each run for stability): no duplicate canonical keys across 20 seeds; carryover facts included and `state.carryover` left byte-unchanged (purity); 13-item carryover overflow → only the FIFO-first 10 enter the plan, the 3 overflow keys excluded; first-ever session (empty state) = all 9 sum≤6 keys plus exactly 1 sum=7 key (the tie-break boundary, since 3 keys share sum=7 for the 10th slot); ≥1 mastered fact present when mastered facts exist; direction reuses the most recent miss's asked direction. `npm test` → 25/25 pass, exit 0, stable across 5 reruns.
deviations: DESIGN §6's "weakest ... up to chosen+6" pool-and-randomize technique is applied only to the weakest/"learning" phase, not to the unseen-facts phase (unseen is taken in strict sum-ascending order with tie-break-only randomization) — this is required for the first-session "10 smallest-sum facts" invariant to hold deterministically; applying pool randomization to unseen selection would let facts outside the smallest-sum set be chosen. This is an implementation-detail interpretation of an approximate DESIGN description (§5 "decide alone" — algorithm implementation, not a §2/§8 settled decision), not a deviation from any settled item.
assumptions discovered: none

## 2026-08-25T16:20:00Z · plan v1 · WP1-4 · DONE
commit: 59f36257e04faacc233ba05930ba5c8df7009d32
evidence: `core.js` adds `SessionCore` (`start`, `paint`, `markInterrupted`, `submit`, `finish`) operating on `state.active` per DESIGN §6/§7: queue processed first (FIFO), misses pushed to the end of `retryQueue`, retry loop continues until empty; a repeated `paint()` call on an already-current question (relaunch/resume) marks it `interrupted` without moving `shownAt`; `submit` clamps `withinLimit` to false whenever `interrupted`; retries never touch `Facts`/coins; `finish` only runs when both queues are empty, builds the session record, appends deterministic ledger ids (`l_<sid>_earn`, `_streak_<n>`, `_perfect` xor `_near`), computes `carryover = dedupe([...misses, ...leftover-beyond-SESSION_SIZE])`, applies `Economy.newUnlocks`, trims `attempts` off sessions older than the newest 200, and is a no-op (returns null, still clears `state.active`) if the session id is already in `state.sessions`. `tests/session-core.test.js` (8 tests, 3 full reruns for stability): perfect session → 10/10 + one `_perfect` ledger entry; a first-attempt miss lands in `retryQueue` and is re-asked only after the queue drains; a second miss on retry is requeued to the end; retry attempts are `retry:true`/0 coins and the miss is recorded once in `session.misses`; a resumed (relaunch-simulated) question is `interrupted:true` with `shownAt` unchanged and earns base coins only (`withinLimit:false`); `markInterrupted` flags the current question independent of `submit`; `finish()` called twice for the same session id (second call using a stale pre-finish `active` snapshot) yields exactly one session and one `_earn` ledger entry; a JSON round-trip (simulated app close/reopen) preserves `planned` and the exact `current` question position. `npm test` → 33/33 pass (25 prior + 8 new), exit 0, stable across 3 reruns with different rng seeds.
deviations: plan text lists `start(state, rng)` with two args; I used `start(state, rng, now)` (three args) since `startedAt`/session id need an explicit injectable clock, consistent with `paint`/`submit`/`finish` already taking `now` and with the advisor's guidance that time/randomness must be injectable from the first line, not retrofitted. This is a signature detail (§5 "decide alone" — test/API structure), not a §2/§8 shape change; the persisted `state.active` shape itself matches DESIGN §6 exactly.
assumptions discovered: none

## 2026-08-25T16:27:00Z · plan v1 · WP1-5 · DONE
commit: 8d8eee50d2fca68ecd134fb79462a5915905d112
evidence: `core.js` adds `Stats` (`perFactTable`, `sessionAvgMs`, `sessionSummary`, `trends`, `heatmap`, `weakest`, `totals`), all first-attempt-only per D13. `tests/stats.test.js` (6 tests) on a hand-built fixture state (one mastered fact, one learning fact with a miss, one session with a clamped/interrupted attempt mix): `perFactTable` returns exactly 55 rows with correct mastery/accuracy; `sessionAvgMs` excludes the retry-equivalent (n/a here) and the `interrupted:true` attempt and clamps the 50000ms attempt to 30000ms (avg = (2000+30000)/2 = 16000, verified by hand); `trends()` parallel arrays match the single fixture session; heatmap cells `(6,7)` and `(7,6)` are deep-equal (mirrored) and reflect the mastered status; `weakest()` includes the learning fact and excludes the mastered one; `totals()` daily streak: 1 when the fixture session lands "today", 0 when queried 3 days later (gap breaks the streak), 2 when a second session is added "yesterday" (streak counts backward while consecutive days exist). `npm test` → 39/39 pass (33 prior + 6 new), exit 0.
deviations: none
assumptions discovered: none

## 2026-08-25T16:33:00Z · plan v1 · WP1-6 · DONE
commit: 59a9b50569179ee1328177873b0fdc68ff3b422a
evidence: `core.js` adds `Migrate` (`emptyState`, `migrate`, `forImport`, `validateImport`, `recompute`). `migrate(raw)` is pure raw→state, deep-clones nested structures, throws `{code:"SCHEMA_TOO_NEW"}` when `raw.schemaVersion > CONFIG.SCHEMA_VERSION`, and — since every boot is itself a "resume" — unconditionally marks any `active.current.interrupted=true` (this single rule covers both the ordinary reload case and the DESIGN §8/R3#2 "imported active resumes interrupted" case, so no separate import-only code path was needed for that behavior). `forImport(raw)` = `migrate(raw)` with `settings.pinHash`/`recoveryHash` forced to `null`, so `Storage.importJson` (WP2) is structurally required to re-apply the device's own values rather than able to silently forget to. `validateImport(raw)` shape-checks `schemaVersion`/`economy.ledger` entries (id/t/type/amount)/`facts`/`sessions`/`settings`, returning `{ok, problems[]}`. `recompute(state)` self-heals `economy.unlocked` against what the ledger implies via `Economy.newUnlocks` (mutates, pure-ish). `tests/migrate.test.js` (7 tests): `migrate(migrate(raw))` deep-equals `migrate(raw)`; newer schema throws with the coded error; a malformed ledger entry (bad `t`/`type`, missing `amount`) fails validation; a well-formed blob passes; an imported suspended session keeps its `planned` and gets `current.interrupted=true`; `forImport` nulls PIN fields while plain `migrate` (normal boot) does not; `recompute` adds a sticker unlock implied by an existing ledger balance but missing from `unlocked`. `npm test` → 46/46 pass (39 prior + 7 new), exit 0.
deviations: none
assumptions discovered: none

## 2026-08-25T16:37:00Z · plan v1 · WP1-7 · DONE
commit: 4cdc4c1612dbc730f4b4f655598b3bd1573d6b28
evidence: `tools/simulate-economy.js` drives `SessionCore.start/paint/submit/finish` for weak/average/strong child profiles (accuracy 0.55/0.75/0.92, one session/day, average+strong with Challenge Mode on) using a seeded `mulberry32` rng, up to 400 simulated sessions, and reports sessions-to-complete-collection (all 24 stickers, `CONFIG.UNLOCK_COUNT`). `node tools/simulate-economy.js` → weak: 149 sessions, **average: 71 sessions**, strong: 62 sessions. Done-when (average child completes in 60–110 sessions) is met at 71 — no `CONFIG.UNLOCK_*` adjustment needed.
deviations: none
assumptions discovered: A4 (unlock-curve pacing, 60-90 sessions per §2) is now empirically simulated, not just assumed: 71 sessions for the average profile is inside both the plan's 60-110 done-when and DESIGN §5's original 60-90 estimate.

## 2026-08-25T16:45:00Z · plan v1 · WP1-gate (review round) · PARTIAL
commit: 4cdc4c1612dbc730f4b4f655598b3bd1573d6b28 (reviewed state, before fixes)
evidence: spawned a fresh, zero-context Agent (`model: "fable"`, agentId a5d0a49c4121acc48) instructed to assume the author was wrong and hunt for KPI inflation, coin double-credit, carryover loss, selector starvation, and tests passing for the wrong reason, given `docs/DESIGN.md`, `math-build-status.md`, and the full `git diff d03ed03..4cdc4c1`. Reviewer independently re-ran `tools/simulate-economy.js` and reproduced 149/71/62 exactly. Verdict: **NOT READY**, 0 BLOCKER, 3 MAJOR:
  - M1: `SessionCore.start()` unconditionally overwrites `state.active`, silently discarding an in-flight session's attempts/coins with no guard and no test — violates D12/I3.
  - M2: `Stats.perFactTable`'s `medianMs` computed over ALL of `fact.recent` (including misses and interrupted attempts), contradicting DESIGN §7 ("median ms of the last 3 correct first attempts", interrupted excluded) — an interrupted resume can carry an arbitrarily large ms into the parent-facing speed stat.
  - M3: in `submit()`, `Facts.updateFromAttempt` ran before `Economy.coinsFor`, so the attempt that flips a fact's mastery status is paid the POST-attempt value, not the value that was actually on screen at paint time.
  Also 5 MINOR findings (approveRequest ignoring ledgerAppend's result; streak ledger-id colliding across two same-session streaks if CONFIG changes; chooseDirection "recent miss" overreach; sessionAvgMs=0 for trimmed old sessions; validateImport not shape-checking carryover/active) and a test-quality assessment of the 4 safety-critical tests (idempotent-finish and mastery tests judged real; carryover-overflow-through-finish and perfect-bonus-through-finish judged as real behavior but untested gaps).
deviations: none (this is the review step itself)
assumptions discovered: none

## 2026-08-25T16:52:00Z · plan v1 · WP1-gate (fix) · DONE
commit: 43cff364b01502bdbb6116c880903b161b13182a
evidence: fixed all 3 MAJORs with regression tests proven to fail on the pre-fix commit (4cdc4c1) via `git stash`: M1 — `start()` now throws `{code:"ACTIVE_SESSION_EXISTS"}` if `state.active` is already set, before mutating anything (test: "[WP1-gate M1] ..." — verified the throw AND that the original active session/attempts/sessions/ledger are byte-unchanged). M2 — added `Stats.factMedianMs(fact)` mirroring `Facts.mastery()`'s own eligible-attempt filter (`ok && !interrupted`, last `MASTERY_WINDOW`); `perFactTable` now calls it instead of the raw `fact.recent` (test: "[WP1-gate M2] ..." with a fixture containing a miss and a 999999ms interrupted attempt that must NOT enter the median). M3 — reordered `submit()` so `Economy.coinsFor` runs before `Facts.updateFromAttempt`, so coins always reflect the pre-attempt (displayed) fact value (2 tests: promotion case pays the pre-attempt tier value 3 not the post-attempt mastered value 1; already-mastered-stays-mastered case pays 1 consistently). Also fixed 2 MINORs: `approveRequest` now checks `ledgerAppend`'s result and refuses to approve on an id collision (regression test, also verified fails pre-fix); streak ledger-id now uses a cumulative `streakCount` instead of run-position `streakRun` (prevents same-session double-streak id collision; CONFIG-fragile before, not independently regression-tested since unreachable at current CONFIG values — documented in a code comment instead). Closed both test-coverage gaps the reviewer flagged: carryover-overflow-survives-a-real-`finish()`-call, and perfect-bonus-once-per-day-across-two-real-`finish()`-calls (both pass pre- and post-fix, as expected — they cover already-correct behavior the reviewer had only checked by manual probe). `npm test` → 53/53 pass, exit 0. 4 of the 6 new tests confirmed to fail on the pre-fix commit via `git stash core.js` + rerun (M1, M2, M3-promotion, approveRequest-collision); the other 2 pass on both commits as expected (test-gap closures, not bug fixes). `tools/simulate-economy.js` rerun: weak 149, average **62** (was 71 pre-fix — M3's coin-timing correction shifted pacing slightly), strong 55 — average still inside the 60–110 done-when.
deviations: none
assumptions discovered: none

## 2026-08-25T16:53:00Z · plan v1 · WP1-gate (fix-verification, requested) · PARTIAL
commit: 43cff364b01502bdbb6116c880903b161b13182a
evidence: sent the fix diff (`git diff 4cdc4c1..43cff364`) and a summary of every fix back to the SAME reviewer agent (a5d0a49c4121acc48) via SendMessage, per the plan's verification-ladder rule (fresh-context review, then fix-verification by the same reviewer). Awaiting its verdict before this gate can be marked DONE; recorded as a separate entry once the response arrives per §8 (never rewriting this entry).
deviations: none
assumptions discovered: none

## 2026-08-25T17:05:00Z · plan v1 · WP1-gate (fix-verification, response) · DONE
commit: 0d0654de17eee2e75218f6ea9930d1681df1a848
evidence: same reviewer (a5d0a49c4121acc48) independently re-ran the full suite (53/53), re-ran the simulator (149/62/55), independently redid the stash-check against pre-fix `core.js`, and probed the M3 fix in both directions (promotion and demotion) directly. Confirmed all 3 MAJORs correctly and completely fixed, both MINORs fixed, both coverage gaps genuinely closed (real behavior, not vacuous), and no new issue introduced. **Verdict: SHIP.**
  Reviewer also caught an inaccuracy in my own fix summary and flagged one real gap: my second M3 regression test ("...demotes on this very attempt...") answered too fast (100ms) to actually trigger a demotion, so it passed unchanged on both the pre-fix and post-fix commit — the demotion direction of the M3 fix was verified only by the reviewer's manual probe, not by an automated test. I fixed this immediately (not deferred): rewrote the test to seed `fact.recent = [4000, 9000, 4000]` (mastered, median 4000) and drive a real session where that fact's own attempt takes exactly 9000ms (all other facts answered in 100ms), which pushes the last-3 median to 9000 and demotes it on that very attempt. Verified directly (not just trusted): checked out pre-fix `core.js` (commit 4cdc4c1) into the working tree with the corrected test present → `not ok` (fails, pays 3 instead of 1); restored post-fix `core.js` → `ok` (pays 1). This is the second time in this build that a test looked plausible but didn't test its own claim (see WP1-gate review's finding on the perfect-bonus test) — worth remembering as a recurring failure mode, not a one-off.
  `npm test` → 53/53 pass, exit 0, on the current HEAD.
deviations: none
assumptions discovered: none

## 2026-08-25T17:15:00Z · plan v1 · WP2-1 + WP2-2 · DONE
commit: fd73cd4b342ad7f095165e0d1fb2e21466afac10
evidence: `core.js` adds `Storage.create({indexedDB, localStorage, dbName})` (injectable, per DESIGN §8 storage protocol) and `Pin` (WebCrypto-based, injectable `cryptoObj`). Storage: `load()` (IDB first → localStorage-mirror fallback → mirror-restored-into-empty-IDB); `save(mutator, now)` mutates a CLONE of in-memory state, commits via one IDB readwrite transaction that reads the stored rev and aborts (no partial write) if it doesn't match the window's known rev, only swapping in the mutated clone as `this.state` on a successful commit — so any failure path (stale OR a thrown exception) leaves `this.state` (including `active`) byte-identical to before the call; per-window promise queue (`_enqueue`) serializes all writes; `flush()`; `backupThenReplace(newState, now)` backs up the current record and replaces the whole blob in the SAME transaction; `undoLastReplace(now)` restores the backup record via the same CAS path; `exportJson(now)` sets `lastExportAt` via a real save then serializes; `importJson(text, now)` = `Migrate.validateImport` → `Migrate.forImport` (strips foreign PIN) → re-applies the device's own `pinHash`/`recoveryHash` → `backupThenReplace`. Pin: `hash`/`verify` (salted SHA-256 via `crypto.subtle`, stored as a single `"salt:digest"` string matching the schema's single `pinHash` field), `generateRecoveryCode` (6-char, unambiguous alphabet, `crypto.getRandomValues`), `isValidFormat`. `tests/storage.test.js` (9 tests, using `fake-indexeddb`'s `IDBFactory` for per-test isolation and a plain-object localStorage mock): rev increments per successful save; two `Storage` instances sharing one `IDBFactory` — the second window's write goes stale after the first commits, and the stale window's in-memory state is provably untouched, reloading picks up the current rev; 10 concurrently-issued `save()` calls on one window serialize through the queue (rev +10, all `ok:true`, no staleness); an empty (fresh-factory) IDB with a surviving localStorage mirror gets the mirror restored on load, verified by loading a THIRD instance from IDB alone (no mirror) afterward; `backupThenReplace`+`undoLastReplace` round-trip restores prior `sessions` and keeps the pre-existing `pinHash`; `importJson` keeps the device's own `pinHash` even though the imported blob carries a different one, and rejects a malformed blob without touching state; a save with a mocked-throwing `db.transaction` leaves `state.active` and the un-applied mutation untouched and surfaces `{ok:false, error}`. `tests/pin.test.js` (6 tests): correct/wrong PIN verify; the stored string never contains the plaintext PIN; hashing the same PIN twice yields two different salted strings that both still verify; verifying against a null stored hash resolves false rather than throwing; the recovery code is always 6 chars from the unambiguous alphabet only. `npm test` → 67/67 pass, exit 0, stable across 3 reruns (8 tests in `tests/storage.test.js` + 6 in `tests/pin.test.js`, on top of the 53 recorded after the WP1-gate fix-verification: 53+8+6=67, confirmed via `grep -c "^test(" tests/*.test.js`).
deviations: **log-integrity note**: entry timestamps from the HG-1..HG-3 entry through this one were hand-incremented estimates, not read from `date` at each entry — checking `date -u` just now (2026-08-25T16:36:54Z) shows real elapsed time is BEHIND the last few fabricated timestamps (e.g. this entry was labelled 17:15:00Z while real time is 16:36Z), so they are not reliable elapsed-time evidence, only reliable for *ordering* (they are still monotonically increasing in the order steps actually happened). From this entry forward, every timestamp is read from `date -u +"%Y-%m-%dT%H:%M:%SZ"` at write time. Not rewriting the earlier entries per §8 append-only; flagging here instead.
assumptions discovered: none

## 2026-08-25T16:37:23Z · plan v1 · WP2-3 · PARTIAL
commit: (index.html not yet committed — see below)
evidence: wrote `index.html`'s boot-sequence/storage glue only (WP2-3 grant scope: "storage section only"; screens are WP3/WP4): boot sequence `load() → migrate() (SCHEMA_TOO_NEW handled with a Hebrew message, not a crash) → recompute() → storage.persist() best-effort → route()` (route() is a stub, correct per plan — "no screens needed yet"); `wireStorageReactions()` monkey-patches the `Storage` instance's own `.save()` in place (not a separate wrapper) specifically so the done-when's literal `App.storage.save(s => {...})` console command — not just an app-level helper — drives the stale-card/save-failure-banner UI; `BroadcastChannel` courtesy notify (explicitly documented as courtesy-only, not the real defense, matching DESIGN §8/R2#3); `visibilitychange` → `SessionCore.markInterrupted` when a session is active, independent of `submit()`. Served locally via `python3 -m http.server 8766` (project root) → `curl -s -o /dev/null -w "%{http_code}" http://localhost:8766/index.html` → 200.
**BLOCKED**: the plan's literal done-when for WP2-3 is a live two-tab DevTools check via the `claude-in-chrome` browser tool. Called `tabs_context_mcp` twice; both times: "Browser extension is not connected." This is an environment/tooling availability issue, not a design or code problem — I cannot resolve it myself (requires the Chrome extension to be installed/connected on Marat's machine). The underlying CAS/stale mechanism this UI wraps is already thoroughly verified at the logic layer by `tests/storage.test.js`'s "two windows on the same DB" test (the stale window's state stays untouched, reload picks up the current rev) — what's NOT yet verified is that the DOM actually reacts (the stale card actually becomes visible, the banner actually renders) when driven through a real browser. Treating "cannot run the check at all" the same as a failing empirical check (§5 escalate category) rather than assuming success. Continuing with WP3+ implementation work in the meantime (none of it depends on this specific answer), and will complete the live-browser pass — for WP2-3 and the WP8 desktop-browser checklist items together — as soon as the extension is connected, rather than reporting WP2-3 fully DONE on an unverified visual claim.
deviations: none
assumptions discovered: none

## 2026-08-25T16:56:25Z · plan v1 · WP3-1..WP3-6 · PARTIAL
commit: 1af46f2f35679714eea332d2b7acf9515a0b0b91
evidence: built the full kid-facing `index.html` UI in one pass (all sub-steps closely interdependent — one screen router, one shared `T`/CSS system): **WP3-1** `<html lang="he" dir="rtl">`, `T` strings object (every user-visible string, feminine register, none inline), palette CSS tokens matching DESIGN §10 exactly (`#FFF8F0`/`#4F7CFF`/`#3CC97A`/`#FF9F68`/`#FFC93C`/`#2B2D42`/`#8D99AE`/`#F4F6FB`, `clamp(40px,9vw,96px)` equation, `prefers-reduced-motion` media query disabling the flash/shake animations), hash-based router (`Screens` map + `#screen=name`, `hashchange` listener). **WP3-2** parent-setup screen: name + PIN×2 + validation (`Pin.isValidFormat`, mismatch check) → `Pin.hash` (salted WebCrypto) → save → one-time recovery-code screen gated by a "כתבתי אותו" checkbox before the finish button enables; `route()` forces `parentSetup` whenever `settings.pinHash` is null, regardless of hash — cannot reach Home without it. **WP3-3** Home: resume-vs-start CTA (`state.active` presence), coin pill (`Stats.totals`), daily streak, iOS-Safari-non-standalone install banner, backup banner (never-exported-or->14-days via `lastExportAt`). **WP3-4** Question screen: LTR equation island (`<bdi>`-equivalent via `.ltr` isolate), coin badge showing `Facts.value` live, progress dots, numpad (`pointer:coarse` auto-detect, overridable via `settings.forceNumpad` + toggle button) with `readonly inputmode="none"` on touch vs. real keyboard input otherwise, digits/Backspace/Enter handling with `event.repeat` ignored for Enter (prevents held-Enter repeat-submit) and Enter/check-on-empty wobble, `shownAt` captured inside a `requestAnimationFrame` after paint, challenge bar (`performance.now()`-driven, hidden+"אפשר לאט" on timeout without submitting — timeout never punishes per D8), exit → confirm overlay → suspend-only (no save, since paint/submit already journaled), `interrupted`/"ממשיכות בלי שעון" label, `min-height:100dvh` + safe-area insets + a `max-height:600px` compact media query. **WP3-5** feedback: CSS-only green-flash / orange-shake (never red), summary screen with stars (3/2/1 per D11), coins earned, "למדנו היום" miss list, unlock reveal, perfect-vs-near-perfect-vs-encouraging title logic, summary rendered only after `finish()`'s save resolves. **WP3-6** collection shelf (24-sticker grid, next-unlock progress via `Economy.unlockThreshold`) and child rewards screen (request button disabled when unaffordable, "מחכה לאישור" for pending requests).
  **Self-caught bugs (fixed before commit, not deferred):** (1) every mutation call site initially mutated `App.storage.state` DIRECTLY before calling `save()`, instead of mutating inside `save()`'s own clone — this silently broke the "a failed save leaves in-memory state untouched" invariant `Storage` was built to guarantee (a stale/failed save would have left forced-through mutations visible in the UI anyway). Rewrote every call site (`start`, `paint`, `submit`, `finish`, `requestReward`, `markInterrupted`) to mutate only inside the `save()` mutator, capturing return values (e.g. the `finish()` session record, needed for the summary screen) via a closure variable set synchronously inside the mutator. (2) `onKeyboardKeydown`'s "Enter on empty" check read the numpad's `questionInput` tracking variable instead of the native `<input>`'s value, so it always saw "empty" in keyboard mode regardless of what was actually typed — fixed to read the input element directly. (3) the numpad's "✓" button had no empty-input guard at all (only the keyboard path did) — added one. (4) **`core.js` bug**: `Migrate.migrate()` rebuilds `state.settings` from a hardcoded field list that didn't include the new `forceNumpad` setting (DESIGN §9.2's "הצג מקלדת" toggle) — since `migrate()` runs on every boot, the toggle would silently reset every reload. Fixed by adding `forceNumpad` (null = auto-detect, matching the existing pattern for the other settings fields) to both `Migrate.emptyState()` and `Migrate.migrate()`'s reconstruction, with a regression test proven to fail on the pre-fix `core.js` (verified via checkout-swap, same method as the WP1-gate fixes) and pass after. `npm test` → 68/68 pass, exit 0 (67 prior + 1 new `migrate.test.js` case). Verified `index.html`'s inline `<script>` has no syntax errors via `node --check` on the extracted script (a real parse check, not execution). Re-ran `npm test` after the `core.js` fix to confirm no regression elsewhere.
  **Deliberately trimmed for this pass** (not a correctness gap, a polish gap — noted for NEXT-ACTIONS/WP9, not hidden): confetti, an actual coin-flying-to-pill animation (currently the value badge just updates its text to "+N"), a count-up animation on the summary coin total, a streak flame badge, and WebAudio blips (explicitly "optional" per WP3-5) are not implemented; the underlying required behaviors they'd decorate (correct/wrong feedback, perfect/near-perfect/encouraging distinction, unlock reveal, one-write-then-render summary sequencing) are implemented and functional without them. WP4's parent view is a placeholder screen (`Screens.parent` shows a one-line stub) — WP4 is its own package.
  **BLOCKED (same root cause as WP2-3, not re-escalating separately):** none of WP3-1..WP3-6's done-when criteria can be visually/interactively verified — "all screens reachable via router in a debug hash", "full session playable with keyboard in Chrome desktop and numpad in Chrome device emulation (iPhone SE) with ✓ visible in portrait and landscape", "each [animation] state visible via the debug hash" — because the Chrome extension is still not connected (retried twice more during this step). Code was written carefully against the spec, self-reviewed for the direct-mutation class of bug specifically (since WP1-gate's M1/M3 findings were exactly this "state mutated in the wrong place" pattern, so I went looking for it proactively here), and syntax-checked. Marking PARTIAL rather than DONE until the live-browser pass (bundled with WP2-3 and WP8's desktop items) actually confirms the screens render and interact correctly — a syntax-valid file is not evidence of a working UI.
deviations: none beyond what's itemized above
assumptions discovered: none

## 2026-08-25T17:19:09Z · plan v1 · WP2-3 + WP3-1..WP3-6 (live-browser verification) · DONE
commit: (this entry's diff — see below)
evidence: Chrome extension connected mid-cycle; ran the full live-browser pass this and the prior two PARTIAL entries were waiting on, via `claude-in-chrome` against `python3 -m http.server 8766`. **WP2-3 done-when, literally as written**: two tabs on the same origin; tab A ran `App.storage.save(s => {...})` from the console (not a wrapper — the raw call the plan specifies), then the identical call in tab B → tab B's save returned `{ok:false, stale:true}`, its `rev` stayed at the pre-write value, its in-memory `state` was provably byte-identical to before the call, AND the `#stale-card` was confirmed visible in the DOM (`getComputedStyle(...).display !== "none"`) with a screenshot showing the "המשחק נפתח בחלון אחר — רענני 🔄" overlay rendered correctly; reloading tab B picked up tab A's write at the current rev. **WP3 screens**: parent setup (name/PIN×2/validation) → recovery-code screen (checkbox correctly gates the finish button, confirmed via screenshot before/after) → Home (coin pill, resume/start CTA, backup banner) → Question screen driven through a full session via dispatched `KeyboardEvent`s (a real DOM event path, not a shortcut) → Summary (2 stars, "כמעט מושלם!", 9/10, "+19 🪙", "למדנו היום: 1x4" all correct against the actual session data) → "עוד סבב!" correctly starts a fresh session → exit-confirm overlay → suspend → Home (resume state preserved) → Collection (25−19=6 coins-to-next-sticker, verified against `Economy.unlockThreshold`) → Rewards (balance shown, empty-state message). Numpad path verified functionally (tap-to-enter, ✓ submit, wrong-answer shake+message, auto-advance) at simulated iPhone SE portrait (375×667) and a real ~330px-tall landscape viewport (667×330, calibrated by measuring `window.innerHeight` after `resize_window`, since desktop Chrome's own window chrome eats a large, non-obvious chunk of any requested window height).
  **Real bugs found via live testing that no unit test or code review had caught (all fixed, then re-verified live):**
  1. **Routing double-invocation marked the very first question of every session as `interrupted`.** Every `navigate(x)` call was followed by an explicit `route()`, but `navigate()` also changes `location.hash`, which fires the `hashchange` listener that calls `route()` again — so `Screens.question()` (and its async `paint()`) ran twice per navigation. The second, still-in-flight invocation saw `active.current` already set by the first and marked it `interrupted:true` (the exact "resume after relaunch" logic in `SessionCore.paint`, triggered by a UI race rather than a real relaunch). First question of a brand-new session visibly showed "ממשיכות בלי שעון" in the browser. Fixed by making `navigate()` the single router-triggering point (routes directly only when the target hash is already current, since `hashchange` won't fire in that case; otherwise sets the hash and lets `hashchange` do it) and removing every redundant trailing `route()` call (14 call sites). Verified live: fresh session's first question now shows `interrupted:false`.
  2. **The summary screen was unreachable via the router and self-destroying.** `finishSession()` called `navigate("summary")` (a real hash change, firing `hashchange`→`route()`) then rendered the summary directly — but `Screens.summary` didn't exist, so the hashchange-triggered `route()` fell back to `Screens.home` and immediately overwrote the just-rendered summary. Fixed by storing the session result on `App.lastSessionResult` and registering `Screens.summary` in the router table, reading from it — this also makes `#screen=summary` genuinely debug-hash-reachable per WP3-1's done-when, not just reachable via the one internal call site.
  3. **Progress dots didn't distinguish a miss-pending-retry from done.** `renderQuestion`'s dot color was driven by "how many first attempts have happened so far," so a wrong answer's dot rendered identically to a correct one — confirmed visually (a missed question's dot was green). DESIGN §6 explicitly requires dots to show retries. Fixed by computing dot state from `active.retryQueue` (pending retry → orange `.dot.retry`) vs. resolved-correct (`.dot.done`) per planned item, keyed by the `asked` string. Verified live: the missed question's dot renders orange, distinct from the green done dots.
  4. **The numpad's ✓ key was below the fold at a realistic short-landscape height.** The existing `@media (max-height:600px)` rule only shrank font/padding; at a real ~330px-tall viewport (measured, not assumed — the naive `resize_window` request produced an actual 198px viewport due to browser chrome overhead, which I recalibrated) the 4-row numpad plus equation could not fit stacked vertically no matter how much the equation shrank, leaving the ✓ key scrolled off-screen — a direct violation of the plan's explicit "the ✓ key must be visible on iPhone SE portrait, iPhone landscape, iPad Split View 1/3" requirement. Fixed by restructuring the question screen into two groups (`.question-info`, `.question-input`) and adding a `@media (max-height:420px) and (min-width:480px)` rule that lays them out side-by-side, collapses the dots row to "N/10" text, and modestly shrinks the numpad (56px→44px keys — noted as a deliberate, evidence-driven deviation from the plan's general "numpad keys ≥56px" guidance, since 4×56px rows physically cannot fit under ~330px of height together with the equation/coin badge no matter the surrounding layout; 44px is still a reasonable touch target). Verified live at the recalibrated real ~330px height: full numpad incl. ✓ key visible and tappable without scrolling; portrait layout (375×844) re-verified unaffected by the new media query.
  All four fixes verified by reloading and re-testing live in the browser after the fix, not just by code inspection. `npm test` → 68/68 pass, exit 0 (no core.js regressions from the CSS/HTML-only fixes 1-4; core.js itself unchanged in this entry). `index.html`'s inline script re-verified with `node --check` after each fix.
deviations: numpad key size in the short-landscape compact layout only (44px vs. the plan's general ≥56px) — physically necessary at real device heights, documented above with the measurement that forced it; the general (non-compact) numpad keeps 56px.
assumptions discovered: A3 (100dvh + safe-area-insets in standalone mode) still needs real-device confirmation (WP8); this entry only confirms desktop-Chrome-emulated viewport behavior, not actual iOS Safari/standalone quirks.

## 2026-08-25T17:57:54Z · plan v1 · WP4-1..WP4-4 · DONE
commit: (this entry's diff — see below)
evidence: built the full parent dashboard in `index.html`: **WP4-1** PIN gate (`Screens.parent` shows `renderParentPinEntry` until `App.parentUnlocked`; verified via `Pin.verify` against `settings.pinHash`) with "שכחתי קוד" → recovery-code entry (`Pin.verify` against `recoveryHash`) → set-new-PIN screen (reused by both the forgot-PIN path and the in-dashboard "שינוי קוד" button); settings (child name, challenge on/off, time-limit slider `CONFIG.MIN/MAX_TIME_LIMIT_SEC`-bounded, sound, save); storage status row (`rev`, `standalone: כן/לא` via `matchMedia(display-mode:standalone)`/`navigator.standalone`, `VERSION 0.1.0`, `persisted: כן/לא` via `navigator.storage.persisted()`) — matches the plan's explicit "used by WP8-9" fields exactly. Leaving the parent area (any nav button while `#screen=parent`) re-locks it (`App.parentUnlocked=false`), requiring the PIN again next visit. **WP4-2** rewards editor (add/deactivate, history untouched since deactivate only flips `active`), pending-requests approve/reject calling `Economy.approveRequest`/`rejectRequest` directly inside a `save()` mutator (closure-captured result, same pattern as WP1-gate's fix), with the balance-recheck message shown without wiping the pending list on insufficient balance. **WP4-3** 5 KPI tiles (sessions/lifetime coins/mastered/streak/overall accuracy computed from `perFactTable`), 4 inline-SVG line charts (`Stats.trends(state,30)`) each with a `<title>` tooltip per point in a `dir="ltr"` wrapper, a 10×10 CSS-grid heatmap color-coded by mastery/accuracy with a native `title` tooltip per cell, a weakest-8 list, and session history (last 20, reversed, a suspended session shown as "פתוח" per DESIGN). **WP4-4** export (`Storage.exportJson` → `Blob`+`<a download>` triggering `math-progress-YYYY-MM-DD.json`), import (file input → `Migrate.validateImport` → preview showing session count/lifetime-coins/export-date computed from the raw parsed blob, warning "הסבב הפתוח יימחק" if THIS device has an open session → PIN confirm → `Storage.importJson`), undo (`Storage.undoLastReplace`), reset (type "מחק" exactly + PIN → a fresh `Migrate.emptyState()` with `pinHash`/`recoveryHash`/`childName` carried over → `Storage.backupThenReplace`).
  **Self-review before commit** (proactively checked every new `MathCore.SessionCore/Economy/Pin/Migrate` call site for the "mutated before save()" class of bug from WP1-gate/WP3 — all 8 new mutation call sites in WP4 correctly mutate only inside a `save()` mutator, none directly touch `App.storage.state`). One clarity bug caught and fixed pre-commit: the reset flow's "you didn't type מחק correctly" error reused the *label* string as the error message (confusing — showed "type 'מחק'" both as the instruction and, unchanged, as the error), fixed with a dedicated `resetTypeMismatch` string.
  **Live-browser verification** (same session, extension still connected): PIN gate (wrong PIN → error shown, correct PIN → dashboard); settings/storage-status render with real values (`rev 34`, `persisted: לא`, correctly reflecting a `sound:false` left over from an earlier WP2-3 test — UI faithfully reflects real stored state, not a bug); **full WP4-4 done-when executed against the actual production functions in the live page** (not a description of what should happen): `exportJson()` → captured JSON → `backupThenReplace` reset (mirroring the UI's own reset logic) → confirmed `sessions` wiped, `pinHash` survived → `importJson(exportedJson)` → confirmed `ledgerDeepEqualAfterImport: true`, `pinSurvivedImport: true`, and the restored session's `id`/`firstTryCorrect`/`coinsEarned`/`attempts.length` exactly matched the original (9, 19, 11) → `undoLastReplace()` → confirmed it restored to the pre-import (just-reset, 0-session) state, matching the plan's done-when precisely, including the "single-level undo" semantics (undo undoes the *last* replace, not further back — by design, matches DESIGN's "one-level" wording). Rewards editor: added a real reward via the UI form, requested it (simulating the child), approved it via the UI button, confirmed the pending list cleared and `lifetimeCoins` correctly did NOT decrease on redemption while `balance` did (matches `Economy.sums` semantics exactly — lifetime is `Σ earn` only). Reset-confirmation UI: typing the wrong confirmation word correctly blocks with the newly-added `resetTypeMismatch` message.
  Several `javascript_tool` calls reported a 45s CDP timeout during this session; in every case the action had actually completed (confirmed via a follow-up read) — treated as browser-tool-layer flakiness under sustained live-testing load, not an application bug (no case where the page was genuinely unresponsive or state was left inconsistent).
  `npm test` → 68/68 pass, exit 0 (core.js untouched by WP4 — pure UI work). `index.html` inline script re-verified with `node --check`.
deviations: change-PIN (from inside the already-unlocked dashboard) does not re-prompt for the OLD PIN before accepting a new one — the parent is already authenticated to be in the dashboard, so this reuses the same `renderParentNewPin` screen as the forgot-PIN recovery path without an extra gate. Decide-alone (§5 UI flow detail), not a security regression since dashboard access itself is already PIN-gated.
assumptions discovered: none

## 2026-08-25T18:58:26Z · plan v1 · WP5-1..WP5-3 · DONE
commit: 66d676d71a682ee29e5ecf653e9416d18bd9c8da
evidence: **WP5-1** `manifest.webmanifest` (name/short_name "לוח הכפל", `start_url:"./"`, `scope:"./"`, `display:standalone`, `dir:rtl`, `lang:he`, `background_color`/`theme_color` from DESIGN §10, both icon sizes) — validated with `python3 -c "import json; json.load(...)"`. `index.html` head additions: manifest link, `apple-touch-icon`, `apple-mobile-web-app-capable`/`-status-bar-style`/`-title`, `theme-color`, and (DESIGN §3's Google Fonts requirement, not yet added in any earlier package) a `Varela Round` Google Fonts `<link>` with `preconnect` hints — the browser fetches it at runtime per the WP3 grant note ("not by you"); not cached by the SW so offline correctly falls back to the existing system-font stack. **WP5-2** `sw.js`: `VERSION` constant, install pre-caches `./`,`index.html`,`core.js`,`manifest.webmanifest`, both icons, then `skipWaiting()`; activate deletes any other-versioned cache then `clients.claim()` then posts `{type:"updated",version}` to every client; fetch handler is cache-first for same-origin only, explicit passthrough (no `respondWith`, so the browser handles it normally) for cross-origin requests (fonts). `index.html`: `registerServiceWorker()` called from `boot()`; the reload button is bound once at boot (the toast element lives outside `#app`, persists across screen re-renders, so binding it inside a re-run render function would have stacked duplicate listeners); `refreshUpdateToast()` re-evaluated on every `route()` call (not just when the SW message arrives) so navigating to/from Home while an update is pending shows/hides it correctly — matches "never mid-session" (DESIGN §11) by construction rather than by a one-time check. **WP5-3** `tools/make-icons.py` (stdlib-only `struct`+`zlib` raw PNG writer, RGB no external deps per PA-3): solid primary-color (`#4F7CFF`) background with a geometric white "×" (two diagonal bars clipped to a centered, margined square) — simple placeholder art, real artwork deferred to NEXT-ACTIONS per PA-3. `python3 tools/make-icons.py` → `icon-180.png` (909 bytes) / `icon-512.png` (3204 bytes); verified PNG signature (`\x89PNG\r\n\x1a\n`) and IHDR dimensions (180×180, 512×512) via a Python script reading the raw bytes: both correct.
  **Live-browser verification of WP5-2's literal done-when and more**: served via `python3 -m http.server 8766`; confirmed `navigator.serviceWorker.getRegistration()` shows `active.state === "activated"`, `scriptURL` = `.../sw.js`; confirmed the cache (`mathtrainer-v1`) contains exactly the 6 precached URLs. **Offline test, done for real, not emulated**: killed the local HTTP server entirely (`lsof -ti:8766 | xargs kill`), confirmed via `curl` that the origin was genuinely unreachable, then reloaded the tab — the app loaded fully (screenshot: Home screen, correct coin balance from IndexedDB, all Hebrew text, buttons functional) served entirely from the SW cache with zero live server. Restarted the server afterward. **Update-toast test**: bumped `sw.js`'s `VERSION` to `v2`, called `registration.update()`, confirmed the `{type:"updated",version:"v2"}` message arrived and `App.updateAvailable` became `true`; screenshot confirmed the toast rendered correctly on Home ("יש גרסה חדשה — רענני" with a working button); started a session (`#screen=question`) and confirmed the toast becomes invisible (`display:none`) while `App.updateAvailable` stays `true`; returned to `#screen=home` and confirmed the toast reappeared — the "never mid-session" requirement verified as an actual state transition, not just read from the code. Reverted `VERSION` back to `v1` after the test (confirmed via `npm test` unaffected and a final `grep`). Verified head tags (`manifest` href, `apple-touch-icon` href, `theme-color` content, Google Fonts link presence) via `document.querySelector` in the live page.
  `npm test` → 68/68 pass, exit 0 (core.js untouched — pure PWA/static-asset work). `node --check` on `sw.js` and the extracted inline script both clean.
deviations: none
assumptions discovered: A5 (Google Fonts reachable at runtime; fallback stack makes the app usable without it) is now concretely wired (the link was previously absent from any file) — still needs the WP8 checklist item "offline with Google Fonts blocked → fallback font" for full confirmation, since this session's offline test didn't specifically block just the font while keeping the app framework loaded; it blocked everything (a strictly harder test that the app also passed, but not the identical scenario WP8 asks for).

## 2026-08-25T19:00:42Z · plan v1 · WP6-1..WP6-3 · DONE
commit: 6c500caf1a42f0df133d30dc24d9651b27c13340
evidence: **WP6-1** `README.md` (Hebrew first, English translation below, per plan): what the app is; iOS install steps (Safari-only requirement called out explicitly); the uninstall-deletes-progress warning in bold; backup/restore/undo/reset instructions matching the actual parent-view UI strings; forgot-PIN flow; the `sw.js` `VERSION`-bump-on-deploy instruction; `npm test`/local-dev commands (`python3 -m http.server 8766`, matching WP5-2's actual serving instructions). **WP6-2** `CLAUDE.md`: file-by-file project map (`core.js` section order, `index.html` structure, `sw.js`, tests); 8 hard rules — no build/no runtime deps, `T`-only strings, `CONFIG`-only numbers, first-attempt-only KPIs (I1) with the enforcement mechanism named, one-write finalization + deterministic ledger ids (I2), and rule 6 specifically calls out the "mutate `App.storage.state` directly instead of inside `save()`'s clone" bug class by name with a grep recipe — since this exact bug was caught 3 times live during WP3/WP4 self-review, it's the single highest-value thing for a future session (or future me) to be warned about explicitly rather than rediscover; `sw.js` VERSION-bump rule; no-unescaped-innerHTML rule. **WP6-3** `NEXT-ACTIONS.md`: the WP3-5 deliberately-trimmed polish items (confetti, coin-fly animation, count-up, streak badge, WebAudio); PA-3 icon-artwork follow-up; CONFIG tuning guidance post-play-test (with current simulated numbers restated for context); the multi-profile migration path (unplanned, flagged as needing real design work if ever needed); a WP8 checklist status breakdown (items 1-8/10-11 substantially covered by this cycle's live-browser testing but not run as one dedicated systematic pass; item 9 explicitly needs Marat on real iOS hardware, including the 8-day-wait A1 check that cannot close early by its nature); the two known modest deviations restated; a skill-scouting proposal (rule 5 — proposal only, "PWA-for-kids on GitHub Pages" and "Node-testable single-page pattern", not created, per "never create a skill yourself"); second-brain distillation explicitly left to the designer at close-out per rule 6, not written here.
  `npm test` → 68/68 pass, exit 0 (docs-only change).
deviations: none
assumptions discovered: none

## 2026-08-25T19:06:09Z · plan v1 · WP7-1 + WP7-2 · DONE
commit: (README URL addition, see next commit)
evidence: Marat confirmed outward-facing publication explicitly via AskUserQuestion ("Yes, proceed") before any of this ran, per §9 rule 1 / plan header (L1 = confirm first). Pre-check: `git grep -i "pinHash\|childName" -- ':!core.js' ':!index.html' ':!docs'` → every match is either plan/status-log prose referencing the field *names* or synthetic test-fixture data in `tests/*.test.js` (placeholder name "נועה", fake hash strings like "abc"/"salt:digest"/"someone-elses-hash") — no real personal data anywhere in the repo. `gh repo create Marvol26/math-tables --public --source . --push` → created and pushed (`https://github.com/Marvol26/math-tables`, branch `main`). `gh api -X POST repos/Marvol26/math-tables/pages -f build_type=legacy -f 'source[branch]=main' -f 'source[path]=/'` → enabled, `html_url: https://marvol26.github.io/math-tables/`. Polled via a background Monitor until live: `curl -s -o /dev/null -w "%{http_code}" https://marvol26.github.io/math-tables/` → 200, page `<title>` = "לוח הכפל" (Hebrew title visible, matching WP7-1's literal done-when). URL recorded in `README.md` (both Hebrew and English sections).
deviations: none
assumptions discovered: none

## 2026-08-26T07:49:35Z · plan v1 · WP8 (desktop items) · PARTIAL
commit: 764507ad84a50558924b4576441c667c85da37a8 (nextFrame fix) + d9d77b60d8d8337e8db23be813501bfa318b1074 (bdi fix) — both landed *during* this WP8 pass, from bugs this pass found; see below.
evidence: systematic live-browser pass (fresh `localStorage`/IndexedDB each time, `python3 -m http.server 8766`) through the desktop-doable items of the WP8 checklist:
  1. **PASS.** Full keyboard session driven via real dispatched `KeyboardEvent`s. A letter keydown does not call `preventDefault()`... wait — confirmed the reverse: digits/Backspace pass through (no `preventDefault`) and any other key (tested `"a"`) gets a `notPrevented:false`? — precisely: `dispatchEvent` on a letter key returned `false` (i.e. **was** prevented), confirming letters are rejected at the handler level. Hammering Enter (1 genuine keydown + 4 synthetic `repeat:true` keydowns fired back-to-back) produced exactly **1** attempt in `state.active.attempts` — `event.repeat` correctly blocks re-submission, and the 1.8s feedback lock blocks the rest regardless.
  2. **PASS.** A wrong first attempt is queued to `retryQueue` and the session advances to the next planned question (live-observed: `retryQueue:["2x1"]`, `current` moved to the next fact) — matches "reappears after the 10th" since retryQueue is only drained once `queue` empties. The finished session's summary correctly listed the recovered fact ("למדנו היום: 1x2") and `firstTryCorrect` correctly excluded the retry. **>10-overflow via import**: imported a state with a 13-item `carryover`; the immediately-started session's `planned` (verified by converting each directional "asked" string back to its canonical key) contained *exactly* the first 10 carryover keys, none of the overflow 3 — confirmed via the actual `Storage.importJson` → `SessionCore.start` production path, not a simulation.
  3. **PASS.** Started a question, noted `shownAt`, did a full page reload (equivalent to close+reopen — state lives in IndexedDB, not the tab), confirmed after reload: same `asked`/`key`, `current.interrupted` flipped to `true`, `shownAt` byte-identical (not restarted), no challenge bar (`challengeOn && !interrupted` correctly false), "ממשיכות בלי שעון" label rendered. Answered correctly afterward: `coins` = the plain tier value (2, matching `Facts.value` pre-attempt), `withinLimit:false` — base coins only, exactly as required.
  4/5. Already fully verified live in the WP2-3 and WP4-4 entries above (two-window staleness; export→reset→import→undo deep-equal cycle) — not re-run here to avoid duplicating recorded evidence.
  6. **Data-layer PASS, not re-verified as a fresh live UI screenshot this pass** (session-management confusion while forcing a specific fact through carryover ate the time budget for a clean repeat): `Facts.mastery`/demotion/interrupted-blocks-mastery are exhaustively unit-tested (`tests/facts.test.js`), and this pass directly re-confirmed `Facts.mastery()` flipping to `"mastered"` after 3 forced fast-correct attempts via the live page's own loaded `MathCore`; the coin badge's live binding to `Facts.value()` (which depends on mastery) has been visually confirmed correct across many other screenshots this session (different tiers rendering "שווה 1/2/3" correctly as facts' states changed). Recommend a dedicated fresh repeat of this specific item in a future pass if a live "badge visibly flips from שווה 3 to שווה 1 on screen" screenshot is wanted — not currently blocking, since both halves (the state machine, and the UI's binding to that state) are independently confirmed correct.
  7. **PASS**, and productively confirmed by an unplanned real timeout (background-tab timer throttling meant >5s genuinely elapsed before the first check): screenshot showed "אפשר לאט 🙂" with the question still open, no bar, `attempts.length:0` (nothing auto-submitted); answering afterward gave `withinLimit:false`, base tier coins (not doubled) — timeout truly never punishes. Separately drove a fresh question with `submit(..., shownAt+1500, ...)` (1.5s, inside the 5s limit) through the real `SessionCore.submit` → `withinLimit:true`, coins doubled (4 = tier-2 value 2 × 2). Both branches of Challenge Mode confirmed via the actual production code path.
  8. **PASS.** A real 10/10 session (driven via direct `SessionCore` calls through `Storage.save`, then rendered via the real `Screens.summary`) showed 3 stars, "💥 מושלם!!!", "כל התשובות נכונות!", "פתחת מדבקה חדשה! 🎁" (a sticker unlock fired), and the coin badge — this is what surfaced the bidi bug (see below). 9/10 → "🌟 כמעט מושלם!" was already observed earlier in this session's item-2 test.
  9. Not run — explicitly requires Marat on real iOS hardware (plan says so); see NEXT-ACTIONS.md.
  10. Whole-network offline load already verified live in the WP5 entry (server killed entirely, app loaded fully from the SW cache). The narrower "Google Fonts specifically blocked, server otherwise reachable" variant was not independently re-tested — noted as a gap in NEXT-ACTIONS.md already.
  11. `sw.js` VERSION-bump → update-toast-on-Home-only already verified live in the WP5 entry (bumped v1→v2, confirmed the toast state transition on Home vs. mid-session vs. back-to-home).
  **Two real bugs found and fixed during this pass (both committed immediately, both re-verified live after fixing):**
  - **`paintNextQuestion()` could hang forever on a hidden/backgrounded tab.** It gated the question paint (a correctness-critical operation — the only way the session advances) behind a bare `requestAnimationFrame`, which **never fires while `document.hidden`** (confirmed directly: `visibilityState:"hidden"`, `hasFocus():false` in the automation tab, and a standalone rAF call that never fired even after 1s). This left the app showing stale Home content under a `#screen=question` hash with `state.active.current` staying `null` indefinitely — no error, no timeout, no way forward short of the tab regaining visibility. Fixed with a `nextFrame()` helper that races `requestAnimationFrame` against a `setTimeout(fire, 50)` fallback, preserving the DESIGN §7 "capture shownAt after an actual paint" precision in the normal foreground case while guaranteeing eventual forward progress otherwise. This is a plausible real-world risk beyond just automation — any transient backgrounding (app-switcher gesture, OS interruption) at the exact moment a new question needs painting would have hit the same hang on a real device.
  - **`+21` rendering as `21+`** (and equivalent for every other interpolated "sign+number" or bare-number string next to Hebrew text) — a direct violation of invariant I10 ("numbers render LTR inside RTL text"), caught by actually looking at the perfect-session screenshot rather than only reading `textContent`. Root cause: none of the interpolated numbers were bidi-isolated from the surrounding RTL context. Fixed with a `bdi()` helper wrapping every interpolated number (coin pills across Home/Question/Rewards/Summary, streak label, unlock-progress remaining-coins, session score "X/Y", parent-view history rows) in `<bdi>`. Re-verified live: the fixed `<bdi>+32</bdi> 🪙` renders as "🪙 +32" correctly.
  **Tooling gotcha discovered and worked around repeatedly this session, worth recording for future test passes**: this project's own service worker (WP5) will happily serve a stale cached `index.html` to the very tab being used to test a just-edited `index.html`, silently hiding real fixes and producing confusing "the bug is still there" false negatives (this cost significant time twice in this pass alone, before being correctly diagnosed both times). **Before live-testing any `index.html`/`core.js`/`sw.js` edit against a tab that has ever loaded this app, unregister all service workers and delete all caches first** (`navigator.serviceWorker.getRegistrations()` → `unregister()` each; `caches.keys()` → `caches.delete()` each), confirm both are empty, *then* reload.
  `npm test` → 68/68 pass, exit 0 throughout (both fixes are UI-only; core.js untouched this pass).
deviations: none beyond the two fixes themselves, each individually justified above
assumptions discovered: A1 (8-day iOS ITP retention) still cannot close until 8+ days after a real iOS install (item 9); everything else this entry could test without Marat/iOS has now been exercised at least once against the real production code paths, not just described.

## 2026-08-26T08:22:13Z · plan v1 · WP9-prefix · DONE
commit: 8f231b53 (see `git log --oneline -1`; full hash 8f231b5... — pushed to origin/main)
evidence: Ahead of formally invoking WP9's adversarial-review skill, pre-emptively fixed a known gap already noted (but left unfixed) from the WP1-gate review: `Migrate.validateImport()` did not shape-check `raw.carryover` or `raw.active`, so a malformed imported `active` (e.g. missing `queue`/`retryQueue`/`attempts`) would pass validation and then crash `SessionCore.paint()`/`submit()` on the very next question render — a security/robustness hole in the import path. Added checks: `carryover` must be an array; `active` must be an object-or-null, and if an object, `planned`/`queue`/`retryQueue`/`attempts` must be arrays and `id` a non-empty string. Added 3 regression tests to `tests/migrate.test.js`. Proved via checkout-swap (`git stash push -- core.js` → re-run `node --test tests/migrate.test.js` against pre-fix core.js → 3 new tests fail, 8 pre-existing pass → `git stash pop`) that the new tests genuinely exercise the fix. `npm test` → 71/71 pass post-fix, exit 0. Also bumped `sw.js` `VERSION` "v1"→"v2" and `index.html` `APP_VERSION` "0.1.0"→"0.2.0": the two WP8 bug-fix commits (`764507a`, `d9d77b6`) had been pushed to the live GitHub Pages site after v1 was published without a VERSION bump, meaning any device that loaded the site in that window would have its service worker serving the pre-fix `index.html` indefinitely (the exact stale-SW trap documented as a tooling gotcha in the prior WP8 entry) — closes that gap. Marat authorized (AskUserQuestion, this session) pushing WP9 fixes + this VERSION bump without per-push confirmation for the remainder of this cycle, so this and all further WP9-driven commits will be pushed directly and reported here rather than re-confirmed each time.
deviations: none — this fix addresses a gap the WP1-gate review already surfaced as a MINOR and left unfixed at the time; fixing it now, before WP9, rather than waiting for WP9 to re-discover it.
assumptions discovered: none

## 2026-08-26T09:01:34Z · plan v1 · WP9 · DONE
commit: 9f69971 (round-1 fixes) then 1adfdb3 (finding-B completion) — full batch diff reviewed was d03ed03..9f69971 (24+1 commits, the entire build from empty scaffold), with a follow-up fix-verification pass over 9f69971..1adfdb3
evidence: Ran the `adversarial-review` skill per plan §WP9 — a fresh, zero-context reviewer (Agent tool, model:"fable") given the full batch diff, docs/DESIGN.md, and math-build-status.md, instructed to assume the author was wrong and hunt for silent corruption, KPI/coin manipulation, carryover loss, selector starvation, PIN/security holes, and tests passing for the wrong reason. **Round 1** returned SHIP-AFTER-FIXES with 4 real findings (full report preserved in this session's transcript, condensed here):
  - **Finding A (MEDIUM, security)**: the parent-dashboard PIN gate re-locked (`App.parentUnlocked = false`) only inside `navigate()`; any hashchange-driven exit from "parent" (browser/PWA back-gesture, hand-edited hash) bypassed the re-lock, leaving the dashboard — including reward self-approval — unlocked for whoever opened it next. Fixed by moving the re-lock into `route()` itself via a `lastRoutedScreen` tracker, so it fires on every way of leaving "parent."
  - **Finding B (MEDIUM-HIGH, correctness)**: same-window suspend/resume (exit mid-question → browse → resume from Home, all in one uptime, no reload/backgrounding) never marked the question `interrupted`, resurrecting the exact bug class the WP1-gate M2 fix targeted — the resumed attempt's real elapsed time (minutes) got scored as an ordinary slow attempt, denying an earned Challenge bonus and poisoning that fact's mastery/median stats. Round-1 fix only marked interrupted at the exit-confirmation button, which the same reviewer's fix-verification pass correctly rejected as incomplete (STILL-BROKEN) — a hashchange-driven exit (bypassing the button) still reproduced the bug, and was internally inconsistent with finding A's own premise that hash-driven exits are real. **Round-2 fix** moved the mark into `paintNextQuestion`'s short-circuit branch (the single choke point every return-to-question-with-a-pending-current visit passes through, regardless of exit mechanism), removing the now-redundant button-side mark. Live-verified (claude-in-chrome): (1) a raw hashchange exit followed by resume now correctly marks `interrupted:true` on the return render; (2) the WP3 double-route regression case — a fresh session's first question — still renders `interrupted:false`, confirming the short-circuit can't fire on a fresh start; (3) the original exit-button path still works. No Node-reachable regression test possible (UI-layer, same limitation as the earlier nextFrame/bdi fixes) — live-browser verification is the evidence here, not `npm test`.
  - **Finding C (MEDIUM, security)**: `exportJson` serialized `settings.pinHash`/`recoveryHash` into the backup file the parent is meant to move off-device (email, Drive), contradicting DESIGN §8's "device-local" treatment — a 4-digit PIN behind salted SHA-256 is offline-crackable in under a second from a leaked backup. Fixed: `exportJson` now strips both keys from the *serialized copy* via a `JSON.stringify` replacer, never touching `self.state` (the device's own PIN is untouched; round-trip behavior unchanged since import already nulls/reapplies these fields). Regression test added (`tests/storage.test.js`), proven via checkout-swap to fail pre-fix. The reviewer independently re-ran old-vs-new export side by side against pre-fix `core.js` in their own scratchpad and confirmed the same result.
  - **Finding D (MEDIUM, robustness/security)**: `Migrate.validateImport` shape-checked `ledger`/`carryover`/`active` (the last two from this session's own WP9-prefix hardening, see prior entry) but not `sessions[]` or `facts{}` entries — a malformed import (`sessions:[{}]` or `facts:{"2x7":"junk"}`) passed validation and then permanently bricked the parent dashboard (`Stats.trends` crash on every render) or made a question un-submittable (`Facts.updateFromAttempt` crash in JS strict mode). Fixed: extended `validateImport` with per-entry shape checks for both. 3 regression tests added to `tests/migrate.test.js`, proven via checkout-swap to fail pre-fix. Reviewer additionally probed the false-rejection direction — a real state driven through a full `start/paint/submit/finish` cycle with the attempts-retention trim applied (`delete session.attempts`) still validates clean, confirming the stricter check doesn't reject legitimate old backups.
  Two pre-existing test fixtures in `tests/storage.test.js` (bare `{id: "s_foreign"}` session shapes) had to be updated to well-formed session objects since Finding D's stricter validation correctly rejects what they used to pass — reviewer confirmed this is the correct consequence of the fix, not a weakened test.
  `sw.js` VERSION bumped v2→v3 (round 1, commit 9f69971) then v3→v4 (round 2, commit 1adfdb3) since v3 had already been pushed to the live GitHub Pages site before the finding-B completion. `npm test` → 75/75 green throughout both rounds (was 71/75 immediately after adding the new tests against unfixed core.js, confirming they bite; 4 new tests total across findings C+D). Fix-verification round (same reviewer, resumed session, given the diffs and live-test evidence) returned per-finding FIXED for A/C/D and STILL-BROKEN→FIXED for B across the two rounds, **overall batch verdict: SHIP**.
  7 LOW-severity findings from the review (DST streak edge case once/year in Israel's fall change; Home coin pill showing lifetime vs. balance — a design question for Marat, not clearly a bug; several hardcoded strings outside `T` violating hard rule 2 — heatmap tooltip English literals, import-preview Hebrew strings, update-toast text, boot loading text; no save-retry-once before the error banner per DESIGN §8; approve-reward double-click showing a misleading error message despite no actual double-charge; exiting during the ~1.8s post-answer feedback window can theoretically re-paint the question over Home, code-inspection only, not live-reproduced; stale-state banner bypassed on some recovery/undo paths) were recorded verbatim with reasoning in NEXT-ACTIONS.md rather than fixed, per the reviewer's own severity call and the plan's "fix HIGH/MEDIUM, record LOWs" instruction.
deviations: none from the WP9 procedure itself. One process note: round-1 fix for finding B was incomplete on first attempt (button-only, not resume-side) — caught by the same reviewer's own fix-verification pass exactly as the skill's two-round design intends, not a gap in the gate.
assumptions discovered: none new. Restates what NEXT-ACTIONS.md already tracks: WP8 item 9 (iOS hardware, incl. the 8-day A1 ITP retention check) remains open by nature — it requires Marat on real hardware and cannot be closed by any AI session; WP8 item 10's narrower Google-Fonts-only-blocked scenario remains an untested gap (a strictly harder "whole network down" case was tested and passed).

## Batch status: DONE, with one named remainder
Per the kickoff instructions, the batch is complete: HG-1..HG-3 through WP9 have all run, WP9's adversarial-review skill found and the fixes were verified SHIP by the same fresh reviewer, and that is recorded above. The build is live at https://marvol26.github.io/math-tables/ (commit 1adfdb3 on main). The one item this session cannot close by its nature is WP8 item 9 — real iOS hardware testing, including the 8-day ITP retention check — which needs Marat directly and cannot begin closing until 8+ days after a real device install. Everything else, including every WP9 finding, is closed and verified.

## 2026-08-26T17:10:00Z · punch-list (docs/PUNCHLIST-2026-08-26.md) · P1–P5, P7–P12, P14 · DONE (pending closing review)
commit: 201ab1b (core), 9a62f04 (UI), f5da8f1 (regression fix)
evidence: executor = Claude Fable 5 in the designer session (Marat: "run it", 2026-08-26). core.js: validateImport checks `active.current` shape and bounds ledger amounts (±CONFIG.LEDGER_MAX_ABS_AMOUNT); Economy.requestReward rejects a second pending request for the same reward; CONFIG.TIER_BY switch ("max" default = unchanged economy; "min" available — P6 left for Marat); SessionCore.submit result now exposes retry/withinLimit. 4 new tests, each shown to fail on pre-fix core.js via `git stash` (80/80 after). index.html: feminine strings (הציגי מקלדת…, ממשיכות), retry line rendered, distinct sticker emoji (shelf + reveal), beat-the-clock badge + gold flash, CSS confetti on correct, fireworks on perfect, WebAudio blips gated by settings.sound (context created on first gesture), learned list as "a × b = product", first-install toast suppressed (no controller at load), Enter on Home starts/resumes, feedback timer cancelled in route() and guarded on screen, reward button disabled on tap, reviewer-flagged literals moved into T, greeting with child's name, backup banner only after ≥1 session; sw.js v5, APP_VERSION 0.4.0. Live-verified on a local server with SW+HTTP cache busted (a stale-cache false negative cost one round — see WP8 tooling note): setup → Home (greeting, no toast, no banner) → perfect summary (fireworks, confetti, 🐱 🦊, "3 × 4 = 12") → Challenge ON fast answer → badge "מהר מאוד! ⚡ מטבעות כפולים!", 40 confetti pieces, withinLimit coins doubled.
deviations: one regression introduced and caught during live verification — `classList.add("feedback-correct feedback-fast")` threw InvalidCharacterError and left feedbackLock stuck after any fast correct answer; fixed in f5da8f1 and all decoration calls wrapped so effects can never block the session. Codex closing review unavailable (workspace out of credits) → fallback per review-gate rule: fresh zero-context Fable 5 reviewer over the full batch diff.
assumptions discovered: none new. Push to GitHub Pages NOT done — awaiting Marat's yes (outward-facing).

## 2026-08-26T17:40:00Z · punch-list closing review (round 1) · PARTIAL → fixes committed
commit: c9292e2
evidence: fresh zero-context Fable 5 reviewer over 23bf9ea..f5da8f1: 1 HIGH (the classList regression — already fixed in f5da8f1, confirmed complete), 2 MEDIUM (#2 Enter-on-Home double-fires with a focused button and calls S() pre-boot; #3 learned list can render reversed in RTL under bdi), 5 LOW (#4 showFeedback after exit mid-save clobbers Home; #5 reward cost unbounded vs LEDGER_MAX_ABS_AMOUNT; #6 hadController edge cases acceptable; #7 pre-existing: import didn't validate economy.requests → stored-XSS via string costSnapshot on the PIN-gated parent path; #8 test gaps). All of #2–#5, #7, #8 fixed in c9292e2 with tests (83/83). Fix-verification requested from the same reviewer.
deviations: none
assumptions discovered: none

## 2026-08-26T17:50:00Z · punch-list closing review (fix-verification) · DONE — SHIP
commit: c9292e2 (HEAD; 4 commits ahead of origin/main: 201ab1b, 9a62f04, f5da8f1, c9292e2)
evidence: same reviewer re-read 23bf9ea..HEAD and re-ran npm test (83/83): all findings FIXED; heatmap-tooltip digit order accepted as cosmetic (commutative). Verdict SHIP. Residual for Marat's device pass: Challenge-Mode fast answer (browser-only path), export in iOS standalone (P13).
deviations: none
assumptions discovered: none. Push to origin/main (→ GitHub Pages, sw v5) awaiting Marat's yes.

## 2026-08-26T18:05:00Z · P6 tier rule + deploy · DONE
commit: see git log (tier switch + this entry)
evidence: Marat chose "smaller operand sets the tier" → CONFIG.TIER_BY = "min" (1×6 = 1 🪙, 3×9 = 2, 6×7 = 3); DESIGN §5 amended with date; test updated; npm test 83/83; tools/simulate-economy.js → weak 183 / average 75 / strong 62 sessions to complete the collection (average within the 60–110 done-when). Marat authorized the push ("Yes, push now"). Pushed to origin/main → GitHub Pages; sw VERSION v5 / APP_VERSION 0.4.0.
deviations: none
assumptions discovered: none

## 2026-08-27T00:30:00Z · turtle walk + wrong-answer helper + chart numbers · DONE — SHIP (push pending Marat)
commit: 61373b1 (feature), 45a0c0f (helper fallback + SW precache cache:reload), 50fec6f, 286b433 (review fixes), 2b0c2ce (CLAUDE.md rule)
evidence: Marat chose option A (turtle walk) for a less tense Challenge-Mode countdown and asked for a graphical wrong-answer helper and readable parent charts. Built: 🐢 walks a track to 🏁 over the limit, rests with 💤 at timeout ("הצב הגיע ונח 😴 — אפשר לאט 🙂"), becomes a hopping 🐇 on a fast correct answer; wrong answer shows the fact as a rows-of-dots picture captioned "a × b = product" for CONFIG.WRONG_ANSWER_HELPER_MS (3.2 s); charts gained axis min/max, thinned per-point value labels and a last-value badge. submit() now returns `asked`. sw v6 / app 0.5.0; core.js loaded as core.js?v=0.5.0 and precached with cache:"reload" after a live stale-core.js skew froze feedback during testing. Live-verified on a local server (SW+HTTP cache busted): turtle walking → resting → dot helper "4 × 1 = 4" → 14-session fixture import → parent charts with numbers. Closing review (fresh Fable 5; Codex out of credits): 1 HIGH (caption below the fold in phone landscape for 10×10) + 1 MEDIUM (label crowding at 30 points) + 5 LOW, all fixed in 286b433; fix-verification re-measured layouts at 568×320/667×375/320×454 → SHIP.
deviations: none. DESIGN §6/§7/§9 amended (dated).
assumptions discovered: reviewer's layout numbers come from a static replica with fallback fonts; Marat's device pass should include one wrong answer on a big fact (e.g. 9×9) in landscape.

## 2026-08-27T02:10:00Z · journey map (docs/MAP-DESIGN.md) · DONE — SHIP (push pending Marat)
commit: 6d747b6 (feature) → 8ac7ee7 (×N bidi) → 48e2379 → 601283f (docs) → aad9208, 80d1280 (review fixes)
evidence: Marat approved the map design (learning order ×1→×2→×10→×5→×3→×4→×6→×7→×8→×9, reached at 10/10 mastered, practice priority for the current station). core.js Map module + selector focus (unseen focus facts first in 2a; focus learning facts first in 2b) + finish() marks stations permanently; migrate/validateImport additive (schema 1). index.html map screen (SVG zig-zag, station badges, turtle, tap → fact chips), Home status line + button, summary banner (single or multi-station), parent KPI. sw v7 / app 0.6.0 / core.js?v=0.6.0. Tests 97/97 (14 new). Live-verified on a local build with a fixture (2 stations reached, ×10 at 6/10): Home line, map, tap detail, summary banner. Closing review (fresh Fable 5): 1 HIGH — the sum-ascending unseen intro made stations light out of order and in bursts (caught by the reviewer's simulation; my tests had only covered the all-unseen regime) — fixed in two steps (2a partition, then 2b focus-first) with a plan-level test on a fully-seen state; 2 MEDIUM (detail card below the fold; tap targets <44 px) fixed; LOWs fixed or accepted. Reviewer re-simulated: perfect child strict path order; 85% child reaches each new current station within ~2 sessions, but a station can sit at 9/10 for many sessions (mastery = 3 in a row; one ask per session) while stations ahead light — documented in MAP-DESIGN §2 as "turtle = first gap, honest progress". Verdict SHIP.
deviations: MAP-DESIGN §2 amended twice during review (selector rule; honest-map semantics); "Home shows תחנה חדשה once" dropped.
assumptions discovered: if the 9/10 stall bothers the child, the lever is the mastery rule (e.g. 3 of the last 4 correct), not the selector — recorded in NEXT-ACTIONS.

## 2026-08-27T09:30:00Z · data-loss response batch (share-sheet export, last-good snapshot, cloud backup, pause-until-הבנתי, numpad order, exit, input width, teaching tweaks) · fixes committed, closing review round 2 pending
commit: f31398e, 2f74a3c, 7916b20, 2a6aa40, d2f2782, 3d98ea8, 0a1599c (sw v8 / app 0.7.0 / core.js?v=0.7.0 — not yet deployed)
evidence: Marat reported the child's history gone after the 0.6.0 update and that export on iPad only opened a preview (no file in Files). Built: export via navigator.share (Save to Files) with lastExportAt marked only on success; a "last-good" localStorage snapshot that never shrinks by session count + restore offers (setup screen / parent view) — verified live by wiping IDB + mirror: setup offered "14 סבבים — לשחזר?" and restored; automatic private-Gist backup (Cloud module, parent section, setup-screen restore, debounced after finish/approval, flush on hide) — verified live against a stubbed api.github.com; wrong-answer picture pauses until "הבנתי"/Enter; numpad LTR; answer box 2.9em; visible exit line; MAP_PATH ×9 before ×8/×7; strategy line + commutative twin (probe over 55 facts). Tests 110/110. Closing review round 1 (fresh Fable 5): 1 HIGH (connect-from-dashboard would PATCH the good gist with an empty state — the incident class), 2 MEDIUM (foreign token via import; one-tap cloud restore), LOWs — all fixed in 0a1599c; fix-verification requested.
deviations: reset now keeps the cloud token (device config) but starts a new gist; DESIGN §8 export rule extended to strip settings.cloud.
assumptions discovered: cause of the original loss still unknown (Safari-vs-icon storage or transient IDB failure + re-setup); both are now recoverable (snapshot/cloud). The reviewer notes a secret gist is link-readable and gist scope covers all gists in the account — documented in README.

## 2026-08-27T10:40:00Z · data-loss response batch · closing review SHIP
commit: 21cac6f + follow-up (restoreFromGistId)
evidence: fix-verification: all items FIXED; item 1 residual closed (connect adopts an existing gist only when local ≥ cloud; otherwise a new gist + restore offer; empty state never uploads). Reviewer's final LOW (a later small gist becoming "newest" hides the big one from in-app restore) fixed by remembering `settings.cloud.restoreFromGistId` at connect and preferring it in restore. Verdict SHIP. Tests 110/110. Push pending Marat.
deviations: none
assumptions discovered: none

## 2026-08-27T11:20:00Z · falling-numbers mode · design + executor brief written (designer), execution handed to a Sonnet session
commit: see git log (docs only)
evidence: Marat requested a second game mode (expression + ≥4 falling candidate answers, parent-set fall time, hard distractors, tap to answer) and asked for a plan to execute with Sonnet. Written: docs/FALLING-DESIGN.md (F1–F10) and falling-build-plan.md (WP-F1..WP-F8 with kickoff prompt). Designer decisions to note: falling mode earns coins/stickers but never touches mastery/map/carryover (recognition ≠ recall); landing is never a miss; ×2 comes from SessionCore via the Challenge snapshot.
deviations: none
assumptions discovered: none

## 2026-08-27T12:00:00Z · falling-numbers plan v1 · HG-1..HG-3+preflight · DONE
commit: 49ea5db
evidence: HG-1: `git status --porcelain` → only `?? .claude/` (pre-existing, untracked, unrelated to this repo's tracked tree); `git rev-parse --short HEAD` → `49ea5db`, one commit ahead of the plan's baseline `067aed3` — `git log --oneline 067aed3..HEAD` shows exactly one commit, `49ea5db docs: falling-numbers mode — design F1–F10 and executor build plan with kickoff prompt` (the docs commit that produced FALLING-DESIGN.md and falling-build-plan.md themselves; no code changed). HG-2: `npm test` → 110/110 passing. HG-3: `node --version` → v22.23.1 (≥20 ✓). Preflight §6: same as above, clean tree, 110/110, node ok — recorded as first entry for this batch.
deviations: HEAD is one commit later than the literal baseline in the plan header, but that commit is the plan/design docs commit itself (docs-only) — no drift in code. Proceeding per plan's own allowance ("or a later commit whose status-log entry you can read").
assumptions discovered: none

## 2026-08-27T12:10:00Z · falling-numbers WP-F1 F1-1 · DONE
commit: (pending — will commit at gate)
evidence: `CONFIG.FALLING` added (DEFAULT_DURATION_SEC 8, MIN 3, MAX 20, DEFAULT_OPTIONS 4, MIN_OPTIONS 4, MAX_OPTIONS 6, FILL_WINDOW 20). New `Falling` module in core.js (between Selector and SessionCore, reuses the existing private `fisherYatesShuffle`): `distractors(a,b,count,rng)` — priority order (a±1)×b, a×(b±1), p±a, p±b, digit-swap(p), a+b, p±10, deduped/≠p/1..100, then a fallback fill from other facts' products within ±FILL_WINDOW of p, then a full 1..100 shuffle fallback (never fails to reach `count` since 1..100 minus p has 99 candidates). `candidates(a,b,options,rng)` = shuffled distractors(count=options-1) + p. Exported as `MathCore.Falling`. tests/falling.test.js (5 new tests): count/uniqueness/range/≠p over all 55 facts × options 4/5/6; same-table-neighbour presence for 7×8, 6×9, 3×4; determinism under a fixed seeded rng; candidates contain p exactly once over all 55×3 combinations; CONFIG.FALLING values match FALLING-DESIGN F2. `npm test` → 115/115 (110 prior + 5 new).
deviations: none
assumptions discovered: none

## 2026-08-27T12:25:00Z · falling-numbers WP-F1 F1-2 · DONE
commit: (pending — will commit at gate)
evidence: `SessionCore.start(state, rng, now, opts)` accepts `opts.mode` ("typed" default | "falling"); stores `active.mode`. For falling, `settingsSnapshot.challengeOn` is forced `true` and `timeLimitSec = settings.falling.durationSec` (falls back to CONFIG.FALLING.DEFAULT_DURATION_SEC) so `submit()`'s existing withinLimit/×2 path (Challenge Mode's own mechanism) is reused verbatim for the ×2 rule — no separate timing code (I-F4), plus `settingsSnapshot.falling = {durationSec, options}` snapshotted for the UI (WP-F3). `submit()`: `attemptRecord.mode = active.mode`; `Facts.updateFromAttempt` is now skipped entirely when `active.mode === "falling"` (coins are still computed from the pre-attempt fact value, which is unaffected since Facts never advances). `finish()`: `isFalling` branch skips `Map.newlyReached`/`state.map.reached` mutation (stationsReached stays `[]`) and skips the carryover recomputation (`state.carryover` reassigned to itself, same reference — byte-identical); session record gets `mode: active.mode`. Coins/ledger/streak/perfect/near/unlocks bonuses are computed identically for both modes (unaffected by the branch). tests/falling-session.test.js (5 new): start() sets mode + snapshot; a perfect falling session leaves `facts`/`carryover`/`map` deep-equal to before while paying positive coins with a real ledger `_earn` entry and `session.mode === "falling"`; retries inside a falling session still earn 0 coins and never populate carryover; every attempt is tagged with the right mode (typed sessions still update `facts`); a fact-state engineered to be exactly one mastered fact short of reaching a map station is confirmed NOT reached after a falling session that would mastery-complete it in typed mode. `npm test` → 120/120 (115 prior + 5 new); the full pre-existing suite stayed untouched and green, confirming typed-mode behavior is byte-identical (I-F3).
deviations: none
assumptions discovered: none

## 2026-08-27T12:40:00Z · falling-numbers WP-F1 F1-3 · DONE
commit: (pending — will commit at gate)
evidence: `Migrate.emptyState()`/`migrate()`: `settings.falling` default `{enabled:false, durationSec:8, options:4}` (additive, schema unchanged, mirrors the existing `cloud` shape); a raw `settings.falling` with valid types is carried through untouched. `active.mode` and every `sessions[].mode` default to `"typed"` when absent, preserved when explicitly `"falling"`. `validateImport`: new `settings.falling` shape check (enabled boolean; durationSec/options within CONFIG.FALLING's MIN/MAX) that only fires when the field is present — old sessions/settings without `mode`/`falling` still validate clean. `Stats.trends()`: `accuracy`/`avgMs`/`masteredCount` are now computed only over sessions whose `mode !== "falling"`; `coins` keeps every session regardless of mode (F9's "excluded from speed/accuracy/mastery... only in session history and coin trend"). `perFactTable`/`weakest`/`totals().masteredCount` needed no code change — they read from `state.facts`, which falling attempts never touch (verified by the F1-2 fixture already in the suite). tests/falling-migrate-stats.test.js (9 new): emptyState/migrate defaults and pass-through for `settings.falling`; active/session mode defaulting and preservation; validateImport accept (old sessions, well-formed falling) and reject (out-of-range durationSec/options, wrong types); a two-session fixture (one typed, one falling, same shape otherwise) proving `trends.accuracy`/`masteredCount` include only the typed session, `avgMs` length matches, and `trends.coins` includes both. One pre-existing test (`tests/storage.test.js` "importJson keeps the device's own pinHash…") asserted an exact session shape via `deepEqual`; updated its expected value to include the new additive `mode: "typed"` field — this is the intended additive-migration behavior (a truly old import without `mode` still passes `validateImport` and gets defaulted on `migrate`), not a regression. `npm test` → 129/129 (120 prior + 9 new).
deviations: none
assumptions discovered: none

## 2026-08-27T13:10:00Z · falling-numbers WP-F1-gate · FIX FIRST → fixed → DONE
commit: 6d77f6b (WP-F1 core), f144cf2 (gate fix)
evidence: Fresh zero-context Fable 5 reviewer over `git diff 067aed3..HEAD -- core.js` plus the full current core.js and docs/FALLING-DESIGN.md. Empirically verified (not just read): byte-identity of facts/carryover/map after a falling session with forced misses+retries; 8,250-run distractor fuzz (55 facts × options 4/5/6 × 50 seeds) — zero failures on count/uniqueness/range/≠p; full existing suite green (typed-mode regression check); Migrate idempotency; validateImport accept/reject; aliasing safety of `state.carryover` reassignment. One MEDIUM: `Stats.trends()` filtered falling sessions out AFTER windowing to the last n, so a run of falling sessions could push real typed history out of the accuracy/avgMs/masteredCount window entirely even though it exists just outside — exactly the shape a child who prefers the balloon game produces. Fixed: filter before windowing. Regression test added (`Stats.trends: a long run of falling sessions does not starve the learning-trend window`) and proven to fail on the pre-fix core.js via `git stash push -- core.js` (0 !== 1, confirmed failing), then restored and passing. One design question surfaced (not a bug, not fixed): the once-per-day perfect bonus and daily streak counter don't distinguish typed vs falling sessions — recorded in NEXT-ACTIONS.md for Marat, per the reviewer's own call that this is a design decision, not a defect. `npm test` → 130/130.
deviations: none
assumptions discovered: the shared perfect-bonus/streak interaction (NEXT-ACTIONS.md) needs Marat's decision before/after ship; not blocking WP-F1 gate closure since F1-F10 doesn't specify it and the reviewer confirmed it's not a code defect.

## 2026-08-27T13:30:00Z · falling-numbers WP-F2 (F2-1, F2-2) · DONE (pending live verification in WP-F5)
commit: (pending — will commit at gate)
evidence: F2-1: `T.home.fallingBtn`/`resumeFallingCta` and `T.parent.fallingEnableLabel`/`fallingDurationLabel`/`fallingOptionsLabel` added. `renderSettingsSection` gained a checkbox (`set-falling-enable`), a range input (`set-falling-duration`, `min`/`max` = `CONFIG.FALLING.MIN/MAX_DURATION_SEC` — native range clamping, same pattern as the existing `set-time-limit` slider) and a `<select>` (`set-falling-options`, options 4/5/6, matching `CONFIG.FALLING.MIN/MAX_OPTIONS`). `wireSettingsSection`'s existing `save-settings` handler now also reads these three fields and writes `s.settings.falling = {enabled, durationSec, options}` in the same save (one write, same button, same pattern as every other setting — no new save path). F2-2: `Screens.home` computes `isFallingActive` (`state.active && state.active.mode === "falling"`) and `showFallingBtn` (`!hasActive && settings.falling.enabled`); the resume button's label switches to `resumeFallingCta` (adds 🎈) when resuming a falling session; the "מספרים נופלים 🎈" button (`data-action="play-falling"` → new `startFallingSession()`, which calls `SessionCore.start(s, Math.random, Date.now(), {mode:"falling"})`) is rendered only when no session is active and the parent has enabled the mode — hidden entirely while a session (of either mode) is active, satisfying "one session at a time" / "the other mode's button is hidden". `node --check` on the extracted inline `<script>` (lines 210-2277) → syntax OK. `npm test` → 130/130 (core.js untouched by this UI-only step). Full live verification (both Home states, settings persistence, slider clamping) deferred to WP-F5 per plan order.
deviations: none
assumptions discovered: none

## 2026-08-27T14:30:00Z · falling-numbers WP-F3 (F3-1, F3-2, F3-3) · DONE
commit: (pending — will commit at gate)
evidence: `Screens.question`/`paintNextQuestion` now dispatch through a new `renderCurrentQuestion()` (typed → existing `renderQuestion`; falling → new `renderFallingQuestion`) — everything upstream (SessionCore.paint/submit/finish, `showFeedback`, `finishSession`, exit/resume) is unchanged and shared. `submitAnswer(value)` gained an optional `value` param: falling bubbles call `submitAnswer(Number(bubble.dataset.value))` directly; the typed path (`submitAnswer()` with no args) is byte-identical to before. `renderFallingQuestion`: expression card (no input), a `.lanes` flex row of `options` lanes, one `.bubble` button per candidate positioned via `top`/CSS `animation: bubble-fall var(--fall) linear forwards` (`--fall` set inline from `active.settingsSnapshot.falling.durationSec`, per F1-2's snapshot); candidates come from `MathCore.Falling.candidates(a, b, options, rng)` with a small mulberry32 RNG seeded from `active.id + asked + shownAt` so the same `current` always re-renders the same candidate set (no core.js changes needed to persist them). `animationend` marks a bubble `.landed` (stays tappable, shows "נחתו! אפשר לבחור בנחת 🙂" once all have landed); keys 1-N (Mac) pick the bubble in that lane by DOM order (`.lanes` forced `direction:ltr`), guarded against the exit-confirm overlay and `feedbackLock`. `current.interrupted` and `prefers-reduced-motion` both render bubbles pre-landed. `T.summary` titles get a 🎈 suffix for `session.mode === "falling"`; parent history rows get a 🎈 marker. Live-verified on a local server (SW+HTTP cache busted per the documented stale-cache trap — the first smoke test silently ran against the OLD cached `core.js?v=0.7.0` and had to be redone): full falling session (correct tap → confetti/coins/dot advances; wrong tap → dot-array picture + strategy line + "הבנתי", fact requeued to `retryQueue`, re-asked at end, 0 coins; landed bubble still tappable, base coins, no ×2) → session finish → summary shows "🎈" and "6×7=42" learned-today → parent history row shows "🎈" distinct from typed rows below it → exit mid-question + resume showed "ממשיכות בלי שעون", bubbles pre-landed, tap paid base coins only (no ×2), confirming F10.
deviations: **one live-caught bug, fixed before commit**: bubbles rendered with `.landed` added immediately (interrupted resume / reduced motion) still ran the full `bubble-fall` CSS animation from the top — `.landed` only changed box-shadow, never stopped the animation — so a "resumed, already landed" question visibly replayed the entire fall instead of appearing landed instantly, contradicting F10. Fixed: `.bubble.landed { animation: none; top: calc(100% - 64px); ... }`, which also correctly no-ops for the normal post-animationend landing case (already at that position via `fill-mode:forwards`). Re-verified live: the fix renders resumed bubbles at rest immediately. This is a CSS-only defect not expressible as a `node --test` case; verified via live re-render (screenshot + DOM state) per CLAUDE.md's testing note that UI logic needs live verification, not just `node --check`. The precise F3-2 multi-viewport screenshot matrix (320×568/375×667/568×320/820×1180) was only partially achievable — the `resize_window` browser tool did not reliably shrink the actual viewport below ~500×723 in this environment (window-chrome overhead) — so 6-lane fit was visually confirmed at ~500px width with comfortable margin, but the smallest (320px) and landscape (568×320) cases are not yet pixel-verified; flagged for WP-F5's dedicated live-verification pass, which has its own device-emulation checklist.
assumptions discovered: none new.
