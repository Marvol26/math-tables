{
  "name": "math-tables",
  "version": "0.1.0",
  "private": true,
  "type": "commonjs",
  "scripts": {
    "test": "node --check app.js && node --check strings.js && node --check sw.js && node --test 'tests/**/*.test.js'"
  },
  "devDependencies": {
    "fake-indexeddb": "6.0.0"
  }
}
