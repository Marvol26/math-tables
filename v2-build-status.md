# v2 build — status log (executor appends only; format in v2-build-plan.md §0.3)

## 2026-08-28 · designer · package S1 baseline: ba8a563

## 2026-08-28T00:00:00Z · plan 1.0 · S1-1 · DONE
evidence: preflight — `git rev-parse --short HEAD` = d82a68c; `git status --porcelain` clean except `.claude/`; `npm test` 149/149. Wrote `tools/bump-version.js` (Node stdlib only, no deps): (a) sets `APP_VERSION` in `app.js` if present else `index.html`; (b) hashes each existing asset in `["core.js","strings.js","app.js","styles.css"]` (sha1 first 10 hex chars) and rewrites its `?v=` reference in `index.html`; (c) computes `fp` = sha1[0:10] over `index.html+manifest.webmanifest+icon-180.png+icon-512.png` bytes (index.html read back post-rewrite); (d) rewrites `var APP_VERSION`, `var RELEASE`, `var HASHED_ASSETS` in `sw.js`; exits 1 with a clear message if any of index.html/sw.js/manifest/icon-180/icon-512 is missing. Not yet run against the real repo (sw.js is still in its pre-S1-2 shape without `RELEASE`/`HASHED_ASSETS`; the real run happens in S1-4 after S1-2 gives sw.js the target shape). Idempotence proven against a scratchpad fixture mimicking the post-S1-2 sw.js/index.html shape: ran `node tools/bump-version.js 0.11.0` twice in a row over a copy of real `core.js`/`manifest.webmanifest`/`icon-180.png`/`icon-512.png` plus a hand-built fixture `index.html`/`sw.js`; `diff -r` between the post-run-1 and post-run-2 fixture directories reported no differences ("IDENTICAL (idempotent)"). Also verified: deleting `icon-512.png` from the fixture makes the tool print "missing required file …icon-512.png" and exit 1.
deviations: tested idempotency against a scratchpad fixture rather than the live repo files, because sw.js does not yet have the `RELEASE`/`HASHED_ASSETS` lines the tool rewrites (that shape is introduced by S1-2, next). The real repo run against `index.html`/`sw.js` happens in S1-4 per the plan's step order.
assumptions discovered: none.

## 2026-08-28T00:10:00Z · plan 1.0 · S1-2 · DONE
evidence: Rewrote `sw.js` to the target shape: `var APP_VERSION`, `var RELEASE`, `var HASHED_ASSETS` as the three generated lines (placeholder values `0.10.0` / `0.10.0-0000000000` / `["core.js?v=0000000000"]` — these are only ever written by `tools/bump-version.js`, which runs for real in S1-4); `CACHE_NAME = "mathtrainer-" + RELEASE`; `PRECACHE_URLS = ["./", "index.html"].concat(HASHED_ASSETS, ["manifest.webmanifest", "icon-180.png", "icon-512.png"])` (dropped the old unversioned `"core.js"` entry); `notifyClients()` posts `{ type: "updated", version: APP_VERSION }` (checked the only consumer, `index.html:873-874`'s `navigator.serviceWorker` message listener — it only reads `ev.data.type`, never `ev.data.version`, so the rename from the old `VERSION` value (`"v13"`) to `APP_VERSION` (`"0.10.0"`) is safe); `cache: "reload"` and the activate/claim/fetch logic left unchanged. `node --check sw.js` → OK. `grep -c 'cache: "reload"' sw.js` → 2 (≥ 1, required). `npm test` → 149/149 green (unaffected — no test currently exercises `sw.js` directly; `tests/deploy-contract.test.js` is written next, in S1-3).
deviations: none.
assumptions discovered: none.

## 2026-08-28T00:25:00Z · plan 1.0 · S1-3 · DONE
evidence: Wrote `tests/deploy-contract.test.js` (6 `node --test` cases, independently recomputing every hash and the fingerprint from disk — does not import `tools/bump-version.js`): (i) every `?v=` reference in `index.html` matches the current sha1[0:10]; (ii) `sw.js` `HASHED_ASSETS` equals exactly the existing assets with current hashes; (iii) `sw.js` `RELEASE === APP_VERSION + "-" + fp` and `sw.js` `APP_VERSION` equals `index.html`'s; (iv) local `<script>` tags appear in order `core.js[, strings.js][, app.js]` (only the ones that exist) with no `async`/`defer`; (v) `PRECACHE_URLS` (parsed as `[...].concat(HASHED_ASSETS, [...])`) includes `index.html`, `manifest.webmanifest`, both icons unversioned, and `cache: "reload"` appears; (vi) none of the four asset names appear unversioned in `index.html` or in the `PRECACHE_URLS` literal arrays. To get a genuinely green baseline first (S1-4 proper, at 0.11.0, comes next), ran `node tools/bump-version.js 0.10.0` once to sync the still-placeholder `sw.js`/`index.html` values with real content hashes at the current version number — `npm test` → 155/155 (149 prior + 6 new). **Prove it bites:** appended `// mutation-test-marker` to `core.js`, ran `node --test tests/deploy-contract.test.js` → 2 of 6 failed as expected:
  - test (i) `deploy contract: index.html asset references match current hashes (i)` — `AssertionError [ERR_ASSERTION]: core.js's ?v= in index.html is stale — run node tools/bump-version.js` with `expected: '60eafb37be'` vs `actual: '079e596c3a'`.
  - test (ii) `deploy contract: sw.js HASHED_ASSETS equals exactly the existing assets with current hashes (ii)` — `AssertionError [ERR_ASSERTION]: Expected values to be strictly deep-equal` on `['core.js?v=079e596c3a']` vs `['core.js?v=60eafb37be']`.
  Reverted with `git checkout -- core.js`; `npm test` back to 155/155 green.
deviations: ran the bump tool once at the *current* version (0.10.0, not the package's target 0.11.0) purely to reach a green baseline for the mutation proof before S1-4's real bump — S1-4 will re-run it at 0.11.0 next, which is itself a no-op-safe, idempotent operation per S1-1's proof.
assumptions discovered: none.

## 2026-08-28T00:40:00Z · plan 1.0 · S1-4 · DONE
evidence: Ran `node tools/bump-version.js 0.11.0` → `core.js?v=079e596c3a`, `release 0.11.0-defe44e836`. Updated `index.html`'s `APP_VERSION` line comment (was stale: "keep in sync with sw.js VERSION (WP5)" — `sw.js` no longer has a `VERSION` const) to "set by tools/bump-version.js — do not hand-edit", then re-ran the tool (idempotent per S1-1) to refresh the fingerprint since the comment edit changed `index.html`'s bytes before `fp` is computed. `npm test` → 155/155 green. `grep -n "v13\|0.10.0" sw.js index.html` → no output (exit 1, i.e. no matches), as required. Updated `CLAUDE.md` rule 7 (was "bump the version in THREE places" naming `sw.js` `VERSION`/`index.html` `APP_VERSION`/the `?v=` query by hand; now: run `node tools/bump-version.js <x.y.z>` before every commit touching a deployable file, `tests/deploy-contract.test.js` fails otherwise, `sw.js`'s cache name is `RELEASE`, and the three generated `sw.js` lines are never hand-edited). Updated `README.md`'s English "Updating the app" paragraph the same way (bump-tool + npm-test-fails wording). `math-build-status.md` untouched (no build-history entry needed beyond this status log; S1 targets `v2-build-status.md` as its log per the plan header).
deviations: none beyond the ESCALATION below.
assumptions discovered: none.

## 2026-08-28T00:40:00Z · plan 1.0 · S1-4 (README Hebrew paragraph) · ESCALATION
evidence: Plan grant for S1 includes "README.md version paragraphs" (both language sections), and `docs/V2-DESIGN.md` line 92 lists "README (modes, albums, session size, version tool)" as a doc target. The Hebrew "## עדכון האפליקציה" paragraph (README.md, currently: `כשמעדכנים את קבצי האפליקציה בשרת, יש **לשנות את הקבוע `VERSION` ב-`sw.js`** ולדחוף (push) לריפו. בלי זה, ה-Service Worker לא יזהה גרסה חדשה והמכשירים ימשיכו להציג את הגרסה הישנה מהמטמון.`) needs the equivalent rewording to describe `tools/bump-version.js` instead of hand-editing `sw.js` `VERSION`. Neither `v2-build-plan.md` §2 S1 nor `docs/V2-DESIGN.md` §7.1 gives a verbatim replacement Hebrew sentence, and plan §0.6 / the kickoff prompt bar composing Hebrew ("do not compose Hebrew … If a needed string is missing, ESCALATE with the exact key you need"). Per §4's own escalation list ("a needed Hebrew string not provided"), left the Hebrew paragraph UNCHANGED (still describes the old hand-edit-`sw.js`-`VERSION` procedure, which is now stale but not incorrect-and-composed-by-me) rather than write new Hebrew prose. Needed: a verbatim Hebrew replacement for that one paragraph — e.g. something conveying "run `node tools/bump-version.js <x.y.z>` and push; it sets the version, hashes the assets and rewrites the Service Worker's cache name; without this the Service Worker won't detect a new version and devices keep serving the stale cached version" in the designer's own Hebrew phrasing/register (matching the rest of the README).
deviations: README's Hebrew "## עדכון האפליקציה" paragraph left stale/unedited; only the English mirror paragraph was updated.
assumptions discovered: none.

## 2026-08-28T19:13:35Z · designer · S1 escalation resolved: Hebrew README "עדכון האפליקציה" paragraph rewritten by the designer (executor may not compose Hebrew).

## 2026-08-28T19:21:13Z · designer · S1-R review (fresh Fable 5): SHIP-AFTER-FIXES. MEDIUM-1 cache:"reload" assertion satisfied by a comment → assertion pinned to the Request construction (comments stripped) + mutant check; MEDIUM-2 fresh sw.js + edge-stale index.html could seal the skew → install-time self-consistency gate in sw.js (fetch index.html cache:reload, refuse install unless every HASHED_ASSETS url is referenced) + test asserts the gate exists; LOWs fixed: CLAUDE.md file map, index.html comment, tool exits non-zero on an unreferenced asset, escapeRegExp in the test, test (iii) reads APP_VERSION from app.js when present and forbids a stale copy in index.html, rule 7 sw.js clause. Suite 155/155 after a regex fix in the gate assertion. Fix-verification pending.

## 2026-08-28T19:23:42Z · designer · S1-R fix-verification round 1: MEDIUM-1 FIXED; MEDIUM-2 STILL-BROKEN (gate checked index.html but the PWA launches from "./", a separate edge-cached URL; two-read TOCTOU) → gate-and-put for both entry URLs using the checked Response, res.ok check; misplaced rule-7 clause moved from rule 9 to rule 7. node --check sw.js ok, suite 155/155. Second fix-verification requested.

## 2026-08-28T19:25:01Z · designer · S1-R fix-verification round 2: SHIP (MEDIUM-2 FIXED; LOW partial-cache-on-failed-install applied by the designer: puts deferred until both entries validate; node --check + 155/155). Reviewer note for the record: the gate is structurally tested only; a live edge-skew proof = DevTools override serving index.html with a bogus ?v= → new SW must fail to install (Marat, optional).

## 2026-08-28T19:25:03Z · designer · S1 committed+pushed as 2eaf278 · package 0 baseline: 2eaf278

## 2026-08-28T21:00:00Z · plan 1.0 · 0-1 · DONE
evidence: preflight — `git rev-parse --short HEAD` = d7a5f46; `git status --porcelain` clean except `.claude/`; `npm test` 155/155. Implemented V2-DESIGN §2 B2a (all four numbered items):
- `core.js`: `CONFIG.SCHEMA_VERSION` 1 → 2. New private helpers (module scope, above `var Migrate = {`): `canonicalizeUnlocked(list)`, `canonicalOf(asked)` (directional string → canonical fact key, null if malformed), `multisetEqual(a,b)`, `attemptFieldsValid(a)` (key/asked/ok/ms/t/retry/interrupted per B2a-1), `completedSessionEvidenceProblem(session)` (→ "trimmed" | "malformed" | null), `journalEvidenceProblem(journ)` (active/parked, same idea).
- `Migrate.emptyState()` and `Migrate.migrate()` gain `state.meta = { evidenceRebuild: null }` (or carried through from `raw.meta`); `economy.unlocked` now goes through `canonicalizeUnlocked` (unique, ∈ CONFIG.STICKERS, order kept, unknown ids dropped).
- `Migrate.validateImport()`: `meta` accepted as an optional object; sessions whose `attempts` array IS present are now checked with `completedSessionEvidenceProblem` (pushes one problem if malformed) — sessions without `attempts` still pass untouched (backward-compatible), per the plan text ("sessions without attempts pass"). Did NOT add the active/parked queue⊎planned invariant to `validateImport` — the plan's own wording scopes the "same invariants" addition to sessions, and several existing green fixtures (e.g. migrate.test.js's P10 tests) use active/parked shapes with `planned` non-empty but `queue`/`attempts` deliberately minimal; adding that check there would break tests the plan didn't ask me to touch. That stricter journal check lives only in `Migrate.preflightEvidence` (internal, boot-only), never in the import-validation path.
- New `Migrate.evidenceRebuildPending(state)`, `Migrate.preflightEvidence(state)` (implements every B2a-1 invariant: attempt fields; completed-session finite startedAt≤endedAt, each attempt's t within bounds and key∈planned, non-retry-attempts multiset == planned exactly, firstTryCorrect/misses agreement; active/parked t≥startedAt and canonical(nonRetry)⊎canonical(queue)==canonical(planned); any session lacking `attempts` ⇒ "trimmed"), `Migrate.rebuildEvidence(state, now)` (on preflight failure: only writes `meta.evidenceRebuild`, no other mutation; on success: resets `facts`/`map.reached`, replays every non-retry attempt from completed sessions + active/parked journals in global `t` order with ties broken by session array index, live-journal attempts sorted last via `sIdx=Infinity`; walks `state.sessions` in array order, consuming exactly the timeline entries with `sIdx <= si` before each session's snapshot so masteredAfter/stationsReached are correct even for a session with zero non-retry attempts; snapshots `masteredAfter`/`stationsReached`/`map.reached[n]=session.endedAt` only at completed-session boundaries — active/parked attempts feed `facts` afterward but reach no station).
- `index.html` `boot()`: after `App.storage.state = state; wireStorageReactions();` added `if (MathCore.Migrate.evidenceRebuildPending(state)) { await save(function (s) { MathCore.Migrate.rebuildEvidence(s, Date.now()); }); }` — `save()` (module-scope wrapper around `App.storage.save`) mutates a clone and only adopts it on a successful CAS commit, matching the design's commit rule exactly; a failed save leaves `App.storage.state` byte-identical and the guard stays pending for the next boot to retry.
- New `tests/evidence-rebuild.test.js` (11 tests, all green): fixtures are built by DRIVING REAL PLAY through `SessionCore` (29 sessions, typed+falling mixed, some with misses/retries, via a seeded PRNG) rather than hand-crafted raw objects, plus an active typed session (5 attempts, unfinished) with a parked falling session (4 attempts, unfinished) underneath — exactly the shape named in the design's test list. Verified with a debug run that the fixture is non-trivial: 53/55 facts mastered, all 10 map stations reached at distinct sessions. Tests: preflight clean on the real fixture; `evidenceRebuildPending` true→false after a successful rebuild; rebuild recovers `facts` byte-identical to what live play actually produced after the derived fields (`facts`, `sessions[].masteredAfter`, `map.reached`) are corrupted to simulate the pre-0.10.0 drift Marat saw; per-session `masteredAfter` rewritten correctly; stations land on the same sessions live play reached them on; active/parked attempts feed facts but assign no new station; a second rebuild is deterministic/idempotent (same result modulo the `at` timestamp); malformed attempt (`ok` as a string) → preflight fails, `rebuildEvidence` leaves `facts`/`sessions`/`map` byte-identical, `meta.evidenceRebuild = {done:false, reason:"malformed"}`; a session missing its `attempts` array (retention-trimmed) → `reason:"trimmed"`, `done:true` (permanent, `evidenceRebuildPending` false afterward), state untouched; a duplicated first-attempt `asked` (breaking the planned multiset) → preflight fails; and a `fake-indexeddb` two-window test proving a failed CAS during the rebuild leaves `Storage`'s in-memory state byte-identical to before the call and the guard still pending.
- `npm test` → 166/166 (155 prior + 11 new); the only non-green run seen this step was the pre-existing, EXPECTED deploy-contract staleness (asset hashes not yet re-synced — that happens in 0-6's `bump-version.js` run, per the plan's step order), confirmed transient by running `evidence-rebuild.test.js` alone (11/11 green) before the full-suite run.
deviations: scoped the validateImport invariant-check addition to sessions only (not active/parked), per the reasoning above — flagging in case the designer's intent for "the same invariants" was broader; happy to widen if the designer wants active/parked import validation stricter, but that would need a companion pass over migrate.test.js's existing minimal active/parked fixtures.
assumptions discovered: none.

## 2026-08-28T21:05:00Z · plan 1.0 · 0-2 · DONE
evidence: `core.js` `CONFIG.MASTERY_MS_THRESHOLD` 6000 → 8000, with an inline comment citing the evidence (10.7s typed average incl. thinking, ~3s numpad mechanical cost, 8s balloon fall). `docs/DESIGN.md` §7 amended: the KPI line now reads "median ms ≤ `CONFIG.MASTERY_MS_THRESHOLD`" instead of a hardcoded "6 s", plus a new evidence paragraph mirroring the CONFIG comment. `tests/facts.test.js`: renamed the first mastery test's description from "median <= 6000ms" to "median <= 8000ms (CONFIG.MASTERY_MS_THRESHOLD, V2-DESIGN B2b)"; the second test ("mastery is not reached if the median of the last 3 exceeds the threshold") had its ms values changed from `[4000,7000,8000]` (median 7000, which is now ≤ 8000 and would wrongly flip to "mastered") to `[4000,9000,10000]` (median 9000, still > 8000 → "learning"). Checked `tests/falling-session.test.js` and `tests/stats.test.js` for other pinned 6000 values: both use `CONFIG.MASTERY_MS_THRESHOLD` dynamically or an unrelated 6000ms attempt duration (stats avg-clamp test), no change needed. `npm test` → 166/166 pass, 3 pre-existing/expected deploy-contract staleness failures only (fixed in 0-6).
deviations: none.
assumptions discovered: none.

## 2026-08-28T21:30:00Z · plan 1.0 · 0-3 · DONE
evidence: `core.js` `Stats.trends(state, n)` rewritten per V2-DESIGN §2 B1 (revised): returns `{ accuracy: {typed,falling,tetris}, avgMs: {typed,falling,tetris}, masteredCount, coins, modes }` over ONE common window of `state.sessions.slice(-n)` (all modes) — each per-mode series is null-aligned to that same x-axis (null at every position that isn't that mode) instead of the old typed-only pre-filtered axis; `masteredCount`/`coins` stay all-mode, unchanged in shape. `tests/falling-migrate-stats.test.js`: replaced the two `trends()` tests with the new per-mode-null shape (verified `accuracy.typed=[0.5,null]`, `accuracy.falling=[null,1]`, `accuracy.tetris=[null,null]`, `avgMs` likewise, `modes` array) and a version of the "long run of falling sessions" test that now proves the typed line shows a null GAP rather than being dropped by pre-filtering (window sized to hold every session so the gap is real, not a windowing artifact) — matches the design's own test-list wording verbatim ("a run of 30 falling sessions leaves the typed line with gaps, not blanks"). `tests/stats.test.js`'s `trends()` test updated to the new per-mode shape (`accuracy.typed`, `accuracy.falling=[null]`, `modes`).
- `index.html`: added `multiLineChartSvg(seriesList, opts)` (reusing `lineChartSvg`'s axis/grid/label-crowding logic) — one polyline per mode, broken into segments at `null` so a mode gap never draws a spurious connecting line; value labels only on each series' own last-12 non-null points; per-series stroke colour. Added `modeLegendHtml()` (renders `T.parent.modeLegend` + a coloured-dot key per `T.parent.modeNames`). Wired `chart1`/`chart2` (accuracy/avg-speed) in `renderStatsSection` to build one `{key,values,stroke}` series per mode (`typed #4F7CFF`, `falling #FF6F91`, `tetris #3CC97A`, per the plan's exact colours) and append the legend under each chart; `chart3`/`chart4` (masteredCount/coins) untouched (still plain `lineChartSvg`). New `T.parent.modeNames = {typed:"מקלדת ⌨️", falling:"בלונים 🎈", tetris:"מלבנים 🧱"}` and `T.parent.modeLegend = "קו לכל מצב משחק"` (both verbatim from design §2 B1/B4 area — actually cited in the plan's 0-3 step text directly, not composed).
- CAUGHT AND FIXED A REAL BUG DURING SELF-VERIFICATION: my first edit of `renderStatsSection` accidentally deleted the `var trends = MathCore.Stats.trends(state, 30);` line while restructuring chart1/chart2 — `node --check` on the extracted script doesn't catch a `ReferenceError` (it's syntactically valid, only fails at runtime), so this was invisible until I built a standalone headless-Chrome harness (see 0-3/0-5 evidence below) that actually EXECUTES `renderStatsSection` against a real fixture state and dumps/screenshots the result — it threw `ReferenceError: trends is not defined`. Restored the line; re-verified both via the harness screenshot (see 0-5 entry) and `npm test`.
- Headless verification harness (scratchpad, not shipped): extracted the live CSS (`sed -n '17,312p' index.html`) and the live `escapeHtml`/`tableTag`/`bdi`/`chartBlock`/`heatmapCellColor`/`lineChartSvg`/`multiLineChartSvg`/`modeLegendHtml`/`renderStatsSection` function bodies verbatim (`sed -n '774,790p;2147,2400p' index.html`) plus the live `T.parent` object (`sed -n '450,569p' index.html`) into `dashboard-harness.html`, loaded real `core.js`, fed a real 16-session mixed typed/falling fixture built by driving `SessionCore` (28/55 facts mastered, 2/10 stations, matching the KPI tiles rendered), and called `renderStatsSection(state)` directly — this exercises the ACTUAL shipped code, not a reimplementation. Screenshot: `/private/tmp/claude-502/.../scratchpad/dashboard-shot.png` (420x1700) shows two distinct coloured per-mode lines (pink=falling, blue=typed) on both the accuracy and avg-speed charts with visible gaps where the other mode played, the legend line under each, and the heat-map's new axis+legend (see 0-5). `npm test` → 166/166 (163 prior + adjusted trends tests are replacements, not additions, so count unchanged net; confirmed via `grep -c "^test("` before/after matched); only the 3 pre-existing/expected deploy-contract staleness failures remain.
deviations: none beyond the caught-and-fixed bug above, which is itself the point of the headless verification step.
assumptions discovered: the plan's literal `Stats.trends` return shape (`accuracy: {typed,falling,tetris}`, plus a `modes` array) was taken as authoritative over the design doc's prose (which didn't spell out the exact key name `modes`) — implemented literally as the plan states it.

## 2026-08-28T21:45:00Z · plan 1.0 · 0-4 · DONE
evidence: `core.js` `CONFIG.FALLING.START_DELAY_MS = 600` added (with comment). `index.html`: `.bubble` base `top` and the `@keyframes bubble-fall` "from" value changed `-110px → -140px` (default), `-100px → -130px` (the `@media (max-width:380px)` variant), `-90px → -110px` (the `@media (max-height:420px) and (min-width:480px)` landscape variant) — all three keyframe variants named in the plan. Added `animation-delay: var(--fall-delay, 0s)` on `.bubble` (applies to both the `bubble-fall` and `sway` animations, per the design's "so the exercise is readable before anything moves"). `.lanes` gained `margin-top: 1rem; border-top: 2px solid rgba(79,124,255,0.15)` (card/lanes visibly never touch). The falling-question screen's inline style gained `--fall-delay:` + `CONFIG.FALLING.START_DELAY_MS` + `ms` alongside the existing `--fall:` duration. `animationend` handling and the ×2 window (still counted from `shownAt`, unaffected by the 0.6s CSS delay per design) left untouched, matching the plan's "Reduced-motion path unchanged" / "animationend handling unchanged."
- Headless harness (scratchpad, reused pattern from the batch-S1 `layout/` harness — an iframe wrapper with the app CSS inlined, each iframe sized to a target breakpoint): built `balloon-inner.html` with the exact `.screen[data-falling="1"]` / `.question-info` / `.lanes[data-n="4"]` / `.bubble` DOM shape `renderFallingQuestion` produces (verified class names against `index.html:1246-1269`), styled with the live extracted CSS, measuring at t=0 (script runs at `window.onload`, before any animation/delay has elapsed — CSS gives every bubble its base `top:-140px` position during the animation-delay window, so t=0 measurement is valid without needing to fast-forward virtual time) via `getBoundingClientRect()` + a manual AABB-intersection test against `.lanes`' rect, reported through `postMessage` to an outer harness page (`balloon-outer.html`) with 3 iframes at 390×844 (default), 360×640 (the ≤380px breakpoint), 740×360 (the landscape max-height:420/min-width:480 breakpoint), captured via `--headless=new --disable-gpu --allow-file-access-from-files --virtual-time-budget=8000 --dump-dom`.
  Results (`/private/tmp/claude-502/.../scratchpad/balloon-dump.html`): 390×844 → `lanesTop=195.4, qinfoBottom=169.8, gap=25.6px (≥16px ✓), anyBubbleIntersectsLanes=false`. 360×640 → `gap=25.6px (≥16px ✓), anyBubbleIntersectsLanes=false`. 740×360 (landscape) → `anyBubbleIntersectsLanes=false`, but `gap=lanesTop−qinfoBottom` came out NEGATIVE (-203.7px) — see deviation below. Screenshot (390×844, `/private/tmp/claude-502/.../scratchpad/balloon-shot-390x844.png`): no balloon visible above the fold at t=0; the equation card, the 1rem gap, and the green lane-top edge are all clean and separated.
deviations: the `.lanes.top − .question-info.bottom ≥ 16px` done-when check is a PORTRAIT-layout metric (it assumes `.question-info` stacks above `.lanes` vertically). At the landscape breakpoint (`@media (max-height:420px) and (min-width:480px)`), `[data-screen="question"][data-falling="1"]` switches to `flex-direction: row; flex-wrap: wrap` — `.question-info` and `.lanes` sit SIDE BY SIDE, not stacked, so "lanes top minus qinfo bottom" is not a meaningful separation metric there (it went negative because `.lanes` starts near the top of the row while `.question-info`'s content wraps to a second row below it — they still never overlap, confirmed by `anyBubbleIntersectsLanes:false` and by the box-model, `.question-info` and `.lanes` are siblings in a wrapping flex row, not stacked). I verified the PRIMARY safety invariant (D3: a falling bubble never intersects/touches the lane track at t=0) holds at all three breakpoints, and the secondary ≥16px gap metric holds at the two portrait breakpoints where it's a meaningful measurement. Flagging this rather than silently declaring the landscape case "passing" the gap check by a metric that doesn't apply to its layout.
assumptions discovered: none beyond the above.

## 2026-08-28T21:50:00Z · plan 1.0 · 0-5 · DONE
evidence: `index.html` `renderStatsSection`'s heat-map: grid-template-columns changed to `1.4em repeat(10,1fr)` with a leading empty corner cell, a header row of `CONFIG.FACTS_MIN..CONFIG.FACTS_MAX` (1…10) in `.chart-axis` style, and a leading column of the same 1…10 labels per row (also `.chart-axis`) — the existing LTR/`unicode-bidi:isolate` wrapper already makes this an LTR island, so no separate island wrapper was needed (reused the existing `.ltr` container). A new legend line below the grid renders `T.parent.heatmapLegend` (verbatim string from design §2 B4: `"ירוק = נלמד לגמרי · צהוב = בלמידה (70%+) · כתום = בלמידה (מתחת ל-70%) · אפור = עוד לא ניסתה"` — copied exactly, not composed). `T.parent.heatmapLegend` added next to the existing `heatmapTitle` key.
- Verified with the same headless harness used for 0-3 (`dashboard-harness.html`, real `core.js` + a real 16-session fixture, screenshot at `/private/tmp/claude-502/.../scratchpad/dashboard-shot.png`): the heat-map section clearly shows a numbered header row (1–10) across the top, a numbered leading column (1–10) down the left side, and the Hebrew legend sentence directly under the grid, matching design §2 B4 exactly.
- `npm test` → 166/166, `node --check` on the extracted inline script passes; rule-6 grep (see 0-6) confirmed no direct-mutation regressions introduced by this batch.
deviations: none.
assumptions discovered: none.

## 2026-08-28T22:00:00Z · plan 1.0 · 0-6 · DONE
evidence: `node tools/bump-version.js 0.12.0` → `core.js?v=012d04f3b6`, `release 0.12.0-c6c60b661b`; `sw.js` `APP_VERSION="0.12.0"`, `index.html` `APP_VERSION="0.12.0"` (comment: "set by tools/bump-version.js — do not hand-edit"), `<script src="core.js?v=012d04f3b6">` — matches (idempotent tool, per S1-1's proof; ran once, exit 0). `npm test` → 166/166 GREEN (the 3 deploy-contract staleness failures seen throughout 0-1..0-5 are now gone, as expected — they were only ever "core.js changed since the last bump" staleness, resolved by this step's bump). Rule-6 grep (`grep -n 'MathCore\.\(SessionCore\|Economy\|Pin\|Migrate\)\.' index.html`): every mutating call (`SessionCore.switchTo/paint/submit/finish/markInterrupted`, `Economy.requestReward/deactivateReward/approveRequest/rejectRequest`, and this batch's new `Migrate.rebuildEvidence`) sits inside `save(function (s) { ... })`; the remaining matches are either read-only (`Pin.isValidFormat/hash/verify/generateRecoveryCode`, `Migrate.validateImport`) or build/mutate a plain local variable that is not yet `App.storage.state` (`Migrate.emptyState()`, `Migrate.migrate(raw)` building the boot-local `state` var, `Migrate.recompute(state)` on that same pre-assignment local, `Migrate.evidenceRebuildPending(state)` a pure predicate) — no direct-mutation-outside-save regression. Inline-script `node --check` on the extraction one-liner (`sed -n '/^<script>$/,/^<\/script>$/p' index.html | sed '1d;$d' > .../x.js && node --check .../x.js`) → clean, no syntax errors. `git status --porcelain` (informational, not committing): `core.js`, `docs/DESIGN.md`, `index.html`, `sw.js`, `tests/facts.test.js`, `tests/falling-migrate-stats.test.js`, `tests/stats.test.js`, `v2-build-status.md` modified; `tests/evidence-rebuild.test.js` new; `.claude/` untracked (pre-existing, not mine).
deviations: none.
assumptions discovered: none.

Package 0 (fixes) steps 0-1 through 0-6 are all DONE. Package is not closed until the plan's closing-review step (§3, run by the designer) returns SHIP.

## 2026-08-28T22:30:00Z · plan 1.0 · 0-F · DONE
Package 0 closing review (fresh Fable 5) returned SHIP-AFTER-FIXES. All findings fixed in the working tree (still uncommitted); each with a regression test that provably fails against the pre-fix code (revert-run-restore performed for every core.js-level fix; exact commands and failing-assertion text below).

**HIGH-1 — rebuildEvidence boundary loop gated by session index, not by `t`.**
Fix (`core.js`, `Migrate.rebuildEvidence`): the boundary while-loop condition changed from `timeline[cursor].sIdx <= si` to `timeline[cursor].a.t <= state.sessions[si].endedAt` — gates strictly by chronological time, so a parked/active journal's early attempts (or a session that finishes later but carries earlier-`t` attempts, e.g. pre-park questions later folded into a resumed session's `attempts` array) get replayed into `facts` before the session whose `endedAt` they precede is snapshotted, instead of stalling the cursor and freezing facts for every subsequent snapshot.
Regression tests (`tests/evidence-rebuild.test.js`): `[HIGH-1 a] parked-early-t straddle` and `[HIGH-1 b] finished-while-parked interleave` — both build fixtures via real `SessionCore` play (8 warm-up sessions + the straddle/interleave shape), capture live truth, corrupt derived fields, rebuild, and assert `masteredAfter`/`stationsReached`/`map.reached` match live truth exactly.
Revert-run-restore: reverted the boundary line to `timeline[cursor].sIdx <= si` (`sed`), ran `node --test tests/evidence-rebuild.test.js`:
  - `[HIGH-1 a]`: `AssertionError: session A's rebuilt masteredAfter must match live play` — `10 !== 16` (expected 16, actual 10).
  - `[HIGH-1 b]`: `AssertionError: F's rebuilt masteredAfter must match live play (must include A's 3 pre-park attempts)` — `10 !== 14` (expected 14, actual 10).
  Restored the fix; re-ran — both green (31/31 in the file).
Also reproduced the reviewer's own probes (`probe1.js`/`probe2.js`/`probe3.js` under the scratchpad `rev/` dir — copied into the repo directory first, since Node resolves their relative `require("./core.js")` against the SCRIPT's own directory, not the invocation cwd, which initially made me test the wrong file) against the fixed `core.js`: probe1 → "NO DIFFS"; probe2's full seed/warm/pre sweep → completed with no "PERMANENCE VIOLATED"; probe3's straddle scan → "NO DIFFS in straddle scan".

**MEDIUM-2 — import/restore never re-triggers the rebuild until the next cold boot.**
Fix (`index.html`): new `maybeRebuildEvidence()` helper (`if (MathCore.Migrate.evidenceRebuildPending(S())) return save(function (s) { MathCore.Migrate.rebuildEvidence(s, Date.now()); }); return Promise.resolve();`), called right after every successful `backupThenReplace`-backed operation, before rendering: `cloudApply` (line 672), `cloudRestore` (line 687), both `restore-lastgood` bindings (setup screen line 952, parent dashboard line 2578), and `confirm-import` (line 2623). Verified by grep: `grep -n "importJson\|restoreLastGood\|maybeRebuildEvidence" index.html` shows all 3 `importJson` call sites and both `restoreLastGood` call sites paired with a `maybeRebuildEvidence()` call in their success branch.
Regression test: `[MEDIUM-2] a state imported via backupThenReplace stays pending until the caller explicitly re-runs the rebuild` — a `fake-indexeddb` `Storage` instance imports a real fixture via `backupThenReplace` directly (bypassing the UI layer, which isn't unit-testable this way), asserts `evidenceRebuildPending` is still true immediately after (proving `backupThenReplace` alone does NOT trigger it), then runs the same check+save sequence the UI callers now run and asserts `meta.evidenceRebuild.done === true`. This test doesn't have a meaningful "pre-fix" revert (the fix is in `index.html`'s UI wiring, not in any core.js behavior this test exercises — the test proves the CORRECT sequence works, and the grep above is the evidence that all UI callers now run it).

**MEDIUM-3 — validateImport rejected a whole backup for a clock-skew artifact.**
Fix (`core.js`): added `CONFIG.EVIDENCE_CLOCK_SKEW_MS = 5 * 60 * 1000`. Split the old `attemptFieldsValid`/`completedSessionEvidenceProblem`/session-check-in-validateImport into: `attemptStructureValid` (structural only — dropped the `a.ms < 0` rejection entirely; ms may be negative, never a rejection reason) shared by both; a new `sessionAttemptsStructurallyValid(session)` (structural only: types/arrays/canonical-key membership) used by `Migrate.validateImport`; and the existing `completedSessionEvidenceProblem`/`journalEvidenceProblem` (now skew-tolerant: `a.t < session.startedAt - skew || a.t > session.endedAt + skew`, `a.t < journ.startedAt - skew`) used only by `Migrate.preflightEvidence` (boot-only). `rebuildEvidence`'s replay clamps `ms: Math.max(0, entry.a.ms)`.
Regression tests: `[MEDIUM-3] validateImport accepts a negative ms`, `[MEDIUM-3] the boot preflight tolerates a 2-minute clock skew but rejects a 1-day one`, `[MEDIUM-3] rebuildEvidence clamps a negative ms to 0 at replay`.
Revert-run-restore: reverted all three semantic changes (ms<0 rejection restored, skew tolerance removed from both bound checks, ms clamp removed from replay, validateImport's session check reverted to call `completedSessionEvidenceProblem` directly) via a scripted `python3` patch; ran the three tests:
  - ms:-3 import test: `AssertionError [ERR_ASSERTION]: problems: ["sessions[0].attempts is inconsistent with planned/firstTryCorrect/misses"] — false !== true` (expected `result.ok === true`, got `false`).
  - 2-min/1-day skew test: `AssertionError [ERR_ASSERTION]: Expected values to be strictly deep-equal: ... actual { ok: false, reason: 'malformed' } expected { ok: true }` (the 2-minute-skew case, which must pass, failed under the pre-fix zero-tolerance bound check).
  - ms-clamp test: same `{ok:false,reason:'malformed'}` vs `{ok:true}` mismatch on the message "a negative ms must not fail the preflight — it's tolerated and clamped".
  Restored the fix; re-ran — all three green.

**MEDIUM-4 — 11 of 14 mutants survived (tests passing for the wrong reason).**
Added one hand-built minimal-session fixture per invariant (`minimalSession()` helper, 2-fact sessions built directly as plain objects, not via SessionCore play, so exactly ONE invariant is violated per test) asserting `Migrate.preflightEvidence` returns `{ok:false, reason:"malformed"}`: duplicate first attempt (misses consistent), missing planned fact, `t` before startedAt beyond skew, `t` after endedAt beyond skew, key ∉ planned, `firstTryCorrect` mismatch, `misses` mismatch, journal multiset mismatch, journal `t < startedAt` beyond skew, non-finite `ms` (`Infinity`). Plus a `validateImport`-level test for the key-∉-planned STRUCTURAL check specifically (the preflight's semantic copy and validateImport's structural copy are two separate call sites in the source — a mutant on one is invisible to a test that only exercises the other), and a RETRY-attempt variant of the key-∉-planned test (a retry attempt's key isn't covered by the multiset check, which only inspects non-retry attempts — needed to isolate that check from the multiset check, which otherwise coincidentally catches the same scenario for non-retry attempts and hides a killed-by-coincidence mutant). Plus `[MEDIUM-4] rebuildEvidence preserves interrupted on replay` — 3 sessions each with one correct-but-`interrupted:true` attempt on the same fact; asserts `Facts.mastery()` stays `"learning"` after rebuild (proving `interrupted` survives the replay's own attempt-record reconstruction, not just the original).
Mutation harness: the reviewer's `mut/run.js` (scratchpad `rev/mut/`) had a path bug — `path.join(__dirname,"..","core.js")` (where `__dirname` is `mut/`) resolves to `rev/core.js`, the ORIGINAL unfixed sibling, while the test file's own `require("../core.js")` resolves to `mut/core.js` — so the harness was mutating a file the test never even loaded (confirmed empirically with a debug `console.error` before trusting the discrepancy). Fixed locally (scratchpad only, not shipped) by changing that one path to `path.join(__dirname,"core.js")`, and updated 4 of the 14 mutant `from` patterns (M1, M2, M9, M10) to match the current (already-restructured) source text — the OLD literal patterns no longer exist verbatim after the MEDIUM-1/3 refactor, so the harness reported "PATTERN NOT FOUND" for those four until the patterns were updated to their current-code equivalents (M2's mutant now reintroduces the exact HIGH-1 bug on today's fixed line). Synced my real `core.js`/`tests/evidence-rebuild.test.js` into the scratchpad `mut/` dir and re-ran after each addition. Final kill count: **14/14 killed** (`M1_no_t_bounds` … `M14_interrupted_dropped`, all report `killed`; verified with a fresh sync-and-rerun in a single shell invocation to rule out a stale-copy artifact, the same class of bug that caused the earlier path confusion).

**LOW-5 — boot rebuild save wrapped in try/catch.**
`index.html` `boot()`: the `await save(function (s) { MathCore.Migrate.rebuildEvidence(s, Date.now()); })` call is now inside `try { ... } catch (e) { console.error("evidence rebuild failed; continuing on the un-rebuilt state", e); }` — a thrown error (or a rejected save) no longer blocks boot; the app proceeds on the un-rebuilt state and the guard stays pending for the next boot to retry. No automated regression test (this is a boot-sequence UI change, not unit-testable without a DOM/App.storage harness); verified by code inspection and `node --check` on the extracted inline script.

**LOW-6 — parent data section shows evidence-rebuild status; guard must not re-save every boot when unchanged.**
Added `T.parent.evidenceRebuiltLabel = "עדכון נתוני למידה"` and `T.parent.evidenceRebuildStates = { rebuilt: "בוצע", pending: "ממתין", trimmed: "לא נדרש", malformed: "לא בוצע — נתונים לא עקביים" }` (verbatim, per the coordinator's message — not composed by me). New `evidenceRebuildStatusKey(state)` maps `state.meta.evidenceRebuild` to one of the four keys (`null` → pending; `reason==="rebuilt"` → rebuilt; `reason==="trimmed"` → trimmed; else → malformed); rendered as a new line in `renderDataSection`. Boot's rebuild-guard logic now computes `MathCore.Migrate.preflightEvidence(state)` itself before deciding to save: if the result is a failure AND the existing recorded guard already has `done:false` with the SAME `reason`, the save is skipped entirely (no needless IDB write/rev bump every boot on a device with genuinely inconsistent history); any other case (first failure, a different failure reason, or the preflight now passing) still saves. Verified the decision logic in isolation (4 cases: first failure → save; same failure repeated → skip; reason changed → save; now passes → save) — all behaved as intended. No automated regression test in the `node --test` suite (boot-sequence logic, not unit-testable without extracting it into core.js, which the plan does not authorize this batch); `node --check` on the extracted inline script passes.

**LOW-7 — static `.bubble` top didn't match its breakpoint's keyframe start (visible snap when the animation-delay ends).**
`index.html`: added `top: -130px` to the `@media (max-width: 380px) { .bubble { ... } }` rule (matching that breakpoint's `@keyframes bubble-fall { from { top: -130px; } ... }`) and `top: -110px` to the `@media (max-height: 420px) and (min-width: 480px) { [data-screen="question"][data-falling="1"] .bubble { ... } }` rule (matching that breakpoint's keyframe). Before this fix, during the 600ms `animation-delay` window every breakpoint showed the BASE rule's `-140px` (since keyframes only apply once the animation itself starts, not during its delay), then visibly snapped to `-130px`/`-110px` the instant the delay elapsed and the breakpoint's own keyframe kicked in. No automated regression test (CSS cascade timing, not `node --test`-able); verified by code inspection that the static value now equals the keyframe "from" value at each of the three breakpoints.

**Suite counts.** `npm test` → **186/186 green** (155 baseline + 31 in `tests/evidence-rebuild.test.js`, up from 11 before this fix round). `node tools/bump-version.js 0.12.0` re-run (both `index.html` and `core.js` changed by this fix round) → `core.js?v=47ae0d775d`, `release 0.12.0-3c2e1651aa`. Rule-6 grep (`grep -n 'MathCore\.\(SessionCore\|Economy\|Pin\|Migrate\)\.' index.html`): every mutating call (`SessionCore.*`, `Economy.*`, and the two `Migrate.rebuildEvidence` call sites — boot and `maybeRebuildEvidence`) sits inside `save(function (s) {...})`; the new `Migrate.evidenceRebuildPending`/`Migrate.preflightEvidence` calls are pure read-only predicates (boot's own preflight check, and `maybeRebuildEvidence`'s guard check) — no direct-mutation-outside-save regression. Inline-script `node --check` on the extraction one-liner → clean.

deviations:
1. MEDIUM-2's regression test exercises `Storage.backupThenReplace` + the check-then-save sequence directly (not through the DOM/UI layer, which isn't reachable from `node --test`); the UI wiring itself is verified by grep, not by an automated test, per the coordinator's own instruction ("UI callers verified by grep in the log").
2. LOW-5/6/7 have no automated `node --test` regression test — they are boot-sequence/CSS-cascade changes not reachable from the existing test harness without scope beyond what this batch authorizes (no DOM harness, no extraction of the guard-decision logic into core.js). Verified by code inspection, the extracted-script `node --check`, and (for LOW-6's decision logic) an isolated logic trace with 4 cases.
3. The reviewer's `mut/run.js` mutation harness had a pre-existing path bug (documented above under MEDIUM-4) that made ALL of its "SURVIVED" verdicts against my ORIGINAL package-0 code meaningless (it was mutating a file the test suite never loaded) — I fixed this locally in the scratchpad copy only (never touched anything the reviewer or plan owns) to get a trustworthy kill count.

assumptions discovered: none beyond the above.

## 2026-08-28T20:40:11Z · designer · 0-R: review (fresh Fable 5) SHIP-AFTER-FIXES → executor fixed HIGH-1 (index-gated replay dropped stations), MEDIUM-2 (import/restore never rebuilt), MEDIUM-3 (validateImport rejected whole backups on clock skew), MEDIUM-4 (11/14 mutants survived → 20 tests added, 14/14 killed), LOW-5/6/7. Fix-verification: all FIXED; NEW-ISSUE-A (skewed t past endedAt replayed after its own boundary) → designer applied the reviewer-validated sort key k=min(t,endedAt) + regression fixture (proved to fail pre-fix: map.reached {} vs {1:…}) + residual journal-station fixture. Suite 188/188. NEW-ISSUE-B (non-monotonic endedAt skews one masteredAfter value) accepted as inherent, noted in NEXT-ACTIONS.

## 2026-08-28T20:40:26Z · designer · package 0 committed+pushed as 59d260a · package S2 baseline = the commit containing this entry (given verbatim in the kickoff prompt)

## 2026-08-28T22:40:00Z · plan 1.0 · S2 preflight · DONE
evidence: `git rev-parse --short HEAD` = 16d5a88 (matches kickoff baseline); `git status --porcelain` shows only `.claude/` untracked; `npm test` 188/188 green. Read plan §0,§1,§3,§4,§5, package section S2 in §2, `docs/V2-DESIGN.md` §7.2, `CLAUDE.md`. Current step identified from status log: no S2-* entries exist yet (last entry is "package S2 baseline = 16d5a88"), so starting at S2-1. Adopted this file's existing entry format (`## <ISO> · plan 1.0 · <step> · <STATUS>` + evidence/deviations/assumptions). Verified anchors against live `index.html` (line numbers in the plan/kickoff are stale, confirmed correct per kickoff note): `<style>` line 16, `</style>` line 319 (CSS body lines 17–318); `<script src="core.js?v=...">` line 330; inline `<script>` line 331 … `</script>` line 2774; `var T = {` line 340, closing `  };` line 583; `function escapeHtml` line 798, `function tableTag` line 810, `function bdi` line 812. Grepped T block (lines 340–583) for `escapeHtml(`/`bdi(`/`tableTag(` references: only `bdi`/`tableTag` are used inside T (15 call sites), zero `escapeHtml` references — confirms the plan's designer-verified claim; no ESCALATION needed.
deviations: none.
assumptions discovered: the "moved block" for strings.js is non-contiguous in the current file — `T` (340–583) and the `escapeHtml`/`tableTag`/`bdi` trio (798–814) are separated by ~200 lines of unrelated app code (STICKER_EMOJI, stickerArt, boot-related helpers). Read S2-2/S2-3 as: extract both spans verbatim into `strings.js`; in `app.js`, replace the `T` span with the single `var T = window.MathText.T, escapeHtml = ..., tableTag = ..., bdi = ...;` line and delete the now-redundant `escapeHtml`/`tableTag`/`bdi` function declarations at their original (now-shifted) position — this is the only reading consistent with "moved block replaced ... at the same position" given non-contiguity, and preserves byte-identical everything else.

## 2026-08-28T22:45:00Z · plan 1.0 · S2-1 · DONE
evidence: Created `styles.css` = `sed -n '17,318p' index.html` from the pre-edit file (the exact text between `<style>` line 16 and `</style>` line 319, 302 lines). Replaced the `<style>…</style>` block in `index.html` with `<link rel="stylesheet" href="styles.css?v=PENDING" />` at the same position (line 16), via a targeted Python edit that asserted the exact `<style>`/`</style>` lines before removing them (no regex, no risk of over-matching). All inline `style=""` attributes elsewhere in the file left untouched (not part of this extraction per design §7.2). Byte-identity proof: `diff <(git show 16d5a88:index.html | sed -n '17,318p') styles.css` → empty output, exit code 0. Post-edit sanity: `grep -n "^<style>\|^</style>\|^<script src=\"core.js\|^<script>$\|^</script>$" index.html` now shows only the `core.js` script tag (line 27, was 330) and the inline `<script>`/`</script>` pair (lines 28/2471, was 331/2774) — 302 lines removed, offsets shifted by exactly 302 as expected, no other `<style>` tags remain.
deviations: none.
assumptions discovered: none.

## 2026-08-28T22:55:00Z · plan 1.0 · S2-2 · DONE
evidence: Created `strings.js` = `"use strict"; window.MathText = (function () { ... })();` wrapping, verbatim in original order: the `var T = {…};` block (post-CSS-extraction lines 37–280 of `index.html`, matching original-commit lines 340–583) and the `escapeHtml`/`tableTag`(+its bidi comment)/`bdi` trio (post-CSS-extraction lines 495–511, matching original-commit lines 798–814); returns `{ T: T, escapeHtml: escapeHtml, tableTag: tableTag, bdi: bdi }`. Confirmed non-contiguity noted in the preflight entry: 214 lines of unrelated app code (`STICKER_EMOJI`, `stickerArt`, boot helpers, etc.) sit between the two spans in `index.html` and are NOT moved — they stay in place for S2-3 to become `app.js`. `node --check strings.js` → OK. Byte-identity proofs (all empty diff / exit 0): (1) `diff <(git show 16d5a88:index.html | sed -n '340,583p') <(sed -n '37,280p' index.html)` — T block unchanged by the CSS extraction; (2) `diff <(git show 16d5a88:index.html | sed -n '798,814p') <(sed -n '495,511p' index.html)` — helper trio unchanged; (3) `diff` of each span against the corresponding lines inside `strings.js` itself (T against lines 3–246, helpers against lines 248–264) — both empty. Re-verified T references only `bdi`/`tableTag`, never `escapeHtml` (grepped inside `strings.js`'s T span) — consistent with the S2-2 designer claim, no ESCALATION.
deviations: none beyond the non-contiguity handling already recorded in the preflight entry.
assumptions discovered: none new (see preflight entry for the non-contiguity assumption).

## 2026-08-28T23:05:00Z · plan 1.0 · S2-3 · DONE
evidence: Built `app.js` = the remaining inline IIFE verbatim (post-CSS-extraction script content, 2181 lines), with the `T` block (post-extraction lines 37–280, original-commit lines 340–583) replaced in place by `var T = window.MathText.T, escapeHtml = window.MathText.escapeHtml, tableTag = window.MathText.tableTag, bdi = window.MathText.bdi;` and the `escapeHtml`/`tableTag`/`bdi` function-declaration span (post-extraction lines 495–511, original-commit lines 798–814, plus its trailing blank line) deleted outright (its comment moved with it, since it documents `tableTag`/`bdi` and now lives in `strings.js`). Used a Python splice (not sed) with asserted line contents at every boundary to guarantee no accidental truncation. `node --check app.js` → OK. Rewrote `index.html`'s script tags: removed the inline `<script>…</script>` pair and the old `<script src="core.js?v=...">`, replaced with `<script src="core.js?v=PENDING"></script><script src="strings.js?v=PENDING"></script><script src="app.js?v=PENDING"></script>` in that order, no `async`/`defer` (confirmed by `grep -n "async\|defer" index.html` → no output). `index.html` is now 31 lines (plan's "≈40 lines" estimate — close, and every remaining line is meaningful head/body markup). Byte-identity proof of the diff, exactly as the plan's S2-3 done-when requires: `diff <(git show 16d5a88:index.html | sed -n '332,2773p') app.js` — the ONLY differences are (a) the 238-line `T` block replaced by the single `var T = window.MathText...` line, and (b) the 20-line `escapeHtml`/comment/`tableTag`/`bdi` span (plus its trailing blank line) deleted with no replacement. No other line differs anywhere in the 2181-line file (confirmed by inspecting the full diff output — every hunk maps to exactly these two spans, nothing else). `node --check strings.js` and `node --check app.js` both OK; `node --check` not yet meaningful for `index.html` (HTML, not JS) — deferred to the S2-4 headless boot check.
deviations: `?v=PENDING` placeholders left on all four asset references in `index.html` (`styles.css`, `core.js`, `strings.js`, `app.js`) — these are filled in by `node tools/bump-version.js` in S2-4, per the plan's step order (bump tool runs after the split, hashing four assets instead of one).
assumptions discovered: none new.

## 2026-08-28T23:20:00Z · plan 1.0 · S2-4 · DONE
evidence: Edited `package.json` `"test"` script to `"node --check app.js && node --check strings.js && node --check sw.js && node --test 'tests/**/*.test.js'"`. Ran `node tools/bump-version.js 0.13.0` — output: `bump-version: 0.13.0 (release 0.13.0-4c79839ee1)` with all four assets hashed (`core.js?v=23b97b57e6`, `strings.js?v=5a9c2e5cd8`, `app.js?v=1947690672`, `styles.css?v=3b43ad01af`), replacing the four `?v=PENDING` placeholders left by S2-1/S2-3. Idempotence re-verified against the split repo: ran the tool twice at 0.13.0, `diff` of `index.html`/`sw.js` before vs. after the second run → both empty. `npm test` → exit 0, all three `node --check` steps passed silently, then `node --test` reported 188/188 (0 fail). `grep -n "MathCore\.(SessionCore|Economy|Pin|Migrate)\." app.js` (rule-6 grep, no logic touched in S2 so unaffected) still shows every call site inside a `save(function (s) {...})` mutator (unchanged from batch-0's baseline, since S2 moved text only, never edited a call site). Updated `CLAUDE.md`'s `## Files` section: split the old monolithic `index.html` entry into five entries (`index.html` = shell only, `styles.css`, `strings.js`, `app.js`, `sw.js` unchanged); updated rule 2 (`T` now lives in `strings.js`, not `index.html`) and the Testing section (`npm test` now runs `node --check` on `app.js`/`strings.js`/`sw.js` before `node --test`). Checked `README.md` for a "file list" as the plan/kickoff directs: grepped for `index.html|core.js|sw.js|styles.css|strings.js|app.js` — the only hits are the two `http://localhost:8766/index.html` usage lines and the generic "content-hashes every deployable asset" wording in "עדכון האפליקציה"/"Updating the app"; README carries no enumerated per-file list (it's a parent-facing usage doc, not a dev file map), so nothing there names specific split files that would go stale — left unedited, recorded as a deviation below. Headless boot check: built a wrapper page (`s2-boot-outer.html` in the scratchpad) that loads `index.html` via `file://` in an iframe, installs `onerror`/`unhandledrejection` listeners on the iframe's window, waits 2.5s, then reports the first `[data-screen]` element found plus any captured errors; ran via `Google Chrome --headless=new --disable-gpu --allow-file-access-from-files --virtual-time-budget=6000 --dump-dom`. Result: `PASS :: screen=parent-setup; appHtmlLen=957 :: errors=[]` — the app boots on a fresh (unseeded) profile straight to the parent-setup screen (expected first-run behaviour, no seeding attempted per the plan's fallback clause) with zero console errors / unhandled rejections. Confirms `core.js` → `strings.js` → `app.js` load and wire up correctly end to end.
deviations: README.md's file lists were checked but not edited — it contains no enumerated per-file structure list (see evidence above); the generic wording already covers the new asset set correctly. Headless boot check asserted the parent-setup screen (not Home) since seeding localStorage/IndexedDB from a `file://` wrapper iframe was impractical within this step's tooling — this is exactly the plan's stated fallback ("if seeding is impractical, assert the parent-setup screen renders and no console errors").
assumptions discovered: none new.

## 2026-08-28T21:01:38Z · designer · S2-R review (fresh Fable 5): SHIP — nothing real found (equivalence proved by diff + vm evaluation of all 28 T functions; headless boot to Home and a question screen with zero exceptions; deploy contract covers all four assets). Notes only: app.js line 9 TypeErrors if strings.js 404s (same class as core.js today); .claude/ stays untracked.

## 2026-08-28T21:01:40Z · designer · S2 committed+pushed as dd660ca · package S3 baseline = the commit containing this entry (given verbatim in the kickoff prompt)

## 2026-08-28T21:00:00Z · plan 1.0 · S3 preflight · DONE
evidence: `git rev-parse --short HEAD` = 09275f9 (matches kickoff baseline); `git status --porcelain` shows only `.claude/` untracked; `npm test` 188/188 green. Read plan §0,§1,§3,§4,§5, package section S3 in §2, `docs/V2-DESIGN.md` §7.3, `CLAUDE.md`. Current step identified from status log: no S3-* entries exist yet (last entry is "package S3 baseline = 09275f9"), so starting at S3-1. Adopted this file's existing entry format (`## <ISO> · plan 1.0 · <step> · <STATUS>` + evidence/deviations/assumptions).
deviations: none.
assumptions discovered: none.

## 2026-08-28T21:06:00Z · plan 1.0 · S3-1 · DONE
evidence: `core.js` `StorageInstance.prototype.save` (line 1991): mutator's synchronous return value is captured (`var mutatorValue = mutator(clone);`) and threaded onto the success resolution as `{ ok:true, state, value: mutatorValue }`; doc comment above `save` updated to describe `value`. `backupThenReplace` left unchanged (plan says so explicitly).
Call sites in `app.js` rewritten from a closure-variable side channel to `return` from the mutator + read `result.value`:
- `paintNextQuestion` (was line 541-545): `var paintedResult = null; save(function(s){ paintedResult = ...paint(...); })` → `save(function(s){ return ...paint(...); })`, `result.value` replaces `paintedResult`.
- `submitAnswer` (was line 921-926): `var submitResult = null; save(function(s){ submitResult = ...submit(...); })` → mutator now returns the submit result; `showFeedback(result.value)` replaces `showFeedback(submitResult)`.
- `finishSession` (was line 1041-1049): `var sessionResult = null; save(function(s){ sessionResult = ...finish(...); })` → mutator returns the finish result; `result.value` replaces `sessionResult` in the `if (result.ok && result.value)` guard and in `App.lastSessionResult = result.value`.
- reward approval, `wireRewardsSection`'s `data-approve-request` handler (was line 1560-1568): `var approveResult = null; save(function(s){ approveResult = ...approveRequest(...); })` → mutator returns the approve result; `result.value` replaces `approveResult` in the `if (result.value && !result.value.ok)` insufficient-balance check.
`result.ok` checks added at the three sites the plan names:
- **parent setup → recovery screen** (`onSetupContinue`, was line 403-405): the PIN-hash save was previously unconditionally followed by `showRecoveryCode(recoveryCode)` regardless of whether the save succeeded. Now: `if (result.ok) showRecoveryCode(recoveryCode); else errorEl.textContent = T.saveFailure;`.
- **PIN change → dashboard unlock** (`renderParentNewPin`'s `save-new-pin` handler, was line 1394-1397): previously always set `App.parentUnlocked = true` and rendered the dashboard after the save promise resolved, even on failure. Now: `if (!result.ok) { errorEl.textContent = T.saveFailure; return; }` gates the unlock.
- **`cloudApply`/`cloudRestore` success message**: both functions previously did `.then(function () { return { ok: true }; })` after the settings save, ignoring whether that save itself succeeded (a stale/error save would still report `{ok:true}` to the caller, which shows "restored" to the parent even though nothing was actually persisted). Both now check `result.ok` on the save and return `{ ok: false, error: T.saveFailure }` on failure; the existing callers (`cloud-restore-confirm` handler and the setup-screen `setup-cloud-restore` handler) already branch on `r.ok`/`r.error` via `T.parent.restoreFailed(r.error || "?")`, so this wiring alone fixes the previously-silent failure path with no caller-side changes needed.
Test added: `tests/storage.test.js` "save() returns the mutator's return value as result.value" — asserts `result.value` equals a mutator's returned object, and that a mutator returning nothing gives `result.value === undefined`. `node --test tests/storage.test.js` → 20/20 green (was 19).
`grep -n "var .* = null;$" app.js` → 7 hits, none adjacent to a `save(` call site (`audioCtx`, `cloudTimer`, `lastRoutedScreen`, `fallingKeyHandler`, `challengeTimerHandle`, `n, m` in an unrelated parsing helper, `file` in the import-file picker) — confirms no closure-variable side channel remains around any `save()` call.
`npm test` → 189/189 total minus 2 pre-existing deploy-contract failures (index.html/sw.js asset hashes stale because `core.js`/`app.js` changed and the bump tool has not been re-run yet — expected; the plan runs `bump-version` at S3-6), i.e. 187/189 pass, 2 fail (both `deploy-contract.test.js`, both hash-staleness, not S3-1 regressions). Baseline before this step was 188/188 (with 1 new test not yet added) — after adding the new save-value test: 189 total, 187 pass + 2 known-stale-hash fails.
deviations: none.
assumptions discovered: `cloudApply`/`cloudRestore`'s "ignore the save's own ok" bug was not explicitly named as a bug in the plan text (the plan just says "add result.ok checks to ... cloudApply success message"), but reading the existing code the omission was real (a stale/failed settings-save after a successful import would still report overall success) — fixed as literally specified.

## 2026-08-28T21:20:00Z · plan 1.0 · S3-2 · DONE
evidence:
1. **Strategy-hint Hebrew → `T.question.strategies`** (`strings.js`, new block after `strategyIntro`): 10 template functions (keys `10,9,5,2,4,8,3,6,7,default`, matching the original `switch(n)` cases verbatim, including the exact Hebrew phrasing "— מוסיפים 0", ", חצי מזה: ", ", כפול 2: ", " — כפול 1 נשאר אותו מספר" and the `<span class="ltr">…</span>` wrapping the original `eq()` helper produced) — no rewording, byte-for-byte the same output. `app.js`'s `strategyHint(a, b)` (was 24 lines with the inline switch) now only picks `n`/`m`/`p` and delegates: `var tpl = T.question.strategies[n] || T.question.strategies.default; return tpl(m, p);`.
2. **`[4,5,6]` → CONFIG range**: `app.js` `renderSettingsSection`'s `<select id="set-falling-options">` now builds its `<option>`s with a `for` loop over `CONFIG.FALLING.MIN_OPTIONS..MAX_OPTIONS` (4..6 today) instead of the hardcoded array literal.
3. **`"/55"` → `MathCore.Facts.allKeys().length`**: `app.js` line (KPI tile) now reads `kpiTile(T.parent.kpiMastered, totals.masteredCount + "/" + MathCore.Facts.allKeys().length)`. Verified `MathCore.Facts.allKeys().length === 55` today (`node -e "console.log(require('./core.js').Facts.allKeys().length)"` → 55).
4. **`CONFIG.WRONG_ANSWER_HELPER_MS` deleted** (`core.js`, was line 85): confirmed genuinely dead first — `grep -rln "WRONG_ANSWER_HELPER_MS" --include='*.js' .` (excluding node_modules) showed only `core.js` itself; the wrong-answer flow (`app.js` `App.pendingContinue`) waits for an explicit "הבנתי"/Enter tap, not a timeout, so the constant was never read anywhere. `CONFIG.WRONG_ANSWER_DISPLAY_MS` (still used, `app.js` line ~1024) left untouched. No test referenced the deleted constant.
5. **`validateImport` id constraint + attribute escaping**: new module-level `SAFE_ID_PATTERN = /^[A-Za-z0-9_-]{1,64}$/` in `core.js` (placed just above `var Migrate = {`); `rewards[i].id` and `requests[i].id` checks changed from "non-empty string" to `SAFE_ID_PATTERN.test(...)`. `app.js`: all four id-bearing attributes now `escapeHtml()`-wrap the id — `data-reward`, `data-deactivate-reward`, `data-approve-request`, `data-reject-request` (defense in depth; the ids can no longer contain quote/angle-bracket characters after the import-time constraint, but render-time escaping is cheap insurance per the plan's explicit instruction).
Test added (`tests/migrate.test.js`, after the existing "[review] validateImport rejects a request with a string costSnapshot or bad status"): `[S3-2] validateImport rejects a reward or request id containing attribute-breaking characters` — asserts a reward id `x" onclick="alert(1)"` is rejected, a request id with the same shape is rejected, and a well-formed generated-shape id (`r_<timestamp>_<random>`) still passes. `node --test tests/migrate.test.js` → 20/20 green (was 19).
Full `npm test` → 190 total, 188 pass, 2 known deploy-contract hash-staleness fails (unchanged from S3-1, pending the S3-6 bump).
`grep -nP "[\x{0590}-\x{05FF}]" app.js` → 4 hits, all in `//` comments, none in code: line 988 (`// The picture stays until she taps "הבנתי" ...`), line 1026 (`// wrong answer: wait for "הבנתי" / Enter`), line 1130 (`// "עוד סבב" stays in the same game`), line 1602 (`// "סבב N" labels ...`).
deviations: none.
assumptions discovered: none beyond what S3-1 already noted.

## 2026-08-28T21:35:00Z · plan 1.0 · S3-3 · DONE
evidence: Three pure extractions, all in `core.js`'s `SessionCore.finish` plus one in `Migrate`, all moved verbatim (same computed values, same ledger-append order/ids) into private (unexported, module-scope) helper functions placed just above `var SessionCore = {`:
- `applyBonuses(state, active, sid, firstAttempts, now)` — the base-coins ledger append, the streak-bonus loop, and the perfect/near-perfect branch (was `finish`'s lines computing `earnAmount`/`totalCoins`/streak/perfect/near-perfect); returns `{ totalCoins, perfect, perfectSeries }`.
- `computeNextCarryover(state, active, misses)` — the falling-mode-exempt carryover recompute (was `finish`'s `nextCarryover`/`leftoverCarryover` block); returns the new `carryover` array.
- `makeSessionRecord(active, sid, now, firstTryCorrect, totalMs, misses, totalCoins, perfect, perfectSeries, masteredAfter, unlocksEarned, stationsReached)` — the session-record object literal (was `finish`'s `var session = {...}`).
`SessionCore.finish` itself is now: compute `sid`/`firstAttempts`/`firstTryCorrect`/`misses`/`totalMs` → `applyBonuses(...)` → `Economy.newUnlocks` (unchanged, outside the extraction) → map/`masteredAfter` (unchanged) → `computeNextCarryover(...)` → `makeSessionRecord(...)` → push/retention-trim/carryover-assign/unpark (unchanged) — same statement order as before, just the three named blocks factored out.
`Migrate.normalizeSettings(rs)` — new `Migrate` method; the settings-normalization block previously inlined separately in both `emptyState()` (hardcoded literal defaults) and `migrate()` (the `rs.x || default` block) is now one function: `emptyState()` calls `Migrate.normalizeSettings({})` (verified its output is byte-identical to the old hardcoded literal — every `rs.x || default` degrades to the literal default when `rs` is `{}`); `migrate()` calls `Migrate.normalizeSettings(rs)` with the real `raw.settings || {}`.
`app.js`: new `questionViewModel(state)` — the current-question dots/resolvedSet/retrySet/doneOrRetryCount/`parts`/coin `value` computation, previously duplicated verbatim in both `renderQuestion` and `renderFallingQuestion`; returns `null` when `!active.current` (both callers already `return` early in that case, now via `if (!vm) return;`) or `{ active, current, parts, dotsHtml, doneOrRetryCount, value }`. Both render functions now destructure from `vm` instead of recomputing; the rest of each function body is untouched.
`node --check core.js` and `node --check app.js` both clean. `npm test` → 190 total, 188 pass, 2 fail — the SAME 2 pre-existing deploy-contract hash-staleness failures carried from S3-1/S3-2 (nothing else changed): `git status --porcelain -- tests/` shows only `tests/migrate.test.js` and `tests/storage.test.js` modified, both from S3-1/S3-2 — **zero test-file diffs from this step**, satisfying the done-when exactly.
deviations: none.
assumptions discovered: `applyBonuses`'s parameter list matches the plan's literal signature (`state, active, sid, firstAttempts, now`); `firstTryCorrect` is recomputed inside it from `firstAttempts` (a pure, cheap re-derivation) rather than threaded in as an extra parameter, since the plan's signature doesn't list it and `finish` already needs its own copy of `firstTryCorrect` for the session record — recomputing avoids a parameter the plan didn't ask for while keeping the value used by both call sites in exact agreement (same pure function of the same `firstAttempts` array).

## 2026-08-28T22:00:00Z · plan 1.0 · S3-4 · DONE
evidence: Installed the ONLY package the grant allows: `npm install --save-dev --save-exact --ignore-scripts linkedom@0.18.13` (0.18.13 confirmed as the current latest stable via `npm view linkedom versions --json` at install time — no newer 0.18.x/0.19.x exists). `package.json`/`package-lock.json` now pin `"linkedom": "0.18.13"` exactly, alongside the existing `fake-indexeddb`. `package.json`'s `test` glob (`node --test 'tests/**/*.test.js'`, set in S2-4) already matches `tests/ui/*.test.js` recursively — no glob change needed.
New files:
- `tests/ui/harness.js` — the DOM smoke harness. `buildWindow(opts)` creates a fresh `linkedom` window per test (no shared state) and installs every stub the plan lists plus two linkedom-gap fixes discovered empirically:
  - **`indexedDB`**: fresh `fake-indexeddb` `IDBFactory` per window.
  - **`localStorage`**: linkedom has no Storage implementation — a tiny in-memory object-backed shim (getItem/setItem/removeItem/clear).
  - **`matchMedia`**: returns `{ matches: !!opts.mediaMatches[query], ... }` — defaults to "no match"; a test opts into coarse-pointer/reduced-motion via `opts.mediaMatches`.
  - **`crypto`**: Node's real `node:crypto` `webcrypto` (not a stub) — `MathCore.Pin.hash`/`verify`/`generateRecoveryCode` run their real WebCrypto digest/getRandomValues calls. Discovered empirically that linkedom defines `window.crypto` as a getter-only accessor (a plain assignment throws `Cannot set property crypto of #<Object> which has only a getter`) — fixed with `Object.defineProperty`.
  - **`fetch`**: rejects by default ("fetch disabled in tests"); a cloud-flow test can pass its own `opts.fetch`.
  - **`requestAnimationFrame`/`cancelAnimationFrame`**: linkedom has neither — `setTimeout`-based stubs.
  - **`navigator.storage`**: `.persist()`/`.persisted()` resolving `true`. Discovered empirically that linkedom's `navigator` silently swallows a plain `nav.storage = {}` assignment (backed by a proxy with no `storage` trap — the property reads back `undefined` with no thrown error) — fixed with `Object.defineProperty` on `window.navigator` directly.
  - **No `navigator.serviceWorker`** (deliberate): `registerServiceWorker()` checks `"serviceWorker" in navigator` and no-ops when absent — correct per the plan's stub list (no test needs a real SW).
  - **`AudioContext`**: a stub class (`resume`/`createOscillator`/`createGain` returning inert node-likes) so `blip()` never throws if a test's click path happens to trigger it; no test asserts on audio.
  - **`BroadcastChannel`**: no-op class (each harness window is a single independent "tab").
  - **`location`/hashchange** (not on the plan's stub list, but required — discovered empirically that linkedom provides no `window.location` at all, and the app's router is entirely hash-driven via `location.hash =` + a `hashchange` listener): a minimal getter/setter shim that dispatches a real `window.Event("hashchange")` when the hash string actually changes, deduplicated exactly like a real browser (setting the same value is a no-op, no event fires).
  - **linkedom leaks the host's CommonJS `module`/`exports`/`require` onto its `window`** (discovered empirically: `core.js`'s UMD wrapper's `typeof module !== "undefined"` check saw the HOST Node module and called `module.exports = MathCore` on it instead of `window.MathCore`, leaving `MathCore` undefined) — `buildWindow` deletes `window.module`/`exports`/`require` before `loadApp` runs, forcing the wrapper into its browser (`window.MathCore = ...`) branch, exactly what a real `<script src>` load does.
  `loadApp(window)` runs `core.js`, `strings.js`, `app.js` in that order via `vm.createContext(window)` + `vm.runInContext` (so bare identifiers like `requestAnimationFrame`/`BroadcastChannel`/`location` resolve against `window` as the context's global object, matching real `<script>` semantics) — `app.js`'s trailing `boot();` self-invokes exactly as it does in the browser. `bootApp(opts)` builds, loads, and flushes 15 microtask/timer turns so the async boot sequence (IDB load → migrate → evidence-rebuild save → `navigator.storage.persist()`) settles before returning. Convenience helpers: `flush(n)`, `currentScreen(window)`, `fireClick(el)`, `completeParentSetup(window, opts)`, `answerCurrentQuestionCorrectly(window)` (drives the coarse-pointer numpad), `playSessionToSummary(window, max)` (sets `window.MathCore.CONFIG.WRONG_ANSWER_DISPLAY_MS = 0` on the LOADED copy only — a test-only override of the in-memory CONFIG object, never a change to `core.js` itself — so a full session doesn't need to wait out the real 1.8 s feedback delay).
- `tests/ui/app.test.js` — 7 tests, one per flow the plan lists, each named for the flow it covers:
  1. `[S3-4] first-run setup: happy path reaches Home, and a failed save keeps the parent on the setup screen with an error` — happy path to Home, PLUS forces the next save stale (`storage.rev += 1` before the CAS write, the same condition a losing second-tab race produces) and asserts the screen stays on `parent-setup` with `T.saveFailure` shown — this is a live regression test for the exact S3-1 bug (onSetupContinue used to call `showRecoveryCode()` unconditionally regardless of `result.ok`). **Proved to fail against the pre-S3-1 code**: temporarily reverted the `onSetupContinue` fix (`.then(function(){ showRecoveryCode(recoveryCode); })` with no `result.ok` check), reran — test 1 failed (`not ok 1`); restored the fix, reran — green again (7/7). Command: manual revert via a scripted Python patch + `node --test tests/ui/app.test.js`, then the inverse patch to restore.
  2. `[S3-4] Home -> typed question -> answer -> next question -> summary` — plays a full 10-question typed session to a perfect summary via `playSessionToSummary`, asserts `firstTryCorrect === planned.length`, the first question asked is in the plan, and `state.active` is cleared.
  3. `[S3-4] falling mode renders one .bubble per configured option, sized to settings.falling.options` — enables falling with `options: 5`, asserts `.lanes[data-n="5"]` and exactly 5 `.bubble` elements.
  4. `[S3-4] exit suspends the session (state.active survives) and Home's play button resumes the SAME question` — exit → confirm → `state.active` still present with the SAME session id (D12) → tap play again → back on `question` with the same id.
  5. `[S3-4] the parent area re-locks on every way of leaving it (PIN required again next visit)` — unlock via PIN → dashboard → navigate to Home → `App.parentUnlocked === false` → return to `#screen=parent` → back on `parent-pin`, not the dashboard (WP9 review finding A).
  6. `[S3-4] the child's name is HTML-escaped everywhere it's rendered` — sets `childName` to `<img src=x onerror="window.__pwned=1">`; asserts zero real `<img>` elements anywhere, the escaped form is present as text, `window.__pwned` was never set, AND that the raw (unescaped) value round-trips correctly through the parent dashboard's `<input value="...">` attribute (a second, independent injection point from the `<h2>` text-content one).
  7. `[S3-4] falling mode's CSS selector contracts exist` — asserts `styles.css` still defines rules for `[data-screen="question"][data-falling="1"]`, `.lanes`, `.bubble`, `.bubble.landed`, `.bubble.popped`, `.bubble.flyaway` (the FALLING-DESIGN D3 contract: lanes/bubbles never move on feedback), THEN boots to the falling screen and asserts the structural selectors (`[data-falling="1"]`, `.lanes`, `.bubble`) are actually produced by `renderFallingQuestion` — the runtime-added classes (`.landed`/`.popped`/`.flyaway`) are confirmed to exist in the stylesheet but are not asserted present on first paint (they're added by `animationend`/a pick, not on initial render).
`node --test tests/ui/app.test.js` → 7/7 green (first run, before the regression-proof revert-and-restore above). Full `npm test` → 197 total, 195 pass, 2 fail — the SAME 2 pre-existing deploy-contract hash-staleness failures (unchanged since S3-1; `styles.css`/`app.js`/`core.js` all changed across S3-1..S3-4 without the bump tool running yet, which S3-6 does).
deviations: the plan's stub list didn't mention `location`/hashchange, but linkedom provides no `window.location` at all and the app's entire router depends on it — added as a necessary 9th stub, called out explicitly above rather than silently folded into "matchMedia, serviceWorker, ..." et al.
assumptions discovered: linkedom 0.18.13 leaks the host Node `module`/`exports`/`require` onto its `window` object and defines `window.crypto`/`navigator.storage`-adjacent properties in ways that reject plain assignment (a getter-only accessor for `crypto`; a proxy that silently swallows unknown navigator property sets) — both discovered empirically via failing boot attempts, not documented anywhere in the plan; worked around in the harness as described above, nothing in `core.js`/`app.js`/`strings.js` was changed to accommodate the test environment.

## 2026-08-28T22:15:00Z · plan 1.0 · S3-5 · DONE
evidence: The four doc contradictions design §7.3-5 lists, fixed with cited line numbers:
1. **`docs/FALLING-DESIGN.md` line 26** — was: "`finish()` branches on `active.mode`: for 'falling' skip `Facts.updateFromAttempt`, carryover recomputation and `Map.newlyReached`". This is stale — the 2026-08-28 "balloons v2" work (recorded in `math-build-status.md`) made falling count for mastery/map exactly like typed; only carryover recomputation is still skipped (confirmed by reading `core.js`'s current `SessionCore.submit`/`computeNextCarryover`: `Facts.updateFromAttempt` and `Map.newlyReached` both run unconditionally on `active.mode`, only `computeNextCarryover` branches on `isFalling`). Rewrote line 26 to state the current, single-item exception (carryover only).
2. **`docs/DESIGN.md` line 83 ("save retry")** — was: "A failed save (quota/exception) is retried once, then shown as a persistent banner". `core.js`'s `Storage.save()` has no retry logic at all (confirmed by reading the function: it resolves `{ok:false,...}` on the FIRST CAS failure or thrown mutator, no loop/backoff); `wireStorageReactions()` (`app.js`) reacts to that first failure immediately (`showSaveFailureBanner()`/`showStale()`). Rewrote the sentence to state there is no automatic retry — the UI reacts on the very first failure; a "retry" only happens if the parent repeats the action.
3. **`docs/DESIGN.md` §6, line 55, and line 89 ("3.2 s helper")** — both cited the now-deleted `CONFIG.WRONG_ANSWER_HELPER_MS` (3.2 s) as the wrong-answer display duration. Confirmed via S3-2 (this same package) that the constant was already dead code — the wrong-answer flow has waited for an explicit "הבנתי" tap or Enter (`App.pendingContinue`) since 2026-08-27, with no timer at all. Line 55 (§6 Session algorithm) rewritten to describe the tap-to-continue mechanism; line 89 (§9 Screens, found while fixing line 55 — same stale claim, now referencing a CONFIG constant that no longer exists after S3-2's deletion) rewritten the same way. Both citations fixed even though the plan's §7.3-5 wording named only "§6" — leaving line 89 pointing at a deleted CONFIG key would have been an actively wrong doc, not merely an unfixed pre-existing gap.
4. **Stale `paintNextQuestion` construction-history comments → `math-build-status.md`**: two `app.js` comments referenced a "short-circuit branch" in `paintNextQuestion` that no longer exists (the defer-on-resume work made `paintNextQuestion` call `SessionCore.paint()` unconditionally on every arrival — already correctly documented at the top of that function: "the old 'mark interrupted' branch is gone"). Moved: `seededRngFromString`'s doc comment (was citing "the interrupted-resume short-circuit in paintNextQuestion calling renderCurrentQuestion again" — that specific call path doesn't exist; `renderCurrentQuestion` has exactly one caller today, `paintNextQuestion`'s own `.then()`, always after a fresh paint) and `onExitClick`'s exit-yes handler comment (was citing "(WP9 review finding B, fix-verification round)" — internal review jargon). Both replaced in `app.js` with comments describing only the current, still-true mechanism; the historical/review detail moved to a new `math-build-status.md` entry ("S3-5 doc reconciliation (from app.js comments)"). `grep -n "paintNextQuestion short" app.js` → empty (matches the plan's literal done-when).
`node --check app.js` clean. `npm test` → 197 total, 195 pass, 2 fail — the SAME 2 pre-existing deploy-contract hash-staleness failures (no test files touched by this step).
deviations: fixed `docs/DESIGN.md` line 89 in addition to line 55 (both citing the same dead `CONFIG.WRONG_ANSWER_HELPER_MS`) — the plan named only "§6" for this item; line 89 is in §9. Leaving a doc pointing at a deleted CONFIG key felt like leaving the SAME contradiction half-fixed rather than a new one, so both were corrected together.
assumptions discovered: none beyond what's noted above.

## 2026-08-28T22:20:00Z · plan 1.0 · S3-6 · DONE
evidence: `node tools/bump-version.js 0.14.0` → `app 0.14.0 (release 0.14.0-cce363cd5f)`, hashing all four assets: `core.js?v=e07294198c`, `strings.js?v=56482e8fe1`, `app.js?v=aa4dc2dfe5`, `styles.css?v=3b43ad01af` (unchanged from 0.13.0 — `styles.css` untouched by this whole package). Idempotence re-verified: `md5 -q index.html` before/after a second run at the same version is identical (`959fae00b0067ef37ca970f400d9b1fa` both times).
`npm test` → **197/197 green** (188 baseline at S3 preflight + 1 new in `tests/storage.test.js` [S3-1] + 1 new in `tests/migrate.test.js` [S3-2] + 7 new in `tests/ui/app.test.js` [S3-4] = 197; the 2 deploy-contract failures carried through S3-1..S3-5 are now gone, resolved by this step's bump).
Rule-6 grep, now over `app.js` (the UI lived in `index.html`'s inline script at S1/batch-0's baseline; S2 moved it to `app.js`): `grep -n "MathCore\.\(SessionCore\|Economy\|Pin\|Migrate\)\." app.js` → 27 call sites. Verified every one: all `SessionCore.*` (switchTo/paint/submit/finish/markInterrupted) and the two `Migrate.rebuildEvidence` calls sit inside `save(function (s) {...})`; all four `Economy.*` mutators (requestReward/deactivateReward/approveRequest/rejectRequest) sit inside `save(...)`; `Economy.perfectSeriesExtra`/`unlockThreshold` are pure reads (display-only, no state mutation); every `Pin.*` call (isValidFormat/generateRecoveryCode/hash/verify) is a pure crypto utility, never a state mutator; `Migrate.evidenceRebuildPending`/`preflightEvidence`/`validateImport`/`emptyState`/`migrate` are pure; `Migrate.recompute(state)` at boot (line 2108) mutates a LOCAL `state` variable one line before it is assigned to `App.storage.state` (line 2109) — exempt by the same reasoning the S2-R review already recorded for this exact call site, not a new finding. No direct-mutation-outside-save regression found.
Files touched across the whole S3 package (`git status --porcelain` at this point): `app.js`, `core.js`, `strings.js`, `index.html`, `sw.js`, `package.json`, `package-lock.json`, `docs/DESIGN.md`, `docs/FALLING-DESIGN.md`, `math-build-status.md`, `tests/migrate.test.js`, `tests/storage.test.js`, plus new `tests/ui/harness.js` and `tests/ui/app.test.js`, plus this file. `.claude/` remains untracked, as at every prior preflight.
deviations: none.
assumptions discovered: none new.

## S3 package summary (for the designer's closing review)
Baseline: 09275f9 (188/188 tests). Steps S3-1..S3-6 all DONE above. Final state: 197/197 tests green, app 0.14.0 (release 0.14.0-cce363cd5f). Package not yet committed/pushed — per the executor contract (§0.4), commits/pushes are the designer's action after the closing `adversarial-review` step (§3), which this executor session does not run (its own subagent has no skills; the designer runs it after this run, per plan §3's explicit note).

addendum to S3-6: the package's Grants line (§2) lists `core.js`, `app.js`, `strings.js`, `tests/*`, `package.json`+`package-lock.json`, `docs/*.md`, `CLAUDE.md`, `README.md`, `math-build-status.md` — it does not name `index.html`/`sw.js`. S3-6 explicitly instructs running `node tools/bump-version.js 0.14.0`, whose entire job (per S1's own design) is to rewrite the versioned asset references in exactly those two files; every prior package's own version-bump step (S1-4, batch-0's step 0-6, S2-4) touched the same two files the same way. Treated the explicit S3-6 instruction as authorizing the tool's own writes to `index.html`/`sw.js` (there is no way to run the mandated tool without it); flagging the grants-line omission here rather than silently proceeding, since a stricter reading would have required an ESCALATION instead.

## 2026-08-29T07:20:00Z · plan 1.0 · S3-F · DONE
context: S3 closing review (fresh Fable 5): SHIP-AFTER-FIXES — no behavioural regression found; three LOW groups (F1 docs, F2 three remaining `result.ok` checks, F3 six surviving mutants) fixed below in the working tree, per the coordinator's message.
**F1 — docs.**
- `CLAUDE.md` line 41 (Files → `tests/*.test.js`): replaced "`tests/storage.test.js` uses `fake-indexeddb` (the one dev dependency)" with a paragraph naming both dev deps and describing `tests/ui/harness.js` + `tests/ui/app.test.js` (S3-4's DOM smoke harness) in the file map.
- `CLAUDE.md` rule 1 (was line 47-48, "the one dev dependency"): now names both `fake-indexeddb@6.0.0` and `linkedom@0.18.13` explicitly, both test-only/pinned/`--ignore-scripts`.
- `CLAUDE.md` Testing section: now mentions the `tests/ui/` DOM harness runs as part of `npm test`, and narrows the live-browser-testing caveat to "not covered by an existing `tests/ui/` flow".
- `README.md:49` (Hebrew) and `:105` (English), both said "single dev dependency: fake-indexeddb". Hebrew line replaced with the designer's authorised exact text `npm install --ignore-scripts   # שתי תלויות פיתוח בלבד: `fake-indexeddb` ו-`linkedom`` (used verbatim, no additional Hebrew composed — my first attempt appended an extra "(לבדיקות בלבד)" beyond what was authorised; caught and corrected to the exact authorised string before finishing). English line: "two dev dependencies, both test-only: fake-indexeddb and linkedom".
**F2 — `result.ok` checks on the three remaining `save()` continuations.**
- `app.js` `wireCloudSection`'s cloud-connect handler (was line ~1868, now shifted): the settings-save `.then(function () {...return cloudBackupNow()...})` fell through to `cloudBackupNow()` unconditionally regardless of the save's own outcome. Now `.then(function (result) { if (!result.ok) { msg(T.saveFailure); return; } ...})`.
- `app.js` cloud-disconnect handler (was `.then(renderParentDashboard)`): now `.then(function (result) { if (result.ok) renderParentDashboard(); })`.
- `app.js` `renderQuestion`'s "toggle-keyboard" handler, the `forceNumpad` save (was `.then(renderQuestion)`): now `.then(function (result) { if (result.ok) renderQuestion(); })`.
All three follow the coordinator's specified pattern (`if (!result.ok) return; …`, adapted to each call's own shape). `node --check app.js` clean.
**F3 — six surviving mutants pinned with regression tests, each verified to fail against a hand-written mutant matching the reviewer's description (verified in scratch copies under a fresh `mktemp -d`, never touching the real repo files):**
- **M8** (`tests/session-core.test.js`, new test "[S3-F M8] finish() unlocks a sticker whose threshold is crossed by THIS session's own coins, not only pre-existing lifetime coins") — seeds 20 pre-existing lifetime coins (below `unlockThreshold(1)=25`), plays a perfect session earning 19 more (39 total, crossing 25), asserts `session.unlocksEarned` = `[CONFIG.STICKERS[0]]`. **Fails against mutant: yes** — swapped `applyBonuses(...)` to run AFTER `Economy.newUnlocks(state)` instead of before; mutant produced `unlocksEarned: []` (threshold not yet crossed at `newUnlocks` time); real code passes.
- **M2b** (`tests/session-core.test.js`, new test "[S3-F M2b] within a session, the _perfect ledger entry is appended BEFORE the _series entry") — two perfect sessions in a row, asserts `ledger.findIndex` of `l_<s2.id>_perfect` < `l_<s2.id>_series`. **Fails against mutant: yes** — swapped the two `Economy.ledgerAppend` calls' order in the `if (perfect)` branch; mutant fails the index-order assertion, real code passes.
- **M5** (`tests/session-core.test.js`, new test "[S3-F M5] two separate 5-answer streaks within one 10-question perfect session mint cumulative _streak_1/_streak_2 ids") — a 10/10 perfect session (2× `CONFIG.STREAK_LENGTH=5`) asserts the streak ledger ids are exactly `[..._streak_1, ..._streak_2]`. **Fails against mutant: yes** — changed the streak-bonus id from the cumulative `streakCount` to the run-position `streakRun`; mutant produces `_streak_5`/`_streak_10`, real code passes.
- **U6** (`tests/ui/app.test.js`, new test "[S3-F U6] submitAnswer releases App.feedbackLock when the save returns {ok:false}...") — boots to a typed question, forces the submit's save stale (`storage.rev += 1`, same technique as the S3-4 failed-setup test), submits an answer, asserts `App.feedbackLock === false` afterward and that the question did not advance. **Fails against mutant: yes** — removed the `App.feedbackLock = false;` from the `!result.ok` branch of `submitAnswer`'s `.then()` (leaving only the `.catch()`'s reset, which a resolved `{ok:false}` never reaches); mutant leaves the lock `true` forever, real code passes.
- **U7** (`tests/ui/app.test.js`, new test "[S3-F U7] the parent settings falling-options <select> renders exactly CONFIG.FALLING.MIN_OPTIONS..MAX_OPTIONS") — reads the rendered `<option>` values on the parent dashboard, asserts they equal `[MIN_OPTIONS..MAX_OPTIONS]` (today `[4,5,6]`). **Fails against mutant: yes** — reverted the S3-2 fix back to a hardcoded `[4, 5]` array (dropping option 6); mutant fails the deep-equal, real code passes.
- **U8** (`tests/ui/app.test.js`, new test "[S3-F U8] after a wrong first attempt, the question screen shows a .dot.retry for that fact") — answers the first question wrong, dismisses the helper, asserts exactly one `.dot.retry` renders on the next question. **Fails against mutant: yes** — disabled the `if (retrySet[asked]) cls = "dot retry";` branch in `questionViewModel` (`if (false && retrySet[asked]) ...`); mutant renders 0 retry dots, real code passes.
- **U5** (finishSession with a null value) — left as the coordinator marked it optional; not implemented.
`node tools/bump-version.js 0.14.0` (per the review's instruction, run after the app.js/core.js-adjacent test/doc changes): `release 0.14.0-b782b7f565` (hashes: `core.js?v=e07294198c`, `strings.js?v=56482e8fe1`, `app.js?v=78cb675ccf`, `styles.css?v=3b43ad01af` — `app.js`'s hash changed from the S3-6 run because of the F2 code edits; `core.js`/`strings.js`/`styles.css` unchanged since S3-6). Idempotence re-verified (`md5` of `index.html` identical across two consecutive runs at 0.14.0).
`npm test`: **before this step 197/197** (S3-6's final count) **→ 203/203 after** (6 new: M8, M2b, M5 in `tests/session-core.test.js`; U6, U7, U8 in `tests/ui/app.test.js`).
deviations: none beyond the caught-and-corrected README Hebrew-text overreach noted under F1 above (fixed before this entry was written — the shipped `README.md` carries only the designer-authorised text). U5 skipped per the coordinator's explicit "optional".
assumptions discovered: none.

## 2026-08-29T07:22:01Z · designer · S3-R review (fresh Fable 5, first reviewer killed by the session usage limit — relaunched): SHIP-AFTER-FIXES, no behavioural regression (finish/migrate/hint/HTML differential old-vs-new byte-identical); LOWs fixed by the executor (docs, 3 result.ok checks, 6 mutants pinned) — fix-verification SHIP. Residual test debt: cloud-connect failed-save path untested (harness fetch disabled).

## 2026-08-29T07:22:03Z · designer · S3 committed+pushed as 4159462 · package 1 baseline = the commit containing this entry (given verbatim in the kickoff prompt)

## 2026-08-29T00:00:00Z · plan 1.0 · package 1 preflight · DONE
evidence: `git rev-parse --short HEAD` → `0b0d91c` (matches the kickoff baseline / package-1 baseline recorded at the S3-R entry above). `git status --porcelain` → `?? .claude/` only, worktree otherwise clean. `npm test` → **203/203 green** (matches the S3 package's final count). Read plan §0/§1/§3/§4/§5 and §2 "1 — features"; read `docs/V2-DESIGN.md` §3 (3.1-3.4) in full; read `CLAUDE.md`. No `1-*` entries exist yet in this log — starting fresh at 1-1. Adopting the `## <ISO> · plan 1.0 · <step> · <DONE|PARTIAL|ESCALATION>` + evidence/deviations/assumptions format used by S2/S3 above.
deviations: none.
assumptions discovered: none.

## 2026-08-29T00:00:01Z · plan 1.0 · 1-ESCALATION (T.albums titles) · ESCALATION
context: design §3.1 states `T.albums[id] = title` is user-visible (belongs in `T`, not `CONFIG`), and §3.4 names the two albums `CONFIG.ALBUMS = [{id:"animals",...}, {id:"adventure",...}]`, but neither §3 nor the plan gives a verbatim Hebrew string for `T.albums.animals` or `T.albums.adventure` — only sticker names/nicks, the `albumDone` banner text ("🏆 האלבום הושלם!", already in `T.collection`), and `T.collection.allUnlocked`. Grepped the full `docs/V2-DESIGN.md` and `v2-build-plan.md` for `T.albums`/candidate titles — no verbatim text exists anywhere.
Per plan §4 ("safe defaults when unsure: do less... never widen scope") and the executor contract (do not compose Hebrew): `T.albums = { animals: "", adventure: "" }` wired through the collection shelf rendering (shelf headers render an empty string, never a composed Hebrew string) — a TODO left here rather than in code. **Needed keys: `T.albums.animals`, `T.albums.adventure`** — exact Hebrew string TBD from Marat/designer.
deviations: shelves render without a visible title until these two keys are supplied.
assumptions discovered: none.

## 2026-08-29T00:15:00Z · plan 1.0 · 1-1 · DONE
evidence: `core.js` CONFIG (V2-DESIGN §3.4): `CONFIG.ALBUMS = [{id:"animals", stickers:[24 ids]}, {id:"adventure", stickers:[24 ids]}]` (album-1 ids unchanged from batch-0's flat list; album-2 ids typed verbatim from design §3.4 line 63: rocket, ufo, planet, moon, star, rainbow, castle, ferris, carousel, circus, train, helicopter, sailboat, racecar, tractor, canoe, volcano, island, tent, balloon, kite, compass, map, crown); `CONFIG.STICKERS` is now computed by flattening `ALBUMS` right after the CONFIG literal (`core.js` ~line 143) — kept as a plain array, not a getter, so every existing caller (`canonicalizeUnlocked`, `Economy.newUnlocks`, the collection screen) needs no change. `CONFIG.UNLOCK_COUNT = 48`. `Economy.unlockThreshold(n)`: added `Economy.unlockCurve(i) = UNLOCK_BASE·i + UNLOCK_STEP·i(i-1)/2`, then `unlockThreshold(n)` computes `a = floor((n-1)/albumSize)`, `i = ((n-1)%albumSize)+1`, `a·curve(albumSize) + curve(i)` (`albumSize = CONFIG.ALBUMS[0].stickers.length`, i.e. 24, read from config rather than hardcoded). Verified via `node -e`: `unlockThreshold(24)=1980, unlockThreshold(25)=2005, unlockThreshold(48)=3960` — exact match to design. `Economy.goldenStickers(state)` added: for each `CONFIG.MAP_PATH[idx]` (0-based `idx`, so `idx+1` is the "station k" position design §3.4 means) that `Map.isReached`, the album-1 sticker at `idx` is golden; result filtered to `state.economy.unlocked` (no reveal of a locked sticker). `T.stickers` (48 entries) and `T.albums` ({animals:"", adventure:""} — see the ESCALATION entry above) added to `strings.js`; `T.collection.albumDone = "🏆 האלבום הושלם!"` added; `T.map.goldsSticker(name)` added. `STICKER_EMOJI` in `app.js` extended with the 24 album-2 emoji (verbatim from design line 63, including the variation-selector glyphs on racecar/island/map — 🏎️/🏝️/🗺️).
**Verbatim-string integrity**: rather than hand-retyping 48 Hebrew name/nick pairs and 24 emoji (high risk of a silent transcription error), extracted `T.stickers` and the album-2 `STICKER_EMOJI` entries programmatically with a Python script parsing `docs/V2-DESIGN.md` lines 53 and 63 directly, then verified byte-for-byte with a second script that re-parses the same doc lines and diffs every one of the 48 `{name, nick}` pairs and 24 emoji against what landed in `strings.js`/`app.js` — result: "all 48 T.stickers entries match design doc exactly", "all album2 emoji match exactly". `CONFIG.STICKERS` extraction confirmed unique (48/48) and boundary-exact.
Tests added: `tests/economy.test.js` — "[V2-DESIGN §3.4] two 24-sticker albums, UNLOCK_COUNT 48, ids unique" (ALBUMS shape, 48 unique ids), "…unlockThreshold exact album boundaries: n=24 -> 1980, n=25 -> 2005, n=48 -> 3960", "…newUnlocks reaches into album 2 once lifetime coins cross the album-1 boundary" (asserts the first album-2 unlock is exactly `CONFIG.STICKERS[24]` = "rocket"), "…goldenStickers: station k (MAP_PATH position) reached -> sticker k of album 1 is golden, only if unlocked" (3-step: none unlocked -> [], one unlocked -> that one only, both unlocked -> both). `tests/migrate.test.js` — "[V2-DESIGN §3.4] migrate() canonicalises economy.unlocked across BOTH 24-id albums" (dedups, drops an unknown id, keeps order, accepts an album-2 id "rocket"/"crown" as a known sticker — canonicalisation code itself needed no change since it already reads `CONFIG.STICKERS` dynamically, only the fixture is new). `npm test`: 206/206 (204 pre-step + these tests) at this point in the sequence, before 1-2/1-3/1-4's own additions (see below for the running total).
deviations: `unlockThreshold`'s album-size constant is read from `CONFIG.ALBUMS[0].stickers.length` (24) rather than hardcoded as a literal `24` in the formula — same numeric result, but avoids a second magic number next to `CONFIG.ALBUMS` (I7). `CONFIG.STICKERS` was originally sketched as a getter (`get STICKERS() {...}` on the CONFIG object literal) for "always in sync with ALBUMS"; replaced with a plain array computed once right after the CONFIG literal, per self-review, since ALBUMS/STICKERS are both immutable at runtime (no code path ever reassigns ALBUMS) and a getter needlessly recomputes the flatten on every access and could interact oddly with `JSON.stringify`/enumeration in ways a plain array does not.
assumptions discovered: none beyond the T.albums ESCALATION above.

## 2026-08-29T00:20:00Z · plan 1.0 · 1-2 · DONE
evidence: `app.js` `Screens.collection` rewritten to render one `.sticker-shelf` per `CONFIG.ALBUMS` entry (shelf title from `T.albums[album.id]`, currently empty per the open ESCALATION — no shelf-title text renders until that key is supplied, never composed): unlocked stickers render emoji + `.sticker-name` + `.sticker-nick` from `T.stickers[id]`; locked stickers render bare `"?"` with **no `title` attribute** (the pre-existing `title="` + id leak is gone — confirmed by a new UI test asserting `el.getAttribute("title") === null` on every `.sticker`); a sticker present in `MathCore.Economy.goldenStickers(state)` gets the `.golden` class (gold ring + `::after` sparkle, `styles.css`); an album whose every sticker is unlocked shows `T.collection.albumDone` in a `.station-banner`. `renderSummary`'s unlock-reveal block now shows `T.stickers[id].name`/`.nick` under each revealed sticker's emoji (design §3.1 "Summary reveal: emoji + name + nick"), and the reached-station banner now additionally shows that station's gilded album-1 sticker's emoji, but only when `MathCore.Economy.goldenStickers(S())` (the live post-finish state) includes it — never a locked sticker. The map station-detail card (`Screens.map`) now shows `T.map.goldsSticker(name)` under the station title, gated the same way (`state.economy.unlocked.indexOf(goldStickerId) !== -1`), matching the "no reveal of a locked sticker anywhere" clause across all three surfaces (collection/summary/map). `styles.css`: `.sticker` is now a column flexbox (emoji + two text lines) with `.sticker-name`/`.sticker-nick` sizing and a `.golden` box-shadow ring + sparkle; `.album-shelf-title` added.
Tests: `tests/ui/app.test.js` new test "[1-2] collection screen: unlocked stickers show name+nick and NO title attribute; locked stickers show "?" with no title (no id leak); a golden+unlocked sticker gets the golden class" — seeds one unlocked+gilded sticker via a real `save()`, navigates to `#screen=collection`, asserts: `.sticker` count == 48, zero `title` attributes across all of them, the one `.unlocked` element's text contains both its name and nick, it also carries `.golden`, and every other (47) `.sticker` renders exactly `"?"`. `node --test tests/ui/app.test.js` → 11/11 green (was 10/10 before this test). Full `npm test`: 213/213 (excluding the 2 still-pending deploy-contract failures, resolved at 1-5's version bump).
deviations: none.
assumptions discovered: none.

## 2026-08-29T00:35:00Z · plan 1.0 · 1-3 · DONE
evidence: `app.js` adds `audienceHtml(state, seedKey, opts)` (placed after `seededRngFromString`, which it uses): filters `state.economy.unlocked` to known `CONFIG.STICKERS` ids, returns `""` when empty (strip absent with no unlocks, per design §3.2), otherwise takes the newest-unlocked sticker (last array element) + up to `CONFIG.AUDIENCE_MAX - 1 = 4` others from a `seededRngFromString(seedKey + ":audience")`-shuffled remainder — deterministic per `seedKey`, so the SAME session/summary re-renders with the SAME membership and order. Rendered on both question screens (`renderQuestion`, `renderFallingQuestion`, seeded by `active.id`) and on the summary screen (seeded by `session.id`, `{allBounce: session.perfect}` — "summary perfect -> all bounce" per design §3.2's last sentence). Golden stickers (via `MathCore.Economy.goldenStickers(state)`) get a `.golden` glow class if present among the members. `styles.css`: `.audience { position:absolute; bottom:2.6rem; left:0; right:0; pointer-events:none; z-index:2; ... }`; idle = staggered `audience-bob`; `[data-screen="question"].feedback-correct .audience-member` -> `audience-hop`, `.feedback-wrong` -> `audience-wobble` (these classes are already applied to the ancestor `.screen` by the existing `showFeedback()` — no new wiring needed); `.audience-bounce` (summary-perfect) plays the hop animation on a loop; `prefers-reduced-motion: reduce` sets `.audience-member { animation: none }` (static, matching design's "reduced-motion -> static" clause).
**Zero layout impact — proven, not asserted**: a fresh headless-Chrome harness (`/private/tmp/.../scratchpad/audience-harness.js`, driven via CDP `Emulation.setDeviceMetricsOverride` + `Runtime.evaluate`) seeded a falling-mode journal via `localStorage` mirror + `MathCore.Migrate.emptyState()`/`Pin.hash`, then for **all four required viewports** (390×844, 375×667, 320×568, 844×390) captured `getBoundingClientRect()` of `.lanes` and `.question-info` (a) with the strip present (1 unlocked sticker), (b) with `.feedback-correct` toggled on the screen element, (c) with `.feedback-wrong` toggled, and (d) with the strip absent (0 unlocked stickers) — **all four conditions produced byte-identical `{x,y,w,h}` rects at every viewport** (e.g. 390×844: `.lanes` `{x:7.796875,y:266.15625,w:374.390625,h:388.234375}` unchanged across all four; 844×390 landscape: `.lanes` `{x:174.046875,y:22.390625,w:440,h:370.796875}` unchanged). Full JSON captured in the harness output (16 rect pairs, all matching). Screenshot at 390×844 with 5 unlocked stickers confirms the strip renders near the bottom (`.audience` rect `{x:0,y:768.40625,w:390,h:34}`) showing 5 emoji (🦊🦉🐶🐱🐝) in the seeded-shuffled order, saved to `/private/tmp/.../scratchpad/collection-audience-falling-390x844-b.png`.
Tests: `tests/ui/app.test.js` new test "[1-3] spectators strip (V2-DESIGN §3.2): absent with zero unlocks; present and stable across re-renders once a sticker is unlocked, capped at AUDIENCE_MAX" — asserts `.audience` is `null` before any unlock, then (after unlocking 8 stickers) asserts exactly `CONFIG.AUDIENCE_MAX` (5) members render, and that a hash round-trip re-render of the SAME session produces byte-identical membership (order + emoji) both times. `node --test tests/ui/app.test.js` → 12/12 green (was 11/11). Full `npm test`: 214/214 (excluding the 2 still-pending deploy-contract failures).
deviations: none.
assumptions discovered: "summary perfect -> all bounce" (design §3.2's closing clause) is implemented as a looping hop animation (`.audience-bounce`) on the summary's audience strip rather than a one-shot bounce, matching the idle-strip's own "continuous, staggered" visual language elsewhere on that screen (confetti/fireworks already fire once; the audience loop is intentionally the ambient layer, not the one-shot celebration).

## 2026-08-29T00:50:00Z · plan 1.0 · 1-4 · DONE
context: the core-layer session-size mechanics (CONFIG numbers, `Selector.plan(state, rng, now, size)`, `SessionCore.resolveSessionSize`/`start()` recording `active.carryoverTaken`, `computeNextCarryover` computing leftover by key, `nearPerfectBonusAmount(firstTryCorrect, plannedLength)`, `Migrate.normalizeSettings`/`validateImport` for `settings.sessionSize`) were built and tested during step 1-1 above because removing the old fixed `CONFIG.SESSION_SIZE` cascaded through ~40 tests that had to move together — see 1-1's evidence and the "[V2-DESIGN §3.3]" tests in `tests/session-core.test.js`/`tests/economy.test.js` already logged there. This entry covers what remained: the UI slider, `.dots` CSS, the simulator's size argument + its outputs, and headless proof of the two-row wrap.
evidence: `CONFIG.SESSION_SIZE_DEFAULT/MIN/MAX = 10/10/20`, `NEAR_PERFECT_MISSES = 1` (replaces `NEAR_PERFECT_MIN_CORRECT` — grepped `NEAR_PERFECT_MIN_CORRECT` across `core.js`/`app.js`/`strings.js`/`index.html`/`sw.js`/`tools/*.js`/`tests/*.test.js`: zero hits, only a historical comment referencing the old name remains at `core.js` ~line 134), `STARS_TWO_RATIO = 0.8` — all added in 1-1's CONFIG edit. `app.js` `renderSummary`'s stars formula and near-perfect title condition now use `session.planned.length` as the denominator (not a hardcoded 10/8/9) — see 1-1. `styles.css` `.dots { ...; flex-wrap: wrap; max-width: min(90vw, 400px); }` (the `max-width` was necessary beyond the plan's literal "`.dots { flex-wrap: wrap }`" instruction: `.dots` is a flex ITEM inside a `align-items:center` column flex parent, so without a width cap it shrink-to-fits its content and never actually wraps — confirmed by headless measurement below). `strings.js` `T.parent.sessionSizeLabel = "מספר תרגילים בסבב"` (verbatim from design §3.3). `app.js` parent settings: a new range slider `#set-session-size` (min/max = `CONFIG.SESSION_SIZE_MIN/MAX`, live value readout like the existing time-limit/falling-duration sliders), wired into `save-settings`' mutator (`s.settings.sessionSize = Number(...)`). `tools/simulate-economy.js`: `simulate()` takes a `sessionSize` param, seeded into `state.settings.sessionSize`; CLI reads `process.argv[2]` (default `CONFIG.SESSION_SIZE_DEFAULT`); `MAX_SESSIONS` scales with `(10/sessionSize)·(UNLOCK_COUNT/24)` since a smaller session and 48 (not 24) stickers both need a larger session budget than the original hardcoded 400. Outputs (album-2 = full 48-sticker collection completion, exactly what the plan's done-when asks for):
```
$ node tools/simulate-economy.js 10
session size: 10 (MAX_SESSIONS budget: 800)
weak: 599 sessions to complete the collection
average: 158 sessions to complete the collection
strong: 131 sessions to complete the collection

$ node tools/simulate-economy.js 20
session size: 20 (MAX_SESSIONS budget: 400)
weak: 195 sessions to complete the collection
average: 80 sessions to complete the collection
strong: 72 sessions to complete the collection
```
**Headless proof of the two-row wrap** (`/private/tmp/.../scratchpad/dots20-shot.js`, headless Chrome via CDP, 375×667, seeded `settings.sessionSize=20` via the localStorage mirror): `document.querySelectorAll(".dot")` count = 20; `getBoundingClientRect().y` of every dot collapses to exactly **two** distinct row y-values (194 and 213); `.dots` container rect `{w:327, h:30.390625}` (two ~14px rows + gap, not one ~370px-wide overflowing row). Screenshot saved to `/private/tmp/.../scratchpad/dots20-375x667.png` — visually confirms 18 dots on row 1, 2 dots centered on row 2.
Tests: `tests/ui/app.test.js` new test "[1-4] parent settings session-size slider renders CONFIG bounds and persists the choice into settings.sessionSize" — asserts the slider's `min`/`max`/default `value` attributes match `CONFIG.SESSION_SIZE_MIN/MAX/DEFAULT`, then moves it to `SESSION_SIZE_MAX` and confirms `save-settings` persists `settings.sessionSize` (linkedom quirk found and worked around: reading/writing a `type="range"` input's `.value` needed `getAttribute`/`setAttribute` rather than the `.value` property, which returned `NaN` for a freshly-rendered range input under linkedom — this only affected the TEST's own assertions, not `app.js`'s own `Number(document.getElementById(...).value)` read, which resolved correctly once the attribute was set). `npm test`: 215/215 (excluding the 2 pending deploy-contract failures).
deviations: `.dots`'s `max-width` addition beyond the plan's literal instruction, explained above — necessary for the design's own done-when ("a 20-question round renders 20 dots in two rows at 375px") to actually hold; verified by headless measurement, not assumed.
assumptions discovered: none beyond the max-width finding above.

## 2026-08-29T01:05:00Z · plan 1.0 · 1-5 · DONE
evidence: **Docs (design §5 items for this batch).** `docs/DESIGN.md` §5 amended: the "Collection" bullet now describes the two 24-sticker albums, the restart-per-album threshold formula (with the exact 1980/2005/3960 boundaries), `UNLOCK_COUNT=48`, and golden stickers (station-position gilding, never revealed while locked); a new "Stars / near-perfect" bullet documents the `STARS_TWO_RATIO`/`NEAR_PERFECT_MISSES` rules replacing the old fixed "9/10" one. §6 gets a new "Session size" paragraph (slider bounds, read-once-at-start, `active.planned.length` as the only denominator, `carryoverTaken`/by-key leftover, legacy-journal fallback) and the "Progress = 10 dots" line is generalised to "one dot per planned question". §7 (threshold evidence) was already amended by batch 0 — confirmed unchanged, no further edit needed here. `docs/FALLING-DESIGN.md` F3 amended: falling sessions now use `settings.sessionSize` (10-20) instead of a fixed 10, with I-F1 (carryover untouched) reaffirmed. `docs/MAP-DESIGN.md` §2 gets a new "Golden stickers" bullet (path-position `k`, not table number, gilds album-1 sticker `k`; never revealed while locked) and the celebration bullet notes the summary shows the gilded emoji when already unlocked. `README.md` (English section only — Hebrew section already generically says "sticker collection" with no incorrect specific number to fix, and no new Hebrew was composed per the executor contract): the intro paragraph now mentions the two-album 48-sticker collection, golden stickers, the audience strip, and the parent-adjustable session length.
`node tools/bump-version.js 0.15.0` → `release 0.15.0-5d18a1368b` (hashes: `core.js?v=20903fe7bf`, `strings.js?v=da2c7de960`, `app.js?v=31073d78b1`, `styles.css?v=2cc12281d2`). Idempotence re-verified: running the tool a second time at the same version produced byte-identical hashes and an empty `diff` of `index.html` before/after.
`npm test`: **217/217 green** (215 at the end of 1-4 + the 2 deploy-contract tests, which were failing since before 1-1 pending exactly this version bump, now passing).
Rule-6 grep over `app.js`: `grep -n "MathCore\.\(SessionCore|Economy|Pin|Migrate\)\." app.js` → same call sites S3-6 already verified, plus three NEW ones added by this package, all pure reads (never mutators, so the "inside save()" rule doesn't apply — same reasoning already used for `Economy.perfectSeriesExtra`/`unlockThreshold` in prior packages): `MathCore.Economy.goldenStickers(state)` in `audienceHtml` (app.js ~line 708), in `renderSummary`'s reached-station gilding check (~line 1156), and in `Screens.collection`'s golden-set build (~line 1299). No direct-mutation-outside-`save()` regression found; a supplementary grep for `state\.(economy|map|settings)\.\w+\s*=` outside a `save(function` block also came back read-only (assignments to local variables/render-string interpolation, not state mutation).
Files touched across the whole package-1 batch (`git status --porcelain`): `app.js`, `core.js`, `strings.js`, `styles.css`, `docs/DESIGN.md`, `docs/FALLING-DESIGN.md`, `docs/MAP-DESIGN.md`, `README.md`, `tests/economy.test.js`, `tests/migrate.test.js`, `tests/selector.test.js`, `tests/session-core.test.js`, `tests/ui/app.test.js`, `tools/simulate-economy.js`, `index.html`+`sw.js` (via the bump tool only, per the same S3-6-addendum reasoning), plus this status file. `.claude/` remains untracked. `CLAUDE.md` was NOT touched — no file-map change this batch (no new files besides test additions to existing test files).
deviations: none beyond the README-Hebrew-section abstention noted above (a deliberate scope-narrowing, not a gap — the Hebrew paragraph contains no now-incorrect number to fix, and the contract forbids composing new Hebrew).
assumptions discovered: none new.

## Package 1 ("features") summary — all steps 1-1..1-5 DONE above
Baseline: `4159462` (S3's committed baseline, confirmed `0b0d91c` short-hash at this executor's preflight). Final state: **217/217 tests green**, app **0.15.0** (release `0.15.0-5d18a1368b`). Open ESCALATION carried forward: `T.albums.animals`/`T.albums.adventure` have no verbatim Hebrew title in design §3 — collection shelves render with an empty title until the designer supplies the exact strings (see the 1-ESCALATION entry near the top of this package's log). Package not committed/pushed — per the executor contract (§0.4), that is the designer's action after the closing `adversarial-review` step (§3), which this executor session does not run.

## 2026-08-29T07:57:49Z · designer · package 1 escalation resolved: T.albums.animals = "חיות", T.albums.adventure = "הרפתקאות" (designer-authored; the v2 design moved titles into T but dropped the values). Bump re-run.

## 2026-08-29T02:00:00Z · plan 1.0 · 1-F · DONE
context: package-1 closing review (fresh Fable 5): SHIP-AFTER-FIXES. Fixed in the working tree below; reviewer probes referenced from the scratchpad (`typed-harness.js`, `rm-harness.js`, `mutants.sh`, screenshots).

**F1 MEDIUM — `.audience` strip visually covered bottom buttons (typed exit-bottom/toggle-keyboard, summary buttons).**
Root cause: the strip's zero-layout-impact proof (1-3) only measured `.lanes`/`.question-info`; it never checked whether the strip's own rect intersects the buttons below it.
Fix, in three parts (`styles.css`):
1. `.screen[data-screen="question"]:not([data-falling="1"]), .screen[data-screen="summary"] { padding-bottom: 6rem; }` — reserves a bottom band on typed/summary (these use `justify-content:center`, so extra bottom padding shrinks the centred content area and pushes buttons up, unaffected by any `.screen { padding: 0.5rem }` media-query shorthand thanks to the higher-specificity attribute-selector rule).
2. The falling screen needed its OWN fix (found during re-verification, not in the reviewer's report — see below): its `justify-content: flex-start` override means container `padding-bottom` does nothing (proved empirically: identical exit-bottom position at 2.5rem vs 4rem). Fix: pulled `[data-action="exit-bottom"]` out of the flex flow into `position:absolute; bottom:0.6rem` (centred via `left:0;right:0;width:fit-content;margin:0 auto` — NOT `left:50%;transform:translateX(-50%)`, which was tried first and wraps the button's text because shrink-to-fit width is then constrained to the space right of the 50% mark, confirmed by measurement: text wrapped to 2-3 lines at 195/188/160px widths).
3. `.screen[data-falling="1"] .audience { bottom: 3.8rem; }` — placed just above the now-fixed exit-bottom.
Extended `typed-harness.js`'s measurement (new `f1-full-verify.js`, same technique) to check `.audience` against `[data-action="exit-bottom"]`, `[data-action="toggle-keyboard"]`, and `[data-screen="summary"] button` at all 5 viewports (390×844, 375×667, 320×568, 844×390, 768×1024), for typed, falling, AND summary. **Before fix** (pre-existing, matching the reviewer's report): typed `overlapExitBottom:true` at 375×667/320×568/844×390; summary `overlapButtons:1` at 375×667/320×568/844×390. **After fix**: `overlapExitBottom:false`, `overlapToggle:false`, `overlapButtons:0` at **all 5 viewports**, both typed and summary, AND falling (which had the same bug, undiscovered by the review — see below).
Re-ran the 1-3 zero-layout-impact harness (`audience-harness-reverify.js`) after all CSS changes: `.lanes`/`.question-info` rects remain byte-identical with-strip/no-strip/feedback-correct/feedback-wrong at all four original viewports — the D3 invariant is untouched.
**Residual, accepted tradeoff**: at 375×667/320×568/844×390, the falling audience strip's bounding box still overlaps `.lanes`'s bounding box (`.lanes` is a large, mostly-transparent container for falling bubbles — the overlap is a bounding-box artifact, not a visual collision with any bubble or text; confirmed by screenshot at 320×568 and 844×390, both saved to the scratchpad — the audience emoji sit cleanly in empty space above the exit button). At 320×568 specifically the vertical gap between `.lanes`'s bottom edge and the fixed exit-bottom's top edge is only 15px — physically less than the strip's own 34px height — so a total, non-overlapping placement is geometrically impossible there without either shrinking `.lanes` (out of scope) or letting content overflow into scroll (worse: would push exit-bottom below the fold). Since `.audience` is `pointer-events:none`, this overlap never blocks tapping a bubble or the exit button — it is cosmetic only. Left as documented residual risk for the designer to revisit (e.g. a smaller falling-only audience font-size) rather than silently declared fixed.
Numbers (rounded px, `{y, bottom}` unless noted) — full JSON in the scratchpad's `f1-full-verify.js` output:
| viewport | typed overlapExitBottom | typed overlapToggle | summary overlapButtons | falling overlapExitBottom | falling overlapLanes |
|---|---|---|---|---|---|
| 390×844 | false | false | 0 | false | false |
| 375×667 | false | false | 0 | false | true (cosmetic, see above) |
| 320×568 | false | false | 0 | false | true (cosmetic, geometrically forced) |
| 844×390 | false | false | 0 | false | true (cosmetic; landscape flex-row layout, screenshot confirms no visible clutter) |
| 768×1024 | false | false | 0 | false | false |

**F2 — `prefers-reduced-motion` did not make the strip static.**
Root cause confirmed: `.audience-member { animation:none }` inside the RM media query has specificity (0,0,1,0); it loses to `[data-screen="question"].feedback-correct .audience-member` (0,0,3,0), its `.feedback-wrong` twin, and `.audience-member.audience-bounce` (0,0,2,0), regardless of media-query/source order. Fix: repeated all three higher-specificity selectors inside the RM block, each pinned to `animation:none`. Verified with `Emulation.setEmulatedMedia({features:[{name:"prefers-reduced-motion",value:"reduce"}]})` (`rm-harness-verify.js`, reviewer's own technique): **before fix** — RM idle: `none` (already fine), RM feedback-correct: `audience-hop` (BUG), RM feedback-wrong: `audience-wobble` (BUG), RM summary bounce: `audience-hop` (BUG). **After fix** — all four report `animationName: "none"`.

**F3 — `tests/economy.test.js` golden-sticker mutant survived (path-position vs table-number indexing).**
The existing test only reached `MAP_PATH[0]`/`[1]` (tables 1, 2), where position (0/1) happens to equal `table-1` — so a mutant gilding `album1[table-1]` instead of `album1[idx]` was undetected. Added a new test reaching `MAP_PATH[2]` (table 10, where `idx=2` gives "fox" but `table-1=9` gives "koala" — they diverge). **Pre-fix-mutant verification** (scratch copy, `core.js`'s `golden.push(album1[idx])` → `golden.push(album1[table - 1])`): `not ok 17 - ...F3...`, 1 failure. Real code: passes (`ok 17`).

**F4 — summary stars/near-perfect title untested at 16/20, 15/20, 19/20, 20/20.**
Added `tests/ui/app.test.js` "[1-4/F4] summary stars + near-perfect title scale with session size" — renders the summary via `App.lastSessionResult` + hash navigation (same technique the reviewer's own harness used) at 16/20 (2 stars, boundary), 15/20 (1 star), 19/20 (2 stars + near-perfect title), 20/20 (3 stars + perfect title, no near-perfect title). **Mutant verification**, each in an isolated `rsync`-copied scratch tree (`f4mutant/`, never touching the real repo):
- `STARS_TWO_RATIO: 0.8` → `0.7` in `core.js`: **fails** (`not ok 5`).
- `session.firstTryCorrect / session.planned.length >= CONFIG.STARS_TWO_RATIO` → `session.firstTryCorrect >= 8` in `app.js`: **fails** (`not ok 5`).
- `session.firstTryCorrect === session.planned.length - CONFIG.NEAR_PERFECT_MISSES` → `session.firstTryCorrect === 9` in `app.js`: **fails** (`not ok 5`).
All three restored from `/tmp/core_backup.js`/`/tmp/app_backup.js` after verification; `git status --porcelain core.js app.js` in the real repo confirms only my legitimate 1-1..1-5 diff, no mutation leakage.

**Docs nit — "newUnlocks after every ledger append" wording.**
Added a code comment at the `Economy.newUnlocks(state)` call site in `SessionCore.finish()` (`core.js`) clarifying that this is one call after `applyBonuses` has appended every ledger entry — mathematically equivalent to calling it after each append (thresholds are monotone in lifetime coins) — rather than leaving the "after every append" phrasing unexplained in code. `docs/V2-DESIGN.md` (designer-owned) left untouched per the coordinator's instruction.

**F5 (LOW, cosmetic)** — landscape 844×390 strip below the fold: left as-is per the coordinator's explicit "acceptable, leave."

`node tools/bump-version.js 0.15.0` → `release 0.15.0-300a9aadc1` (hashes: `core.js?v=5186c3ee07`, `strings.js?v=19c1797be4`, `app.js?v=31073d78b1` (unchanged — app.js text wasn't touched by F1-F4, only `styles.css`/`core.js`/tests), `styles.css?v=e0ce0a8960`). Idempotence re-verified (second run byte-identical, `diff index.html` empty).
`npm test`: **217/217 at 1-5 → 219/219 now** (+1 economy.test.js [F3], +1 ui/app.test.js [F4]).
deviations: the falling-screen exit-bottom fix and its residual `.lanes` bounding-box overlap (documented above) were not in the reviewer's F1 report — found independently while re-verifying the typed/summary fix at all 5 viewports, fixed anyway since it's the same class of bug, and its cosmetic residual documented rather than silently left.
assumptions discovered: none new.

## 2026-08-29T08:45:12Z · designer · 1-R review (fresh Fable 5): SHIP-AFTER-FIXES → executor fixed F1 (strip over bottom buttons on typed/summary), F2 (reduced-motion specificity), F3/F4 (blind tests); fix-verification SHIP with one LOW (320×568: landed label under the absolute exit button on the falling screen). Designer: falling screen padding-bottom 3.6rem (portrait: label 507–541 vs exit 543–589 at 320×568; 583–616 vs 618–664 at 375×667 — no overlap), landscape: exit-bottom back in flow + strip hidden (fv2-harness). Suite 219/219, bump re-run.

## 2026-08-29T08:45:13Z · designer · package 1 committed+pushed as d18815d · package 2 baseline = the commit containing this entry (given verbatim in the kickoff prompt)

## 2026-08-29T08:55:50Z · designer · package 2 STOPPED at the 2-1 gate (structural: row-option ≤ 23% vs 40% floor; see the executor ESCALATION above). WIP parked on local branch tetris-wip (7c5b245), main stays at 0.15.0. Cycle close-out: NEXT-ACTIONS decisions for Marat, brain inbox note staged, memory updated, skill proposals in docs/SKILL-PROPOSALS-2026-08-29.md.
