# Skill scouting — proposals from the math-tables cycles (2026-08-25 → 28)

Per the design-to-build close-out rule: proposals only, nothing auto-created. Marat decides placement and whether to create.

## New skills
1. **`publish-webapp`** (global `~/.claude/skills/`) — take a static single-page app to "usable from any device": preflight (static? domain classification → allowed hosts; personal-data/secret grep), hosting decision table (GitHub Pages public / Cloudflare Pages + Access / Tailscale for anything work-classified or data-bearing), PWA wrapper (manifest, apple-touch-icon, service worker with `VERSION`, `cache:"reload"` precache, `core.js?v=` + the **version-in-three-places** rule, update toast only on Home, first-install toast suppression), iOS specifics (Add to Home Screen ≠ Safari-tab storage; a download link only *previews* on iOS → use the share sheet; AirDropped HTML runs no JS), data safety (last-good ratchet snapshot + restore offer; optional private-Gist backup with a gist-scope token kept on device; export strips secrets; import never adopts tokens), repo hardening (branch protection, issues/wiki off, 2FA), and the **live-test ritual** (unregister SW + delete caches + cache-busted URL before every check). Proven on this project; the mac-webapp skill should point to it for anything that must run on iOS.
2. **`always-on-tailnet-host`** (later, only if tarbut takes option 2) — provision a small VPS, install Tailscale, run a Flask/SQLite app as a systemd service, DB sync/backup, `.command` launchers pointing at the tailnet name, minimal login. Create after it has been done once.

## Updates to existing skills
- **`design-to-build`**: (a) verify against the user's *original wording*, not the plan — the 2026-08-26 verification found the "she'll want to play" layer thin while every plan item was green; (b) when Codex is unavailable (out of credits) the closing reviewer falls back to a fresh Fable agent — write the fallback into Stage 4; (c) reviewer agents can be killed by the session usage limit — the designer can re-run the reviewer's harness itself (keep harness scripts in the scratchpad); (d) a game/UX feature needs a *visual* verification gate, not only tests: require screenshots at named viewports or a headless-Chrome layout harness.
- **`handoff-plan`**: the executor's browser tool cannot resize the viewport and runs the page in a hidden tab (timers throttled, CSS animations paused) — plans with layout work must mandate a headless-Chrome measurement (`--headless=new --window-size … --dump-dom` with an iframe wrapper + `--allow-file-access-from-files`) instead of "screenshot at 320×568"; also require the executor to grep the parent's original request into a checklist.
- **`adversarial-review`**: add the headless layout harness as a standard tool for CSS/layout diffs ("measure, don't reason": the 2026-08-27 review caught lanes collapsing to 0 px in portrait that reasoning had missed); add "text-splice patches: assert non-overlapping ranges" to the mock-honesty checklist (a bad `s.index` slice duplicated 40 lines of `validateImport` and the review caught it only via "problems reported twice").
- **`mac-webapp`**: add a note that its localhost launcher + `file://` pattern cannot serve iOS; link to `publish-webapp` for phone/tablet use; the dual-write storage idea transfers.
- **`context-reset`**: fine as is.
